# מיפוי חוב טכני — Handy

תאריך הביקורת: 2026-09-03 · ענף: `main` · commit: `b7eafe5`

> **עדכון 2026-09-06** — Docker הפך זמין, ושני הפריטים שהיו חסומים עליו טופלו:
> **#22 תוקן**, ו-**#13 נבדק בניסוי ונדחה** — הסעיפים אינם עצמאיים, וההוכחה בפרק 9.
> באותה הרצה התגלתה **רגרסיה שאני הכנסתי ב-#21** והייתה כבר על `main`: קובצי
> ה-`loading.tsx` הפכו 404 ל-200 בחמישה מסכים מאחורי התחברות. תוקן. שום מידע לא
> נחשף. הפירוט המלא בפרק 9.
>
> **עדכון 2026-09-05** — **19 פריטים נוספים נסגרו** בענף
> `fix-technical-debt-batch`, וכל רשימת "מוכן להתחיל מיד" מוצתה למעט שניים.
> **#13** ו-**#22** נדחו: שניהם דורשים Docker ו-Supabase מקומי כדי לאמת, ולא היו
> זמינים. **#30** נסגר כ"אינו באג" אחרי בדיקה במקור. פרק 8 בסוף מפרט.
>
> **עדכון 2026-09-04** — פריטים **#20** (אין `error.tsx`) ו-**#40** (בליעת שגיאות)
> **תוקנו**, בענף `fix-error-observability`. **#41** (מוניטורינג) טופל חלקית: כל
> כשל נרשם היום כשורת JSON ל-stderr, בלי ספק ובלי עלות; בחירת ספק נשארה פתוחה.
> ספירת הענפים ב-#40 עודכנה מ-26 ל-**30** — ה-grep המקורי החמיץ ארבעה ענפים
> ששם המשתנה בהם אינו `error` (`payoutError`, `categoryError`, `nameError`,
> `profileError`). הפירוט המלא בפרק 7, "מה תוקן".

מסמך זה נכתב כ**מיפוי בלבד**, ולא בוצע שום שינוי בקוד במהלך הביקורת עצמה.
התיקונים שבוצעו מאוחר יותר, באישור, מרוכזים בפרק 7 ומסומנים בגוף הטבלאות.

כל ממצא כאן נבדק בפועל — `npm audit`, `npm outdated`, `npm run lint`, `tsc --noEmit`,
`npm run test`, `npm run build`, `npm run format:check`, וקריאה של המיגרציות, ה-CI,
ה-README וקוד האפליקציה — ולא נוחש מתוך היכרות כללית עם פרויקטים דומים.

---

## 1. תקציר מנהלים

**סה"כ נמצאו 42 פריטי חוב טכני.**

| סיווג             | כמות |
| ----------------- | ---- |
| ניתן לפתרון עצמאי | 27   |
| דורש את מעורבותך  | 15   |

| חומרה  | כמות |
| ------ | ---- |
| קריטי  | 1    |
| גבוה   | 6    |
| בינוני | 17   |
| נמוך   | 18   |

### מה שנקי, ובגדול

לפני הרשימה — כדאי לדעת מה **לא** נמצא, כי זה משנה את קריאת השאר:

- `npm audit` — **0 פרצות**, מכל דרגה, על 647 תלויות.
- `npm run lint` — **0 שגיאות, 0 אזהרות**.
- `tsc --noEmit` — **0 שגיאות**. אפס שימושים ב-`any`, אפס `@ts-ignore` / `@ts-expect-error` / `@ts-nocheck`.
- **אפס הערות TODO / FIXME / HACK / XXX** בכל הריפו.
- `npm run test` — 242 טסטים, כולם עוברים. `npm run build` ו-`format:check` עוברים.
- **אפס ייצוא מת** — נסרקו כל השמות המיוצאים מ-`lib/` ומ-`components/`; לכל אחד יש צרכן.
- **אין סודות בקוד**, ו-`tests/secrets.test.ts` הוא מה ששומר על זה.
- כל 70 פונקציות ה-`security definer` נושאות `set search_path = ''`.
- כל 13 ה-`catch` בקוד מתועדים ומכוונים — אין ולו בליעת שגיאה אחת בשוגג.

זהו קוד ממושמע. רוב החוב שלהלן הוא **חוב תפעולי** — מה שקורה כשהמוצר רץ מול משתמשים
אמיתיים — ולא חוב של איכות קוד.

### 5 הדברים הדחופים ביותר

1. ~~**#23 — קודי הדמו חיים על האתר הפרוס.**~~ **הוכרע 2026-09-05: נשארים כרגע**
   — סיכון מקובל עומד, בהחלטת המשתמש. נפתח מחדש לפני המשתמש האמיתי הראשון.
2. ~~**#40 — ענפי שגיאת-DB בשרת בולעים את השגיאה.**~~ **תוקן 2026-09-04.** כל 30
   הענפים רושמים היום את הקוד וההודעה של Postgres, ולכל שגיאה לא-מטופלת יש גבול
   שמציג `digest` שמוביל אל שורת הלוג.
3. **#27 — פיד בעלי המקצוע הוא סריקה לינארית.** 1265ms על 50,000 קריאות. זה נתיב
   הקריאה המרכזי של המרקטפלייס, והוא לא משתמש באינדקס.
4. **#22 — אפס headers של אבטחה.** אין CSP, אין X-Frame-Options, אין Referrer-Policy.
5. **#18 — כל 34 המסלולים הם `force-dynamic`**, כולל `/terms` ו-`/privacy`. רק
   `/robots.txt` סטטי. עמודי ה-SEO של Phase 8 לא נהנים משום cache.

---

## 2. טבלת מיפוי מלאה

מקרא סיווג: **ע** = ניתן לפתרון עצמאי · **מ** = דורש את מעורבותך

### תלויות (Dependencies)

| #   | חומרה  | מאמץ   | סיווג | נושא                                                     |
| --- | ------ | ------ | ----- | -------------------------------------------------------- |
| 1   | נמוך   | קטן    | ע     | ~~פיגור `@supabase/supabase-js`~~ — **תוקן**             |
| 2   | נמוך   | קטן    | ע     | ~~פיגור `@types/react-dom`~~ — **תוקן**                  |
| 3   | בינוני | קטן    | ע     | ~~`@types/node` ל-Node 20~~ — **תוקן**, `^22`            |
| 4   | בינוני | בינוני | מ     | ESLint 9 → 10 (major)                                    |
| 5   | בינוני | גדול   | מ     | TypeScript 5.9 → 7.0 (major)                             |
| 6   | בינוני | בינוני | מ     | Vitest 3.2 → 5.0 (שני majors)                            |
| 7   | בינוני | קטן    | ע     | ~~אין סריקת תלויות ב-CI~~ — **תוקן**, audit + Dependabot |
| 8   | נמוך   | קטן    | ע     | ~~ה-CLI אינו נעול~~ — **תוקן**, פינון מדויק              |
| 9   | נמוך   | קטן    | ע     | ~~אין `engines`/`.nvmrc`~~ — **תוקן**                    |

### איכות קוד

| #   | חומרה  | מאמץ   | סיווג | נושא                                                    |
| --- | ------ | ------ | ----- | ------------------------------------------------------- |
| 10  | נמוך   | קטן    | ע     | ~~שתי פונקציות עזר משוכפלות ×3~~ — **תוקן**             |
| 11  | בינוני | בינוני | ע     | ~~`CARD_CLASS` לא בשימוש; 55 עותקים~~ — **תוקן**        |
| 12  | נמוך   | בינוני | ע     | ~~`lib/actions/pros.ts` 479 שורות~~ — **תוקן**, 379+143 |
| 13  | נמוך   | בינוני | ע     | פיצול `rls_test.sql` — **נבדק ונדחה**, ראו פרק 9        |

### הערות TODO / FIXME / HACK / XXX

| #   | חומרה | מאמץ | סיווג | נושא                                   |
| --- | ----- | ---- | ----- | -------------------------------------- |
| 14  | —     | —    | —     | **לא נמצאה אף אחת.** אין כאן חוב לתעד. |

### טסטים

| #   | חומרה  | מאמץ   | סיווג | נושא                                                   |
| --- | ------ | ------ | ----- | ------------------------------------------------------ |
| 15  | בינוני | קטן    | ע     | ~~אין מדידת כיסוי~~ — **תוקן**, 91% עם סף              |
| 16  | בינוני | בינוני | ע     | ~~`lib/actions/` בלי טסטים~~ — **תוקן**, מיפוי השגיאות |
| 17  | בינוני | קטן    | ע     | ~~`perf:postgis` לא ב-CI~~ — **תוקן**                  |
| 18  | נמוך   | קטן    | מ     | a11y אוכף רק `serious`/`critical`                      |

### ארכיטקטורה

| #   | חומרה  | מאמץ   | סיווג | נושא                                                  |
| --- | ------ | ------ | ----- | ----------------------------------------------------- |
| 19  | גבוה   | בינוני | מ     | כל 34 המסלולים `force-dynamic`                        |
| 20  | גבוה   | קטן    | ע     | ~~אין `error.tsx`~~ — **תוקן 2026-09-04**             |
| 21  | בינוני | בינוני | ע     | ~~אין `loading.tsx`~~ — **תוקן**, 5 מסכים (ראו פרק 9) |
| 22  | נמוך   | קטן    | ע     | ~~N+1 בתיעוד הקריאה~~ — **תוקן 2026-09-06**           |

### אבטחה

| #   | חומרה  | מאמץ   | סיווג | נושא                                                  |
| --- | ------ | ------ | ----- | ----------------------------------------------------- |
| 23  | קריטי  | קטן    | מ     | קודי דמו חיים — **נשארים**, והחשיפה הורחבה 2026-09-06 |
| 24  | גבוה   | בינוני | מ     | אפס headers של אבטחה (CSP וחבריו)                     |
| 25  | בינוני | קטן    | מ     | `site_url` בענן מצביע על `127.0.0.1:3000`             |

### ביצועים

| #   | חומרה  | מאמץ   | סיווג | נושא                                          |
| --- | ------ | ------ | ----- | --------------------------------------------- |
| 26  | גבוה   | גדול   | מ     | פיד בעלי המקצוע — סריקה לינארית, בלי אינדקס   |
| 27  | גבוה   | בינוני | מ     | `admin_jobs()` — `limit 300` קשיח, יצוא חותך  |
| 28  | נמוך   | בינוני | מ     | פילטרים ב-`admin_jobs` על פונקציות פר-שורה    |
| 29  | נמוך   | קטן    | ע     | ~~`RealtimeRefresh` בלי debounce~~ — **תוקן** |
| 30  | בינוני | קטן    | ע     | ~~`setAuth()` פעם אחת~~ — **נבדק: אינו באג**  |

### תשתית ו-CI/CD

| #   | חומרה  | מאמץ | סיווג | נושא                                        |
| --- | ------ | ---- | ----- | ------------------------------------------- |
| 31  | נמוך   | קטן  | מ     | אין workflow לפריסה                         |
| 32  | נמוך   | קטן  | מ     | `run-all-phases.sh` — פיגום שסיים תפקידו    |
| 33  | נמוך   | קטן  | מ     | 4.9MB PNG של `design/` בהיסטוריית git       |
| 34  | בינוני | קטן  | ע     | ~~רק pre-commit~~ — **תוקן**, נוסף pre-push |

### תיעוד

| #   | חומרה  | מאמץ   | סיווג | נושא                                           |
| --- | ------ | ------ | ----- | ---------------------------------------------- |
| 35  | בינוני | בינוני | מ     | `docs/security-checklist.md` מוזכר ×3, לא קיים |
| 36  | בינוני | קטן    | ע     | ~~README מתאר עמוד בית זמני~~ — **תוקן**       |
| 37  | נמוך   | קטן    | ע     | ~~טבלת סקריפטים חסרה~~ — **תוקן**, כל 17       |
| 38  | נמוך   | קטן    | ע     | ~~אין פרק פריסה~~ — **תוקן**                   |
| 39  | נמוך   | קטן    | מ     | מסך `pro-5.4-notifications` מעוצב ולא ממומש    |

### ניהול שגיאות ולוגים

| #   | חומרה | מאמץ   | סיווג | נושא                                                |
| --- | ----- | ------ | ----- | --------------------------------------------------- |
| 40  | גבוה  | בינוני | ע     | ~~30 ענפים בולעים את השגיאה~~ — **תוקן 2026-09-04** |
| 41  | גבוה  | בינוני | מ     | מוניטורינג — **טופל חלקית**, בחירת ספק עדיין פתוחה  |

### גרסת שפה/פריימוורק

| #   | חומרה | מאמץ | סיווג | נושא                                                |
| --- | ----- | ---- | ----- | --------------------------------------------------- |
| 42  | —     | —    | —     | Next 16.3.4 ו-React 19.2.8 מעודכנים. אין API מיושן. |

---

## 3. פירוט הממצאים

### תלויות

**#1 · `@supabase/supabase-js` 2.112.4 → 2.115.0** · נמוך · קטן · **ע**
`package.json:44`. שלוש גרסאות מינור מאחור. הטווח `^2.112.4` כבר מתיר את זה —
`npm update` בלבד.

**#2 · `@types/react-dom` 19.2.5 → 19.2.7** · נמוך · קטן · **ע**
`package.json:52`. אותו דבר, טיפוסים בלבד.

**#3 · `@types/node` מכוון ל-Node 20, הריצה על Node 22** · בינוני · קטן · **ע**
`package.json:50` קובע `^20`, בעוד `.github/workflows/ci.yml` מריץ `node-version: 22`
בשלושת הג'ובים והסביבה המקומית היא v22.16.0. המשמעות המעשית: APIים שנוספו ב-Node 22
לא מוטפסים, ו-`tsc` יסמן שימוש בהם כשגיאה למרות שהם קיימים בזמן ריצה. תיקון: `^22`.

**#4 · ESLint 9 → 10** · בינוני · בינוני · **מ**
שדרוג major. `eslint-config-next@16.3.4` צריך לתמוך בו, וזה תלוי בלוח הזמנים של
Next. **ההחלטה:** לחכות ש-`eslint-config-next` יכריז תמיכה, או לשדרג עכשיו ולספוג
אי-התאמה אפשרית בקונפיג?

**#5 · TypeScript 5.9.3 → 7.0.2** · בינוני · גדול · **מ**
TS 7 הוא הקומפיילר הכתוב ב-Go. שדרוג רוחבי שנוגע בכל קובץ, ב-`tsconfig.json` וב-`next
typegen`. **ההחלטה:** האם לשדרג עכשיו, או להישאר על 5.9 עד שהאקוסיסטם (Next, ESLint,
Vitest) יתייצב סביב 7?

**#6 · Vitest 3.2.7 → 5.0.0** · בינוני · בינוני · **מ**
שני majors מאחור. 242 טסטים עוברים, כך שהסיכון מוגבל לשינויי API בקונפיג.
**ההחלטה:** לשדרג ל-5 בקפיצה, או דרך 4?

**#7 · אין סריקת תלויות ב-CI ואין עדכון אוטומטי** · בינוני · קטן · **ע**
`.github/workflows/ci.yml` מריץ format, lint, typecheck, test, build, pgTAP ו-E2E —
אבל לא `npm audit`. אין `.github/dependabot.yml` ואין `renovate.json`. היום המצב נקי
(0 פרצות), וזה בדיוק הרגע להוסיף את השער — לפני שהוא נדרש.

**#8 · ה-CLI של Supabase אינו נעול כפי ש-CLAUDE.md טוען** · נמוך · קטן · **ע**
`CLAUDE.md` סעיף 2: "Version is locked in the repo and identical for everyone".
בפועל `package.json:58` כותב `"supabase": "^2.116.0"` — טווח caret. `npm ci` אכן נועל
דרך ה-lockfile, אבל `npm install` על מכונה חדשה יכול למשוך 2.x חדש יותר, ושינוי
ב-CLI משנה את `db:reset` ואת `db:types`. פינון מדויק מיישר את הקוד עם ההצהרה.

**#9 · אין `engines` ואין `.nvmrc`** · נמוך · קטן · **ע**
גרסת Node מוצהרת רק ב-`.github/workflows/ci.yml` וב-README כטקסט. `npm install`
תחת Node 18 לא ייכשל, אלא יישבר מאוחר יותר ובאופן פחות ברור.

### איכות קוד

**#10 · שתי פונקציות עזר משוכפלות שלוש פעמים כל אחת** · נמוך · קטן · **ע**
`fieldErrorsOf` — [lib/actions/bids.ts:37](lib/actions/bids.ts#L37),
[lib/actions/pros.ts:42](lib/actions/pros.ts#L42),
[lib/actions/priceUpdates.ts:38](lib/actions/priceUpdates.ts#L38).
`optional` — [lib/actions/bids.ts:32](lib/actions/bids.ts#L32),
[lib/actions/pros.ts:37](lib/actions/pros.ts#L37),
[lib/actions/jobs.ts:17](lib/actions/jobs.ts#L17).
שלושת העותקים של כל אחת זהים. הערה חשובה: הן **אינן** יכולות לעבור ל-`lib/actions/`
כמודול `"use server"` — מודול כזה רשאי לייצא רק פונקציות async. היעד הנכון הוא מודול
ניטרלי חדש, באותה סברה שבגללה `lib/actions/state.ts` קיים בכלל.

**#11 · מערכת העיצוב מוגדרת ואינה בשימוש** · בינוני · בינוני · **ע**
[components/ui/primitives.tsx:13](components/ui/primitives.tsx#L13) מגדיר `CARD_CLASS`,
ו-`<Card>` עוטף אותו. **אף קובץ מחוץ ל-`primitives.tsx` לא מייבא את `CARD_CLASS`.**
במקום זה, המחרוזת `rounded-2xl border border-line bg-surface` מופיעה **55 פעמים
inline, ב-35 קבצים** — 7 מהן ב-[app/(marketing)/pro/[slug]/page.tsx](<app/(marketing)/pro/[slug]/page.tsx>)
לבדו. אותו דפוס בשתי מחרוזות נוספות: `text-3xl font-bold text-ink sm:text-4xl`
(20 פעמים) ו-`mb-1 block text-sm font-medium text-ink` (13 פעמים) — לשתיהן אין בכלל
קבוע ב-`primitives.tsx`.
לשם השוואה, `BUTTON_CTA` ו-`INPUT_CLASS` **כן** בשימוש רוחבי (18 ו-19 קבצים). כלומר
הדפוס נכון והוחל חלקית. ההערה בראש `primitives.tsx` — "a later re-skin is one edit
here instead of a sweep through every component" — נכונה לכפתורים ולשדות, ולא נכונה
לכרטיסים, לכותרות ולתוויות.

**#12 · `lib/actions/pros.ts` — 479 שורות, 10 פעולות שרת** · נמוך · בינוני · **ע**
[lib/actions/pros.ts](lib/actions/pros.ts). הקובץ הארוך ביותר ב-`lib/`. הוא מחזיק את
כל חמשת שלבי ה-onboarding, את הפרופיל, את הזמינות ואת המסמכים. פיצול לפי מסך יקרב
אותו לשאר הקבצים ב-`lib/actions/` (הבא בגודלו הוא 190 שורות).

**#13 · `supabase/tests/rls_test.sql` — 3,026 שורות** · נמוך · בינוני · **ע**
הקובץ הגדול ביותר בריפו. 15 סעיפים ממוספרים בקובץ אחד. `supabase test db` תומך במספר
קבצים בתיקייה, כך שפיצול לפי שלב או לפי טבלה אפשרי ולא ידרוש שינוי בסקריפט.

### טסטים

**#14 · אין הערות TODO/FIXME/HACK/XXX** — נסרק כל הריפו למעט `node_modules`,
`.next` ו-`design/`. ההתאמה היחידה היא `9725XXXXXXXX` ב-[README.md:71](README.md#L71),
שהיא placeholder של מספר טלפון ולא סמן חוב. **אין כאן מה לתקן.**

**#15 · אין מדידת כיסוי טסטים** · בינוני · קטן · **ע**
`vitest.config.ts` אינו מגדיר `coverage`, אין `@vitest/coverage-v8` ב-devDependencies
ואין סקריפט `test:coverage`. 242 הטסטים עוברים — אבל אף אחד לא יודע על כמה מהקוד.
זו ההנחה שכל שאר סעיף הטסטים כאן נשען עליה, ואי אפשר לאמת אותה כרגע.

**#16 · 13 מודולי `lib/actions/` — אפס טסטי יחידה** · בינוני · בינוני · **ע**
לכל 12 מודולי `lib/validation/` יש `__tests__`. ל-`lib/actions/` — לאף אחד מ-13.
ל-`lib/supabase/` יש טסט אחד (`bids.test.ts`) מתוך 20 מודולים.
חלק מזה **נכון בכוונה**: `vitest.config.ts` מסביר שכל מה שתלוי ב-RLS נבדק ב-pgTAP,
וזו הכרעה טובה. אבל לא כל מה שיש ב-`lib/actions/` תלוי ב-RLS. הדוגמה החדה:
[lib/actions/bids.ts:89](lib/actions/bids.ts#L89) ממפה `error.code === "23505"` להודעה
"כבר הגשתם הצעה לקריאה הזו" — לוגיקה טהורה, בלי DB, שאף טסט לא נוגע בה. אותו דפוס
חוזר בכל מודולי הפעולות.

**#17 · `npm run perf:postgis` אינו רץ ב-CI** · בינוני · קטן · **ע**
[scripts/postgis-load-check.mjs](scripts/postgis-load-check.mjs) הוא 328 שורות של
בדיקת רגרסיה לביצועים, עם תקציבי זמן מפורשים (`budgetMs: 200` לאינדקסים,
`budgetMs: 400` לפיד). הוא לא מופיע באף ג'וב ב-`.github/workflows/ci.yml`.
ההערה בקובץ עצמו כותבת את הבעיה: "a feed that has quietly degraded to a sequential
scan fails a budget long before anybody notices it in a page load" — נכון, אבל רק אם
מישהו מריץ את זה. ג'וב `database` כבר מעלה Supabase, כך שהתוספת היא שורה אחת.

**#18 · a11y אוכף רק `serious` ו-`critical`** · נמוך · קטן · **מ**
[e2e/a11y.spec.ts:22](e2e/a11y.spec.ts#L22) מסנן החוצה `moderate` ו-`minor`. הקובץ
מנמק את זה היטב. **ההחלטה:** האם `moderate` נכנס לאכיפה לפני השקה ציבורית, או נשאר
מחוץ לשער לתמיד?

### ארכיטקטורה

**#19 · כל 34 המסלולים הם `force-dynamic`** · גבוה · בינוני · **מ**
`export const dynamic = "force-dynamic"` מופיע ב-34 קבצים. פלט `npm run build` מאשר:
**`/robots.txt` הוא המסלול הסטטי היחיד בכל האפליקציה.**
עבור מסכים מאחורי התחברות זה נכון. עבור עמודי Phase 8 — לא:

- [app/(marketing)/terms/page.tsx](<app/(marketing)/terms/page.tsx>),
  `privacy` ו-`cancellation` הם טקסט קבוע מתוך `lib/content/legal.ts`.
  התוכן שלהם **אינו תלוי בכלום**, והם מרונדרים מחדש בכל בקשה.
- `/services/[category]/[city]` — 180 עמודי SEO, כל אחד עם שתי שאילתות DB בכל טעינה.
- `/guides/[slug]` מגדיר `generateStaticParams()` **וגם** `force-dynamic`. השני מבטל
  את הראשון.

הסיבה זהה בכולם: כל עמוד קורא ל-`getCurrentUser()` כדי להזין את `<AppShell>`, וזו
קריאת DB פר-בקשה. זהו העמוד שגוגל סורק והלקוח נוחת עליו ראשון.
**ההחלטה:** האם להוציא את מצב-ההתחברות מה-shell לרכיב לקוח (או ל-Suspense
boundary), כך שגוף העמוד יוכל להיות סטטי/ISR? זהו שינוי מבני ב-`AppShell` שנוגע
ב-13 עמודים, ולכן הוא שלך.

**#20 · אין `error.tsx` באף מקום** · גבוה · קטן · **ע**
מתוך 49 קובצי מסלול ב-`app/`, קובץ הגבול היחיד הוא [app/not-found.tsx](app/not-found.tsx).
אין `error.tsx`, אין `global-error.tsx`.
המשמעות: כל חריגה לא-מטופלת ברינדור שרת — Supabase לא זמין, RPC שנכשל,
`@react-pdf/renderer` שנופל — מציגה ללקוח את מסך השגיאה הגנרי של Next, בעברית של
אף אחד, בלי דרך חזרה ובלי שהיא נרשמת בשום מקום. בשילוב עם #40 ו-#41, כשלון בייצור
הוא בלתי נראה משני הצדדים בו-זמנית.

**#21 · אין `loading.tsx` באף מקום** · בינוני · בינוני · **ע**
מכיוון שכל מסלול הוא `force-dynamic` (#19) וחוסם עד סיום כל השאילתות, מסך אטי הוא
מסך לבן. `/admin/jobs/[jobId]` הוא המקרה החד: הוא מבצע 4 שאילתות מקבילות, ואז עוד 4,
ואז אחת לכל הצעה (#22) — הכול לפני הבייט הראשון.

**#22 · N+1 בתיעוד הקריאה של המנהל** · נמוך · קטן · **ע**
[app/(admin)/(authed)/admin/jobs/[jobId]/page.tsx:83-88](<app/(admin)/(authed)/admin/jobs/[jobId]/page.tsx#L83-L88>)
מריץ `listThreadMessages(jobId, bid.proId)` — קריאת RPC — פעם אחת לכל הצעה.
ההערה בקוד מנמקת זאת נכון ("a thread is (job, pro), never (job)"), והן רצות במקביל,
כך שהזמן הוא של האטית ולא של הסכום. עדיין: מספר הקריאות גדל עם מספר ההצעות, ופונקציה
אחת שמחזירה את כל השיחות של קריאה הייתה הופכת את זה לקריאה אחת.

### אבטחה

**#23 · קודי הדמו חיים על הפריסה, ולוח הניהול פתוח לניחוש** · קריטי · קטן · **מ**
מתועד כבר ב-`CLAUDE.md` סעיף 9, ומופיע כאן כי זו הפרצה הפתוחה החמורה ביותר במסמך.
`supabase/config.toml` ממפה שבעה מספרים ל-`123456` תחת `[auth.sms.test_otp]`,
ו-`supabase config push` שולח את זה לפרויקט המאוחסן. `972500000005` הוא ה-**admin**.
מי שמוצא את ה-URL ומנסה את המספר המתבקש ואת הקוד המתבקש — מגיע ללוח שנבנה ב-Phase 7.
אין הגנת סיסמה ב-Vercel, ו-[app/robots.ts](app/robots.ts) מזמין אינדוקס.
**ההחלטה:** אחת משלוש — לסובב את הקודים בפרויקט המאוחסן בלבד, למחוק את משתמש
הדמו admin מה-seed בענן, או להעמיד את הפריסה מאחורי Vercel Password Protection.
זו החלטת אבטחה עם השלכה על הדמו למשקיעים, ולכן היא שלך. שים לב לתלות: סיבוב הקודים
הוא `config push`, שדוחף כרגע גם את `site_url` (#25).

**#24 · אפס headers של אבטחה** · גבוה · בינוני · **מ**
`next.config.ts` אינו מגדיר `headers()`. `vercel.json` מכיל `regions` בלבד. `proxy.ts`
אינו מוסיף headers. אין בשום מקום `Content-Security-Policy`, `X-Frame-Options`,
`Referrer-Policy`, `X-Content-Type-Options` או `Permissions-Policy`.
ההשלכה הקונקרטית: כל עמוד מאחורי התחברות — כולל `/admin` — ניתן להטמעה ב-iframe
באתר זר (clickjacking), וה-`Referer` המלא של כל עמוד קריאה דולף לכל דומיין חיצוני
שמקושר ממנו.
**ההחלטה:** ארבעת ה-headers הפשוטים הם עבודה מכנית ואפשר לעשות אותם מיד. CSP הוא
לא — האפליקציה טוענת את Google Maps JS, מדברת עם Supabase Realtime מעל WebSocket
ומשתמשת ב-`next/font`. CSP צר מדי שובר את שלושתם בשקט, בייצור. האם מתחילים
מ-`Content-Security-Policy-Report-Only` ואוספים הפרות, או מוותרים על CSP בשלב הזה
ומיישמים רק את השאר?

**#25 · `site_url` בפרויקט המאוחסן מצביע על `http://127.0.0.1:3000`** · בינוני · קטן · **מ**
מתועד ב-`CLAUDE.md` סעיף 9. היום שום דבר במוצר לא נשבר מזה, כי OTP טלפוני לא מפנה.
זה נשבר ברגע שמישהו מוסיף התחברות במייל או OAuth. הפתרון המכוון הוא `env()` בתוך
`config.toml`, כדי שהוא יישא את ה-origin הפרוס בלי לשבור `supabase start` לכולם.
**ההחלטה:** האם מתקנים עכשיו יחד עם #23 (שממילא דורש `config push`), או משאירים עד
שתידרש שיטת התחברות שמפנה?

### ביצועים

**#26 · פיד בעלי המקצוע אינו משתמש באינדקס** · גבוה · גדול · **מ**
מדוד ומתועד: `docs/roadmap.md` שורה 313 מדווח **1265ms על 50,000 קריאות**, מול 8.9ms
ו-5.1ms לשני האינדקסים המרחביים שכן נבדקים. ההסבר ב-
[scripts/postgis-load-check.mjs:186-193](scripts/postgis-load-check.mjs#L186-L193):
כל מדיניות ה-SELECT על `jobs` היא permissive ולכן מחוברת ב-OR, ושלושה מחמשת הענפים
הם פונקציות `security definer` אטומות מעל השורה. לפיכך שום אינדקס אינו שמיש, וכל
קריאה פתוחה בטבלה נקראת ונבדקת. העלות לינארית — כ-23 מיקרו-שניות לכל קריאה פתוחה.
זה נתיב הקריאה המרכזי של המוצר: המסך שבעל מקצוע פותח בכל בוקר.
**ההחלטה:** זו הכרעה ארכיטקטונית אמיתית, והיא מתנגשת עם `CLAUDE.md` סעיף 3 —
"A job reaches a pro only inside BOTH radii ... enforced in the RLS policy on `jobs`,
not in the feed query, so it holds for anything that ever reads the table". זו
ההחלטה הנכונה מבחינת אבטחה, והיא זו שעולה כאן בביצועים. האפשרויות: עמודה מחושבת עם
אינדקס שמדיניות יכולה לגעת בה ישירות, צמצום ענפי ה-OR, או קבלת התקרה ומעקב אחריה.
**אני לא נוגע בזה בלי הכרעה שלך** — כל שינוי כאן הוא שינוי בכלל האבטחה הנושא של
המוצר.

**#27 · `admin_jobs()` חותך ב-300, והיצוא חותך איתו** · גבוה · בינוני · **מ**
[supabase/migrations/20260907120000_admin_dashboard.sql:565](supabase/migrations/20260907120000_admin_dashboard.sql#L565)
מסתיים ב-`limit 300` קשיח. אין `p_offset`, אין cursor, אין החזרת ספירה כוללת.
שתי השלכות, והשנייה חמורה:

1. טבלת הקריאות ב-`/admin/jobs` מציגה 300 שורות לכל היותר, ו**אינה אומרת זאת**.
   חיפוש ב-`app/(admin)` ו-`components/admin` על "300" לא מחזיר כלום.
2. `/api/admin/report` — כפתור "יצוא דוח" — קורא לאותה פונקציה. ההערה בראש
   [app/api/admin/report/route.ts:16](app/api/admin/report/route.ts#L16) מבטיחה
   "It exports exactly what the jobs table is showing, filters and all, so the
   spreadsheet and the screen can never be two different questions". זה נכון —
   ושניהם חתוכים באותו מקום. מנהל שמייצא דוח חודשי מעל 300 קריאות מקבל קובץ שנראה
   שלם ואינו שלם.

**ההחלטה:** האם 300 היא תקרה מכוונת (ואז המסך והיצוא צריכים לומר זאת), או שהמסך
צריך pagination והיצוא צריך לעקוף את התקרה? השנייה היא שאלה מוצרית על מה "דוח" אומר.

**#28 · פילטרים ב-`admin_jobs` מבוססים על פונקציות פר-שורה** · נמוך · בינוני · **מ**
באותה פונקציה: פילטר העיר מריץ `public.job_city(j.address_text)` על כל שורה, והחיפוש
מריץ `regexp_replace(j.id::text, '\D', '', 'g')`. שניהם אינם ניתנים לאינדוקס.
זה עקבי עם `CLAUDE.md` ("`job_city()` — Derived from ... Never a column"), וזו הסיבה
שזה דורש אותך: אינדקס ביטוי יפתור את זה בלי לשבור את הכלל, אבל זו החלטה על סכימה.

**#29 · `RealtimeRefresh` בלי debounce** · נמוך · קטן · **ע**
[components/ui/RealtimeRefresh.tsx:87-93](components/ui/RealtimeRefresh.tsx#L87-L93)
קורא `router.refresh()` על **כל** אירוע. במסך ההצעות של הלקוח, שלוש הצעות שנוחתות
יחד הן שלושה רינדורים מלאים בשרת של עמוד `force-dynamic`. השהיה קצרה תקבץ אותם לאחד.

**#30 · הטוקן של ה-socket אינו מרוענן** · בינוני · קטן · **ע**
[components/ui/RealtimeRefresh.tsx:74](components/ui/RealtimeRefresh.tsx#L74) קורא
`supabase.realtime.setAuth(...)` פעם אחת, בעליית הרכיב. `supabase/config.toml:164`
קובע `jwt_expiry = 3600`. אין בכל הריפו אף `onAuthStateChange` ואף טיפול
ב-`TOKEN_REFRESHED` — נסרק, אפס התאמות.
הרכיב עצמו מסביר למה ה-`setAuth` הראשוני נחוץ: "no auth event fires to push the token
into the realtime client". אם ההנמקה הזו נכונה גם לריענון, אז טאב שפתוח מעל שעה מפסיק
לקבל עדכונים **בשקט** — בלי שגיאה, בלי אינדיקציה. המסך שנפגע הוא בדיוק זה שנועד
להישאר פתוח: `/requests/[jobId]/track`, המעקב אחרי בעל מקצוע בדרך.
**לא הצלחתי לקבוע בוודאות** אם supabase-js דוחף את הטוקן המרוענן מעצמו — זה תלוי
בהתנהגות גרסה. מה שכן ודאי: **שום דבר בריפו לא בודק את זה**, ו-`e2e/critical-flow.spec.ts`
רץ הרבה מתחת לשעה. הצעד הראשון הוא לאמת, לא לתקן.

### תשתית ו-CI/CD

**#31 · אין workflow לפריסה** · נמוך · קטן · **מ**
`.github/workflows/ci.yml` הוא ה-workflow היחיד, והוא מאמת בלבד. קיימת תיקיית
`.vercel/` מקומית, כלומר הפריסה היא דרך אינטגרציית Git של Vercel — שאינה מחכה ל-CI.
**ההחלטה:** האם פריסה לייצור צריכה להיחסם עד ש-`database` ו-`e2e` ירוקים? זו הכרעה
על תהליך העבודה שלך.

**#32 · `run-all-phases.sh` — 736 שורות בשורש** · נמוך · קטן · **מ**
[run-all-phases.sh](run-all-phases.sh) מריץ את Phase 2..9 בצורה בלתי-מפוקחת. כל
תשעת השלבים הושלמו. הוא הקובץ השלישי בגודלו בריפו ומכיל שער איכות משוכפל שחופף
ל-`ci.yml`, כך שהוא צריך תחזוקה בכל פעם שהשער משתנה.
**ההחלטה:** למחוק, להעביר ל-`scripts/`, או להשאיר בשורש לשלבים עתידיים? למחוק פיגום
שאולי עוד ישמש היא בדיוק ההכרעה שאני לא מקבל לבד.

**#33 · 4.9MB של PNG ב-`design/`** · נמוך · קטן · **מ**
37 קבצי מסך. הם **נחוצים** — הקוד מפנה אליהם בעשרות הערות, ובדיקה הצליבה שכל
הפניה מצביעה על קובץ קיים. שני קבצים אינם מוזכרים באף מקום: `manifest.json`
ו-`pro-5.4-notifications.png` (ראה #39). **ההחלטה:** האם להשאיר בינריים בהיסטוריה,
לעבור ל-Git LFS, או לא לגעת? בגודל הזה זה עדיין לא כואב.

**#34 · ה-pre-commit hook הוא היחיד** · בינוני · קטן · **ע**
`.husky/pre-commit` מריץ `npx lint-staged` — prettier ו-eslint --fix בלבד. אין
`pre-push`. המשמעות: commit ש-`tsc` נכשל עליו, או ששובר טסט, נוצר ונדחף בלי התנגדות,
וה-CI תופס אותו רק אחר כך. `npm run test` לוקח 738ms.

### תיעוד

**#35 · `docs/security-checklist.md` מוזכר שלוש פעמים ואינו קיים** · בינוני · בינוני · **מ**
ההפניות: [supabase/migrations/20260909120000_security_hardening.sql:50](supabase/migrations/20260909120000_security_hardening.sql#L50)
("see docs/security-checklist.md for the full walk"),
[scripts/postgis-load-check.mjs:33](scripts/postgis-load-check.mjs#L33)
ו-[scripts/postgis-load-check.mjs:193](scripts/postgis-load-check.mjs#L193).
תיקייה `docs/` מכילה חמישה קבצים ואף אחד מהם אינו זה. `docs/roadmap.md` שורה 312
מסמנת "checklist אבטחה עבר בשלמותו" כהושלם, ומנמק שה-checklist מתקיים כקוד ולא
כמסמך — הנמקה טובה, שמשאירה שלוש הפניות מתות למסמך שהיה אמור להיכתב.
זה מפריע במיוחד ב-#26: ההערה בסקריפט הביצועים מפנה לשם בשביל **ההסבר המלא של תקרת
הפיד** — כלומר ההנמקה של הממצא החמור ביותר בסעיף הביצועים מפנה לכתובת ריקה.
**ההחלטה:** לכתוב את המסמך (יש חומר — סעיף 12 ב-`rls_test.sql`, המיגרציה של
הקשחת האבטחה, `tests/secrets.test.ts`), או לתקן את שלוש ההפניות שיצביעו על
`CLAUDE.md` סעיף 3? הראשונה נכונה יותר וגם יקרה יותר.

**#36 · README מתאר עמוד בית שכבר לא קיים** · בינוני · קטן · **ע**
[README.md:30-32](README.md#L30-L32): "Open http://localhost:3000 — the temporary home
page reports whether the build, RTL rendering, and the Supabase connection are all
working, and links to both login screens."
זה נכון היה עד Phase 8. היום [app/(marketing)/page.tsx](<app/(marketing)/page.tsx>)
הוא עמוד הנחיתה השיווקי המלא, עם hero, רצועת תחומים מ-`categories` ומטא-תגיות SEO.
זו הפסקה השלישית ב-README ולכן היא הרושם הראשון של כל מי שנכנס לריפו.

**#37 · טבלת הסקריפטים ב-README חסרה חמש פקודות** · נמוך · קטן · **ע**
[README.md:120-133](README.md#L120-L133) מונה 11 פקודות. `package.json` מגדיר 18.
חסרות: `test:watch`, **`test:e2e`**, `test:e2e:ui`, `format:check`, `perf:postgis`.
החוסר המשמעותי הוא `test:e2e` — חבילת בדיקות שלמה של 139 בדיקות Playwright, שנבנתה
ב-Phase 9, שאינה מוזכרת בטבלה שאמורה למנות את הפקודות.

**#38 · אין ב-README פרק פריסה** · נמוך · קטן · **ע**
ה-README מסתיים בפרק "Connecting to the cloud Supabase project". אין בו את הצד של
Vercel: ש-`vercel.json` מקבע `regions: ["fra1"]`, אילו משתני סביבה חייבים להיות
מוגדרים בייצור (`NEXT_PUBLIC_SITE_URL` בפרט — בלעדיו כל canonical מצביע על
`localhost:3000`), ומה קורה עם `ALLOW_NO_MAPS_KEY` בבילד ייצור.

**#39 · מסך מעוצב שלא מומש** · נמוך · קטן · **מ**
`design/screens/pro-5.4-notifications.png` הוא היחיד מ-36 המסכים שאף שורת קוד לא
מפנה אליו. זה עקבי עם ההחלטה הפתוחה ב-`CLAUDE.md` סעיף 9 על התראות push.
**ההחלטה:** התראות push הן החלטה פתוחה מ-Phase 4. שתי הרגעים שמצדיקים אותה —
"בעל המקצוע הגיע" ו-"יש בקשת עדכון מחיר" — קיימים במוצר מאז Phase 5.

### ניהול שגיאות ולוגים

**#40 · 25 מתוך 26 ענפי שגיאת-DB בולעים את השגיאה** · גבוה · בינוני · **ע**
נספרו 26 ענפי `if (error)` ב-13 מודולי `lib/actions/`. **קריאת `console` אחת בסך
הכול**, ב-[lib/actions/auth.ts:60](lib/actions/auth.ts#L60).
הפילוח: `admin` 3, `bids` 4, `completion` 3, `disputes` 1, `jobs` 1, `messages` 1,
`priceUpdates` 2, `pros` 3, `publicProfile` 4, `support` 1, `tracking` 1 — כולם ללא
לוג. הדפוס, לדוגמה [lib/actions/bids.ts:94-98](lib/actions/bids.ts#L94-L98):

```
return {
  error: "לא ניתן להגיש הצעה לקריאה הזו: ייתכן שהיא כבר נסגרה, או שהיא מחוץ לרדיוס...",
};
```

ההודעה נכונה ללקוח. אבל `error.code`, `error.message` ו-`error.details` נזרקים,
והמידע היחיד שנשאר על כשלון הוא ניחוש בעברית. שים לב שזה הכי כואב **דווקא בגלל**
הארכיטקטורה: `CLAUDE.md` סעיף 3 מעביר בכוונה את הכללים למסד הנתונים, כך ש-`select_bid()`
או `complete_job()` שנכשלות מחזירות שגיאת Postgres מדויקת — בדיוק המידע שנזרק כאן.
בעל מקצוע שמדווח "לא הצלחתי לסגור עבודה" אינו ניתן לדיבאג היום.

**#41 · אין מוניטורינג שגיאות** · גבוה · בינוני · **מ**
אין Sentry ואין שקול לו ב-`package.json`, אין `instrumentation.ts` ואין
`onRequestError`. בשילוב עם #20 (אין `error.tsx`) ועם #40 (השגיאה נזרקת), כשלון
בייצור אינו נראה לאף אחד: לא ללקוח שרואה מסך גנרי, ולא לך.
**ההחלטה:** בחירת ספק היא עלות כספית, ולפי `CLAUDE.md` סעיף 8 היא שלך. Sentry
ו-Vercel Log Drains הן שתי נקודות מוצא בעלויות שונות מאוד. שים לב שהתשתית קיימת
בחינם חלקית: Vercel שומר לוגים של runtime, כך ש-#40 לבדו — פשוט לכתוב
`console.error` — כבר יתן חלק גדול מהערך בלי לבחור ספק.

### גרסת שפה/פריימוורק

**#42 · אין ממצא.** Next 16.3.4 ו-React 19.2.8 — `npm outdated` לא מציג אף אחד
מהם, כלומר שניהם בגרסה האחרונה. `proxy.ts` משתמש בשם החדש של Next 16 (לא
`middleware.ts`), הטיפוסים `PageProps<...>` הם החדשים, ו-`serverExternalPackages`
הוא השם היציב (לא `experimental.serverComponentsExternalPackages`). לא נמצא אף API
מיושן.

---

## 4. מוכן להתחיל מיד — ניתן לפתרון עצמאי

27 פריטים, ממוינים לפי חומרה. אין בהם הכרעה עסקית, אין שינוי ארכיטקטוני, ואף אחד
מהם לא שובר פיצ'ר קיים.

### גבוה

| #      | מה                                                    | מאמץ   |
| ------ | ----------------------------------------------------- | ------ |
| ~~20~~ | ~~הוספת `error.tsx` ו-`global-error.tsx`~~ — **בוצע** | קטן    |
| ~~40~~ | ~~רישום `error.code`/`message`~~ — **בוצע**, 30 ענפים | בינוני |

### בינוני

| #   | מה                                            | מאמץ   |
| --- | --------------------------------------------- | ------ |
| 3   | `@types/node` → `^22`                         | קטן    |
| 7   | `npm audit` ב-CI + Dependabot                 | קטן    |
| 11  | החלת `CARD_CLASS`/`<Card>` על 55 העותקים      | בינוני |
| 15  | `@vitest/coverage-v8` + סקריפט + סף           | קטן    |
| 16  | טסטי יחידה למיפוי השגיאות ב-`lib/actions/`    | בינוני |
| 17  | `perf:postgis` בג'וב `database`               | קטן    |
| 21  | `loading.tsx` למסכים הכבדים                   | בינוני |
| 30  | אימות התנהגות ריענון הטוקן, ואז תיקון אם צריך | קטן    |
| 34  | `pre-push` עם typecheck + test                | קטן    |
| 36  | תיקון תיאור עמוד הבית ב-README                | קטן    |

### נמוך

| #   | מה                                 | מאמץ   |
| --- | ---------------------------------- | ------ |
| 1   | `npm update @supabase/supabase-js` | קטן    |
| 2   | `npm update @types/react-dom`      | קטן    |
| 8   | פינון מדויק של `supabase` CLI      | קטן    |
| 9   | `engines` + `.nvmrc`               | קטן    |
| 10  | איחוד `fieldErrorsOf` ו-`optional` | קטן    |
| 12  | פיצול `lib/actions/pros.ts`        | בינוני |
| 13  | פיצול `rls_test.sql`               | בינוני |
| 22  | איחוד ה-N+1 בתיעוד הקריאה          | קטן    |
| 29  | debounce ל-`RealtimeRefresh`       | קטן    |
| 37  | השלמת טבלת הסקריפטים ב-README      | קטן    |
| 38  | פרק פריסה ב-README                 | קטן    |

**סדר מומלץ אם מאשרים הכול:** 20 ו-40 ראשונים (הם הופכים כל תקלה עתידית לניתנת
לאבחון, כולל בזמן שאר התיקונים), אחריהם 7, 15, 17 ו-34 — כולם שערי CI קטנים שמונעים
רגרסיה בהמשך — ורק אז הניקיון של 11, 12, 13 ו-16.

**הערה על #11:** זה הפריט שנוגע בהכי הרבה קבצים (35). הוא בטוח כי הוא החלפת מחרוזת
מחלקות זהה בקבוע שמכיל אותה בדיוק, אבל אני מציע לעשות אותו בקומיט נפרד ולבדוק
ויזואלית לפני מיזוג — `npm run test:e2e` מריץ 139 בדיקות שכוללות RTL בשני viewports
ו-axe, וזו הרשת הנכונה מתחתיו.

---

## 5. דורש ממני החלטה

15 פריטים. לכל אחד — השאלה המדויקת שצריך לענות עליה.

### קריטי

**#23 — קודי הדמו והאדמין על הפריסה החיה** — **הוכרע 2026-09-05: נשארים כרגע.**

> **החשיפה הורחבה ב-2026-09-06, במכוון ולבקשת המשתמש.** פאנל הדמו בדף הבית
> (`lib/demo.ts`) הופך את "מצא את ה-URL ואז נחש שהאדמין הוא `972500000005`"
> לכפתור עם תווית. האישורים לא השתנו; העמימות שהייתה ההגנה היחידה עליהם — כן.
> נוסף **מנוף רביעי, והזול מכולם**: להשאיר את `NEXT_PUBLIC_DEMO_LOGINS` לא מוגדר,
> או להגדיר אותו רק ב-Preview של Vercel ולהדגים משם, כך שה-alias של ה-production
> — זה ש-`robots.ts` מזמין לאינדוקס — לעולם לא נושא את הכפתור. שים לב שהוא
> `NEXT_PUBLIC_`, כלומר מוטבע בזמן בילד: כיבוי בדשבורד לא עושה כלום עד ה-deploy
> הבא.
> החלטת המשתמש, אחרי שהביקורת העלתה את זה כפריט הקריטי היחיד. זהו **סיכון מקובל
> עומד**, לא שאלה סגורה: הפריט נשאר פתוח ברשימה וב-`CLAUDE.md` סעיף 9.
> מה שפותח אותו מחדש הוא הדבר הראשון שאינו דמו פרטי — משתמש אמיתי, קישור ציבורי,
> או כל דבר שמניח את ה-URL במקום שאפשר למצוא אותו.
> שלוש הדרכים לסגור, כשזה יגיע: סיבוב הקודים בפרויקט המאוחסן בלבד, מחיקת משתמש
> הדמו admin מה-seed בענן, או Vercel Password Protection.
> _(תלוי ב-#25: סיבוב הוא `config push`, שדוחף כרגע גם את `site_url` השבור.)_

### גבוה

**#19 — כל המסלולים דינמיים**
❓ האם להוציא את מצב-ההתחברות מ-`AppShell` לרכיב לקוח או ל-Suspense, כדי שעמודי
Phase 8 יוכלו להיות סטטיים/ISR? זה שינוי מבני ב-shell שנוגע ב-13 עמודים.

**#24 — headers של אבטחה**
❓ ארבעת ה-headers הפשוטים — מאושר להוסיף מיד? ולגבי CSP: מתחילים
מ-`Report-Only` ואוספים הפרות, או מוותרים עליו בשלב הזה?
_(CSP צר מדי שובר את Google Maps, את WebSocket של Realtime ואת `next/font` — בשקט, בייצור.)_

**#26 — פיד בעלי המקצוע לינארי**
❓ מקבלים את התקרה ומנטרים אותה, או פותחים את מדיניות ה-SELECT על `jobs` לצורה
שאינדקס יכול לגעת בה?
_(זה כלל האבטחה הנושא של המוצר, `CLAUDE.md` סעיף 3. לא נוגע בלי הכרעה.)_

**#27 — `limit 300` ב-`admin_jobs()` והיצוא**
❓ 300 היא תקרה מכוונת שהמסך והיצוא צריכים להצהיר עליה, או שהמסך צריך pagination
והיצוא צריך לעקוף אותה? זו שאלה מוצרית על מה "יצוא דוח" מבטיח.

**#41 — מוניטורינג שגיאות**
❓ איזה ספק, ובאיזו עלות? Sentry, Vercel Log Drains, או שום דבר בינתיים ומסתפקים
ב-#40 (שהוא בחינם ונותן חלק גדול מהערך)?

### בינוני

**#4 — ESLint 9 → 10**
❓ מחכים ש-`eslint-config-next` יכריז תמיכה, או משדרגים עכשיו?

**#5 — TypeScript 5.9 → 7.0**
❓ משדרגים לקומפיילר החדש, או נשארים על 5.9 עד שהאקוסיסטם יתייצב?

**#6 — Vitest 3 → 5**
❓ קופצים ישירות ל-5, או עוברים דרך 4?

**#25 — `site_url` מצביע על localhost**
❓ מתקנים עכשיו יחד עם #23, או משאירים עד שתידרש שיטת התחברות שמפנה?

**#35 — `docs/security-checklist.md` החסר**
❓ כותבים את המסמך שהמיגרציה וסקריפט הביצועים מפנים אליו, או מתקנים את שלוש
ההפניות שיצביעו על `CLAUDE.md` סעיף 3?
_(הראשון נכון יותר — הוא ההסבר המלא של תקרת הפיד ב-#26 — וגם יקר יותר.)_

### נמוך

**#18 — a11y מתעלם מ-`moderate`**
❓ `moderate` נכנס לאכיפה לפני השקה ציבורית, או נשאר מחוץ לשער?

**#28 — פילטרים לא ניתנים לאינדוקס ב-`admin_jobs`**
❓ מוסיפים אינדקסי ביטוי על `job_city(address_text)`, או משאירים כפי שהוא?
_(אינדקס ביטוי פותר בלי לשבור את הכלל ש-`job_city` לעולם לא עמודה.)_

**#31 — אין workflow לפריסה**
❓ פריסה לייצור נחסמת עד ש-`database` ו-`e2e` ירוקים, או נשארת אוטומטית דרך Git?

**#32 — `run-all-phases.sh`**
❓ למחוק, להעביר ל-`scripts/`, או להשאיר בשורש?

**#33 — 4.9MB בינאריים ב-`design/`**
❓ להשאיר, לעבור ל-Git LFS, או לא לגעת?

**#39 — התראות push**
❓ נכנסות ל-scope, ואם כן — דפדפן או SMS?

---

## 6. מה נבדק, ואיך

| תחום                | הכלי                                                               | תוצאה              |
| ------------------- | ------------------------------------------------------------------ | ------------------ |
| תלויות              | `npm audit`, `npm outdated`                                        | 0 פרצות, 6 פיגורים |
| lint                | `npm run lint`                                                     | נקי                |
| טיפוסים             | `npx tsc --noEmit`                                                 | נקי                |
| פורמט               | `npm run format:check`                                             | נקי                |
| טסטים               | `npm run test`                                                     | 242/242            |
| בילד                | `npm run build`                                                    | עובר               |
| TODO/FIXME          | grep גורף על הריפו המנוהל                                          | 0                  |
| `any` / `ts-ignore` | grep על `*.ts`, `*.tsx`, `*.mjs`                                   | 0                  |
| קוד מת              | הצלבת כל שם מיוצא מ-`lib/` ו-`components/` מול צרכניו              | 0                  |
| פונקציות SQL מתות   | הצלבת 63 פונקציות מהמיגרציות מול קוד האפליקציה ומול pgTAP          | 0 מתות             |
| `security definer`  | סריקת גוף כל פונקציה אחר `set search_path`                         | 70/70 תקינות       |
| בליעת שגיאות        | קריאת כל 13 ה-`catch` בקוד                                         | כולם מכוונים       |
| N+1                 | סריקת `for`/`map(async`/`Promise.all` בכל מסלול                    | אחד (#22)          |
| pagination          | בדיקת `limit` בכל פונקציית רשימה במיגרציות                         | אחד חד (#27)       |
| הפניות מתות         | הצלבת `docs/*.md` ו-`design/screens/*.png` מול הקבצים בפועל        | אחת (#35)          |
| סודות               | `tests/secrets.test.ts` + בדיקת `.gitignore` על `.env*`, `.vercel` | נקי                |

**לא נבדק, ומדוע:** לא הורצו `npm run db:test` ו-`npm run test:e2e` במסגרת הביקורת —
שניהם דורשים העלאת Docker ו-Supabase מקומי, וזו הרצה של דקות שאינה משנה אף ממצא כאן.
כל טענה במסמך זה על ה-DB נקראה מהמיגרציות ומ-`rls_test.sql` כטקסט. `docs/roadmap.md`
מדווח שהם ירוקים (282 טענות pgTAP, 139 בדיקות Playwright).

---

**ממתין לאישורך לפני שאני מתחיל לתקן משהו נוסף מרשימת "מוכן להתחיל מיד".**

---

## 7. מה תוקן (2026-09-04)

ענף `fix-error-observability`, שלושה קומיטים.

### #40 — כל כשל כתיבה מדווח על סיבתו · **הושלם**

`lib/observability.ts` — שתי נקודות כניסה, כי הכשלים הם משני סוגים:
`logServerError` לכתיבה שהייתה אמורה לעבוד, ו-`logExpectedRefusal` לסירוב שיש
למוצר משפט בשבילו (הצעה שנייה על אותה קריאה, slug תפוס, קוד OTP שגוי). בלי
ההפרדה, כל הצעה כפולה הייתה נרשמת באותו משקל כמו תקלת אמת.

**30 ענפים** ב-13 מודולי הפעולות עודכנו. שלושה מהם היו _שקטים_ לגמרי ולא רק
לקוניים, והם הגרועים שבחבורה:

- `tracking.reportJobLocation` החזיר בוליאני חשוף. בעל מקצוע שהפינגים שלו נכשלו
  במשך שעה נראה בדיוק כמו אחד שלא הדליק את המתג מלכתחילה.
- `jobs.postJob` ו-`pros.writeProfile` תפסו חריגת geocode. מפתח Google Maps
  שפג תוקפו או נחסם נכשל שם בדיוק כמו כתובת שגויה, ושום דבר לא הבחין ביניהם.

`publicProfile.saveProfile` מבחין עכשיו בין `23505` ל-`23514`: slug תפוס הוא
המשתמש, אבל אילוץ `check` שדוחה slug ש-`lib/validation` אישר פירושו ששני
העותקים של הכלל נפרדו זה מזה — וזו תקלה, לא טעות של מי שמילא את הטופס.

`auth.verifyOtp` נזקק להבחנה שלא הייתה לו, ולכן `otpErrors.ts` קיבל
`isExpectedVerifyFailure` — **נגזר** מ-`describeVerifyError` במקום לשכפל את
הרשימה שלו, כי לפונקציה ההיא כבר יש תשובה בעברית לכל מה שהיא מזהה, והיא מהדהדת
את ההודעה הגולמית לכל מה שאינה מזהה. ארבע טענות מחזיקות את השתיים יחד, אחת מהן
האינווריאנט עצמו.

**אין באף שורה טלפון, שם, כתובת, גוף הודעה או פרט חשבון בנק.** מזהים מספיקים
כדי למצוא את השורה, ולוג יושב מחוץ לכל מדיניות RLS ששומרת על השאר.

### #20 — גבולות שגיאה · **הושלם**

`app/error.tsx` ו-`app/global-error.tsx`.

הראשון יושב בשורש `app/` ולא בתוך כל אזור, וזה העיקר ולא קיצור דרך: `error.tsx`
אינו עוטף את ה-`layout.tsx` שלצידו אלא רק את המקטעים שמתחתיו — וה-layouts שהכי
סביר שיזרקו כאן הם בדיוק ארבעת ה-`(authed)`, שבהם `requireRole()` קורא את
`profiles` לפני שעמוד מרונדר. גבול בתוך כל אזור היה מפספס את השער של עצמו.

**באג שנתפס תוך כדי:** הגרסה הראשונה בדקה `pathname.startsWith("/pro")`, ו-`/pro/`
הוא שני אזורים באותה קידומת — `/pro/dashboard` של בעל המקצוע המחובר, מול
`/pro/dana-levi` שהוא פרופיל ציבורי שכל לקוח קורא. לקוח שנתקל בשגיאה בפרופיל
היה מקבל קישור ל-`/pro/dashboard`, ש-`proxy.ts` מפנה ממנו למסך התחברות שאין לו
בו חשבון — מבוי סתום שהגיעו אליו מעמוד שניסה לעזור. עכשיו זה נשאל
מ-`loginPathFor()`, אותה פונקציה בדיוק ש-`proxy.ts` מכריע בה את אותה שאלה.

**מה שאומת בפועל, לא בקריאת קוד:** מסלול שזורק, מול `next build && next start`,
ואז דפדפן אמיתי. הכותרת בעברית מוצגת, כפתור "נסו שוב" מוצג, ה-`digest` שעל
המסך (`2369185625`) **זהה** לזה שבשורת הלוג של `onRequestError`, ההודעה האמיתית
לא דלפה לדפדפן, `dir="rtl"`, ואפס גלישה אופקית ב-390px. הפיגום נמחק אחרי הבדיקה.

הערה על Next 16: ה-prop הוא **`retry`** ולא `reset`. השם הישן עדיין עובר
typecheck כ-prop שאינו בשימוש, והכפתור פשוט לא עושה כלום — פקד מת בעמוד שהגיעו
אליו כי כבר משהו השתבש.

### #41 — מוניטורינג · **טופל חלקית**

`instrumentation.ts` עם `onRequestError` תופס כל שגיאה לא-מטופלת בשרת — Next
קורא לו עבור `render`, `route`, `action` ו-`proxy` כאחד — ורושם את ה-`digest`,
הנתיב ו-`routeType`. הפלט הוא שורת JSON ל-stderr, ש-Vercel כבר אוסף, בלי ספק
ובלי עלות. לכן התיקון לא חיכה להחלטה שלך.

מה שעדיין חסר, ונקנה בכסף: התראות, קיבוץ, שמירה ארוכה מזו של Vercel, ו-stack
trace שנתפר בחזרה למקור. ההחלטה נשארה פתוחה ורשומה ב-`CLAUDE.md` סעיף 9. כשייבחר
ספק, הוא מתחבר לשתי הפונקציות האלה בלבד — זו הייתה כל הסיבה לנתב 30 אתרי קריאה
דרכן ולא להשאיר `console.error` מפוזר.

### שערים חדשים שמונעים חזרה

`tests/errorBoundaries.test.ts` קורא את `lib/actions/` כטקסט ונכשל על כל ענף
שגיאה שמחזיר עברית בלי להגיע ללוגר — כלומר על המצב שכל הריפו היה בו עד הענף
הזה, כך ש-server action שייכתב בשלב עתידי לא יוכל לחזור אליו בשקט. **אומת שהוא
באמת תופס:** הסרת קריאת לוג אחת מ-`messages.ts` הכשילה אותו, והחזרתה החזירה
אותו לירוק. הוא גם מקבע את `retry` מול `reset`.

`lib/__tests__/routes.test.ts` מקבע את ההבחנה בין `/pro/dashboard` לבין
`/pro/<slug>` — הבאג שתואר למעלה.

סה"כ הטסטים: 242 → **267**. `lint`, `typecheck`, `format:check`, `test`
ו-`build` — כולם עוברים.

### מה לא נעשה

`npm run db:test` ו-`npm run test:e2e` לא הורצו: שניהם דורשים Docker ו-Supabase
מקומי, ואף אחד מהשינויים כאן אינו נוגע במיגרציה, במדיניות RLS או בזרימה
שה-E2E מכסה. ה-CI מריץ את שניהם על ה-PR.

---

## 8. סבב התיקונים השני (2026-09-05)

ענף `fix-technical-debt-batch`, שמונה קומיטים. **19 פריטים נסגרו**, שניים נדחו.

### תלויות — #1, #2, #3, #8, #9

`@types/node` היה החשוב: הוא הצהיר `^20` בזמן שה-CI, ה-README וכל מפתח כאן רצים
על Node 22, כלומר כל API שנוסף ב-22 לא היה מוטפס. עכשיו `^22` — לא 26, כי
הטיפוסים אמורים לתאר את זמן הריצה ולא את הגרסה החדשה ביותר.

ה-CLI של Supabase היה טווח caret בזמן ש-CLAUDE.md סעיף 2 טען "נעול וזהה לכולם".
עכשיו מפונן בדיוק, כך שההצהרה והקובץ מסכימים. נוספו `engines` ו-`.nvmrc`.

נלקחו גם המינורים שהופיעו מאז הביקורת: `@supabase/ssr`, `@playwright/test`.
מה שנשאר מיושן הוא בדיוק שלושת ה-majors שהם הכרעה שלך — ESLint 10, TypeScript 7,
Vitest 5. `npm audit` נשאר על אפס.

### קוד — #10, #11, #12

**#10** — `optional` ו-`fieldErrorsOf` היו שש פונקציות, שתי התנהגויות, כל עותק
זהה. הסיכון מעולם לא היה הכפילות אלא שהן מכריעות מה פירושו שדה ריק ואיזו הודעה
נוחתת מתחת לאיזה פקד. תשע טענות עליהן עכשיו.

**#11** — `primitives.tsx` נפתח בהבטחה ש"re-skin הוא עריכה אחת כאן". לכפתורים זה
היה נכון; לכרטיס לא — `CARD_CLASS` יוצא ואף אחד לא ייבא אותו, בזמן ש-55 עותקים
inline ב-35 קבצים. `CARD_BASE` הוא הכרטיס בלי ה-padding, וזה מה שאיפשר לגעת בכל
הכרטיסים בלי לגעת באף ריווח.
**שום דבר לא מרונדר אחרת, וזה נבדק ולא הונח:** כל `className` ב-55 הקבצים חולץ
לפני ואחרי, הקבועים נפרסו חזרה לערכיהם, והרשימות הושוו — זהות ברמת הבית, כולל סדר.

**#12** — `pros.ts` היה 493 שורות. חמשת ה-helpers משרתים רק את אשף ההרשמה, ולכן
הוא נשלף שלם: 379 + 143. אומת כהעברה טהורה — 15 פונקציות לפני, 15 אחרי, כל גוף
זהה ברמת הבית.

### חוויית שימוש — #21, #29, #30

**#21** — 13 קובצי `loading.tsx`. כל מסך מאחורי סשן הוא `force-dynamic` וחוסם על
השאילתות שלו, ותיעוד הקריאה מריץ ארבע, אחר כך עוד ארבע, אחר כך אחת לכל הצעה.
השלד **מוכרז ולא רק מצויר**: מסך של מלבנים אפורים הוא כלום עבור קורא מסך, ומי
שהכי צריך לדעת שהדף עדיין עובד הוא זה שלא רואה את זה קורה.

**#29** — כל אירוע Realtime קרא ל-`router.refresh()`. ההבזק עדיין מיידי; רק
השליפה ממתינה 300ms לראות אם מגיע עוד.

**#30 — נבדק ואינו באג.** הביקורת לא יכלה להכריע מהריפו. מהמקור:
`createBrowserClient` מחזיר singleton בדפדפן, ה-constructor נרשם ל-
`onAuthStateChange`, ו-`TOKEN_REFRESHED` מגיע ל-`realtime.setAuth()`, שדוחף את
הטוקן החדש לכל ערוץ מחובר. אותו singleton גם מסביר למה כמה רכיבים במסך אחד
עולים socket אחד ולא שלושה. נרשם ברכיב, כי הספק הזה יחזור.

### שערים — #7, #15, #16, #17, #34

**#15** — הכיסוי נמדד על מה שאמור להיבדק ביחידה, וזו כל הנקודה: על `lib/**` הוא
27%, כי `lib/supabase` ו-`lib/actions` אינם נבדקים ביחידה **בכוונה** — מה שהם
נושאים חי ב-RLS ובפונקציות `security definer` ומוכח ב-pgTAP. שער על 27% לא היה
אומר כלום. על הלוגיקה הטהורה הוא 91%, והספים יושבים מתחת לזה.

**#16** — הענף שקורא `error.code` ובוחר בין משפט שמנקב את הסיבה למשפט שמנחש הוא
לוגיקה טהורה, וטעות בו בלתי-נראית: המסך עדיין אומר משהו בעברית, רק לא הנכון.
שמונה טענות, וכל אחת בודקת גם **איזה** לוגר נקרא.

**#7** — `npm audit` ב-CI ו-Dependabot שבועי. שום דבר לא היה מיושן; מה שחסר היה
מישהו ששם לב.

**#17** — `perf:postgis` רץ בג'וב `database`. 328 שורות של תקציבי רגרסיה שחייבו
שמישהו יזכור להריץ אותן ידנית.

**#34** — `pre-push` עם typecheck וטסטים. `pre-commit` מפרמט ומלנטט את הקבצים
שבסטייג' ואינו יכול לראות אם הריפו עדיין עובד.

### תיעוד — #36, #37, #38

**#36** היה החד: הפסקה השלישית ב-README — הדבר הראשון שכל אחד קורא — אמרה שעמוד
הבית הוא placeholder שמדווח על מצב הבילד. זה היה נכון עד Phase 8.
**#37**: הטבלה מנתה 11 מתוך 18; חסר בין השאר `npm run test:e2e`, חבילה של 139
בדיקות. **#38**: לא היה שום דבר על הפריסה — עכשיו יש, כולל מה בדיוק נשבר כשכל
משתנה סביבה חסר.

### מה נדחה, ולמה

שני הפריטים היחידים שנותרו ברשימת "מוכן להתחיל מיד", ושניהם מאותה סיבה:
**Docker לא רץ על המכונה הזו.**

**#22 — ה-N+1 בתיעוד הקריאה.** התיקון הוא פונקציית `security definer` אחת
שמחזירה את כל השיחות של קריאה במקום אחת לכל הצעה. `thread_messages` היא definer
מסיבה טובה — היא מצרפת שם מ-`profiles`, שאין עליו מדיניות קריאה צולבת — ולכן אי
אפשר להחליף אותה בשאילתה ישירה. פונקציה חדשה פירושה מיגרציה, טענת pgTAP,
ו-`npm run db:types` מחדש; ה-DoD ב-CLAUDE.md סעיף 7 דורש את שלושתם, ואף אחד מהם
לא ניתן לאימות בלי המסד. מיגרציה לא-מאומתת וטיפוסים ישנים גרועים מ-N+1 בחומרה
נמוכה שרץ ממילא במקביל.

**#13 — פיצול `rls_test.sql`.** 3,026 שורות שאפשר לפצל לפי סעיף, אבל הדרך
היחידה לדעת שהפיצול לא איבד טענה היא `npm run db:test`.

שניהם ייסגרו בהרצה אחת ברגע ש-Docker פעיל.

### מצב אחרי הסבב

| מדד                      | לפני הביקורת | עכשיו      |
| ------------------------ | ------------ | ---------- |
| טסטים                    | 242          | 292        |
| קובצי טסט                | 16           | 21         |
| כיסוי הלוגיקה הטהורה     | לא נמדד      | 91%, עם סף |
| שערי CI                  | 6            | 9          |
| פריטי חוב פתוחים מתוך 42 | 42           | 21         |

`lint`, `typecheck`, `format:check`, `test:coverage` ו-`build` — כולם עוברים,
באפס אזהרות.

---

## 9. מה שהיה חסום על Docker (2026-09-06)

### #22 — ה-N+1 בתיעוד הקריאה · **תוקן**

`job_threads()` שואלת פעם אחת את מה ש-`thread_messages()` נשאלה פעם לכל הצעה.
מיגרציה, שמונה טענות pgTAP (300 → 308), טיפוסים מחודשים.

מה שחשוב בה יותר מסיבובי הרשת: **היא אינה קורא של מנהל.** `CLAUDE.md` סעיף 3
קובע שאין בריפו הזה שום הטלה של קריאה שקיימת רק למנהל, וזו לא הראשונה — מסנן
השורות הוא שתי מדיניות ה-SELECT על `messages` בניסוח מחדש. פונקציית definer
משעה RLS, ולכן הכלל שהיא השעתה נכתב בתוכה שוב. **אומת בכך שנמחק המסנן ונצפו שתי
טענות נכשלות**: בעל מקצוע שביקש קריאה שהגיש עליה הצעה קיבל את שתי ההודעות שלו
ואפס שורות של המתחרה.

### #13 — פיצול `rls_test.sql` · **נבדק ונדחה**

לא "לא הספקתי". ניסיתי, ויש ראיה.

חילצתי את סעיף 15 (Phase 8) לקובץ עצמאי עם preamble מלא — כל ה-`\set`,
`pg_temp.act_as`, `plan`, `begin`/`rollback` — והרצתי אותו לבדו. **שלוש טענות
נכשלו**, ואחת מהן מנסחת את הסיבה בעצמה:

> `and its numbers come from commission_charges — real closed jobs, **including
the ones this file closed**`

הקובץ אינו אוסף סעיפים אלא **רצף עם מצב מצטבר**. משתני `\set` מוגדרים לאורכו
(שורות 786, 1206, 2037, 2468), יש `insert into auth.users` באמצע (שורה 544),
וסעיפים מאוחרים טוענים על מצב שסעיפים מוקדמים יצרו — במכוון. פיצול אינו חיתוך
מכני אלא כתיבה מחדש של אסטרטגיית הפיקסצ'רים, על **קובץ הבדיקות היחיד שעומד בין
נתוני לקוח אחד לאחר**, בתמורה לניווט נוח יותר בפריט שסיווגתי בעצמי כחומרה נמוכה.

לא שווה את הסיכון. הפריט נסגר כ"נבחן ונדחה" ולא נשאר תלוי.

### רגרסיה שהתגלתה, ושאני הכנסתי

הרצת Playwright הראשונה שהתאפשרה חשפה **שלושה כשלים ב-`access-control.spec.ts`**,
כולל "לקוחה ב' לא יכולה לפתוח את הקריאה של לקוחה א'". ביסקציה, לא ניחוש: 34/34
עוברים בקומיט שלפני `loading.tsx`, שלושה נכשלים בו.

`loading.tsx` הוא גבול Suspense. התגובה מתחילה לזרום לפני שהשאילתה נפתרת, ואחרי
שהכותרות יצאו `notFound()` כבר לא יכול לקבוע סטטוס — 404 הפך ל-200 שנושא את דף
ה-404.

**שום מידע לא נחשף, וזה נבדק ולא הונח:** RLS החזיר אפס שורות, הדף הציג את מסך
ה-404, ולקוחה ב' שמושכת את הקריאה של לקוחה א' לא ראתה ממנה דבר — קראתי את גוף
התגובה ולא הסתמכתי על הסטטוס. Next גם מסמן גוף זורם של not-found כ-`noindex`,
ולאף מסלול ציבורי לא היה `loading.tsx` מלכתחילה, כך שמנועי חיפוש לא הושפעו כלל.

מה שאבד היה הסטטוס הכן בחמישה מסכים מאחורי התחברות — ושם הוא בדיוק הדרך שבה
הריפו הזה אומר בטסט שלקוח אחד אינו מגיע לקריאה של אחר.

הכלל שיצא מזה: **מסך שיכול לענות "אין רשומה כזו" לא מקבל שלד זורם.** מתוך 13
השלדים, שמונה הפרו אותו — חמישה ישירות ושלושה כי `loading.tsx` עוטף את **כל
תת-העץ**, ו-`admin/loading.tsx` הזרים את תיעוד הקריאה שלוש רמות מתחתיו. חמישה
נשארו, על מסכים שתמיד קיימים.

`tests/streamingStatus.test.ts` מחזיק את הכלל. **הגרסה הראשונה שלו בדקה רק אחים
ועברה בזמן ש-`/pro/jobs/[jobId]` עדיין היה שבור מהורה** — הרחבתי אותו לתת-עץ,
ואימתתי את ההרחבה בכך שהחזרתי את ההורה וראיתי אותו נכשל.

### פאנל הדמו — אומת בפועל

החוב הפתוח מהסבב הקודם. מול GoTrue אמיתי ובילד ייצור:

- ארבעת הכפתורים מתחברים ונוחתים נכון — `/account`, `/pro/dashboard`,
  `/pro/dashboard`, `/admin`
- הפאנל מזהה מי מחובר, מנטרל את הכפתור שלו, ו"חזרה לתצוגת אורח" מחזירה לדף הבית
- לחיצה חוזרת על אותו משתמש בתוך דקה **אכן** נחסמת, וההודעה עברית:
  _"נשלחו יותר מדי בקשות. המתינו דקה ונסו שוב."_ — היא מגיעה מהענף המקודד הקיים,
  לא מהענף החדש שהוספתי; החדש נשאר נכון לנתיב שבו GoTrue אינו מחזיר קוד
  (`e2e/helpers.ts` תיעד בדיוק את המחרוזת הזו)

### מצב הבדיקות עכשיו — הכול הורץ, לא נטען

| חבילה                  | תוצאה                                      |
| ---------------------- | ------------------------------------------ |
| `npm run db:test`      | **308/308**                                |
| `npm run test:e2e`     | **139/139**                                |
| `npm run test`         | **328**                                    |
| `npm run perf:postgis` | כל התקציבים עוברים; הפיד על 222ms מתוך 400 |

הפיד על 222ms מאשר את #26: לינארי, בתוך התקציב בעומס ברירת המחדל, ועדיין הכרעה
ארכיטקטונית פתוחה.
