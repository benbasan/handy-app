# תוכנית פעולה — שלבי בנייה עם Claude Code

עקרון מנחה: **כל שלב נבנה על בסיס יציב ונבדק של השלב הקודם**. לא עוברים לשלב הבא לפני שסעיף ה"הגדרת סיום" (Definition of Done) של השלב הנוכחי מסומן כ-✅ ונדחף ל-`main` בפועל. הפירוט המלא של המוצר ב-`product-spec.md`, הפירוט הטכני ב-`architecture.md`.

לכל שלב מצורף **טקסט קיקאוף מוכן** — זה בדיוק מה שמעתיקים להודעה הראשונה לקלוד קוד כדי לפתוח את השלב. קלוד קוד תמיד יקרא קודם את `CLAUDE.md`, ולפי ההנחיה שם גם את שלושת המסמכים בתיקיית `docs/` ואת `design/README.md` (מיפוי צילומי המסך ב-`design/screens/`).

---

## Phase 0 — תשתית ובסיס פרויקט

**מטרה:** ריפו עובד, ריק מבחינת פיצ'רים, אבל עם כל התשתית שכל שלב עתידי יישען עליה — כדי שקלוד קוד לא "יתחיל מאפס" בכל שיחה.

**מה נכלל:**
- יצירת ריפו ב-GitHub (פרטי), חיבור ל-Vercel.
- יצירת פרויקט Supabase, חיבור מקומי (`supabase` CLI), הפעלת extension ל-PostGIS.
- Scaffold של Next.js (App Router, TypeScript strict, Tailwind), מבנה התיקיות מ-`architecture.md` סעיף 2.
- הטמעת `CLAUDE.md` ותיקיית `docs/` (המסמכים האלה) בשורש הריפו.
- `.env.example` עם כל המשתנים הנדרשים (ריק מסודות אמיתיים).
- ESLint + Prettier + husky pre-commit (lint על כל commit).
- `README.md` בסיסי: איך מריצים מקומית.
- דף בית זמני ("Hello Handy") שמוכיח שה-build עובד ושה-deploy ל-Vercel מצליח.

**הגדרת סיום:**
- [x] `npm run build` ו-`npm run lint` עוברים (וגם `npm run typecheck` ו-`format:check`)
- [ ] דיפלוי ל-Vercel חי ונגיש — **ממתין למשתמש**: הקוד וה-CI מוכנים, החיבור בדשבורד Vercel נעשה על ידיו
- [x] פרויקט Supabase מחובר, ניתן להריץ `supabase migration list` בהצלחה — עובד מול הסטאק הלוקאלי (`--local`); הלינק לפרויקט הקלאוד (`supabase login` + `link`) **ממתין למשתמש** כי הוא דורש טוקן אישי
- [x] `CLAUDE.md` ו-`docs/*.md` נמצאים בריפו
- [x] commit ראשון ב-`main`, לפי מוסכמת ה-commits מ-`CLAUDE.md`

**הערה:** Vitest לא הוקם ב-Phase 0 — הוא לא מופיע ברשימת "מה נכלל" של השלב, וכלל "שלב אחד בכל פעם" אוסר לבנות קדימה. יוקם בשלב שידרוש אותו בפועל.

**טקסט קיקאוף לקלוד קוד:**
> קרא את `CLAUDE.md` ואת כל הקבצים ב-`docs/`. נתחיל ב-Phase 0 מתוך `docs/roadmap.md`: הקמת תשתית הפרויקט בלבד — Next.js scaffold, חיבור Supabase, מבנה תיקיות, lint/CI בסיסי, ודף בית זמני שמוכיח שהכל עובד. אל תבנה שום פיצ'ר מוצרי בשלב הזה. בסוף, ודא שכל סעיפי ה"הגדרת סיום" של Phase 0 מתקיימים, ותן לי סיכום.

---

## Phase 1 — מסד נתונים + אימות (הבסיס האמיתי)

**מטרה:** כל סכימת ה-DB מ-`architecture.md` קיימת כ-migrations אמיתיים עם RLS, וזרימת ההתחברות (טלפון+OTP) עובדת מקצה לקצה לשני התפקידים.

**מה נכלל:**
- Migrations לכל הטבלאות בסעיף 3 של `architecture.md` (`profiles`, `pro_profiles`, `verification_documents`, `categories`, `jobs`, `bids`, `price_updates`, `messages`, `reviews`, `commission_charges`, `disputes`, `saved_pros`).
- מדיניות RLS לכל טבלה (לפי סעיף 4 של `architecture.md`).
- הגדרת Supabase Auth Phone Provider + Twilio.
- מסכי כניסה (טלפון → OTP) ללקוח ולבעל מקצוע, כולל יצירת שורת `profiles` אוטומטית עם `role` נכון.
- Middleware/הפניה לפי role (לקוח → `/(customer)`, בעל מקצוע → `/(pro)`, אדמין → `/(admin)`).
- `seed.sql` עם כמה קטגוריות (`categories`) ומשתמשי דמו.

**הגדרת סיום:**
- [x] כל הטבלאות קיימות עם RLS, ונבדק ידנית: לקוח א׳ לא יכול לראות קריאה של לקוח ב׳ — נבדק גם דרך PostgREST עם JWT אמיתי וגם ב-33 בדיקות pgTAP (`npm run db:test`)
- [ ] אפשר להתחבר בפועל עם מספר טלפון אמיתי ולקבל SMS — **ממתין למשתמש**: הזרימה בנויה ועובדת מקצה לקצה מול `[auth.sms.test_otp]`; SMS אמיתי דורש חשבון Twilio ופרויקט Supabase Cloud מקושר (ראו README, "Going live with Twilio")
- [x] קיים מסך "מי אני" בסיסי שמראה role + פרטי המשתמש שהתחבר — `/account`, `/pro`, `/admin`
- [x] `docs/architecture.md` מעודכן אם היו סטיות מהטיוטה המקורית — ראו "סטיות מהטיוטה" ו"מבנה נתיבים ותפקידים" בסעיף 3

**החלטות שנפלו בשלב הזה** (הפירוט ב-`docs/architecture.md` סעיף 3):
- מבנה נתיבים: לקוח בשורש (`/login`, `/account`), בעל מקצוע ואדמין עם prefix.
- הרשאות ברמת עמודה בנוסף ל-RLS — זה מה שמונע מלקוח להגדיר את עצמו `admin` ומבעל מקצוע לאמת את עצמו.
- `proxy.ts` (השם החדש של `middleware.ts` ב-Next 16) עושה רק רענון סשן ושער אנונימי; אכיפת role ב-layout.
- בדיקות RLS כ-pgTAP, לא Vitest — אין לוגיקה שמצדיקה Vitest בשלב הזה.

**הערה על עיצוב:** מסכי ההתחברות (`/login`) כבר נבנו בשלב הזה, לפני שקובץ `design/screens/` היה קיים. כדאי — לא חובה — לבקש מקלוד קוד להשוות אותם ל-`design/screens/customer-1.2-login-otp.png` ו-`design/screens/pro-1.2-login.png` ולתקן פערים לפני Phase 2, אם עוד לא נעשה.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 1 מתוך `docs/roadmap.md`: הקמת מסד הנתונים המלא לפי `docs/architecture.md` סעיף 3 (migrations לכל הטבלאות, כולל RLS לפי סעיף 4), והקמת אימות טלפון+OTP דרך Supabase Auth ל-2 התפקידים (לקוח, בעל מקצוע). למסכי הכניסה עצמם, תסתכל על `design/screens/customer-1.2-login-otp.png` ו-`design/screens/pro-1.2-login.png`. אל תבנה עדיין את מסכי המוצר עצמם (פרסום קריאה וכו') — רק את השכבה הזו. בסוף, הרץ בדיקת RLS ידנית (נסה לגשת לנתונים של משתמש אחר) ודווח.

---

## Phase 2 — זרימת לקוח: פרסום קריאה

**מטרה:** לקוח יכול לפרסם קריאת שירות אמיתית מקצה לקצה (ללא הצעות עדיין — זה Phase 4).

**מה נכלל:** הטופס הרב-שלבי מ-`product-spec.md` סעיף 3.2 (תחום → תיאור → מדיה → זמינות → כתובת עם Places Autocomplete → סיכום → פרסום), העלאת מדיה ל-Supabase Storage, גיאוקוד הכתובת ושמירת `jobs.location`, מסך "הקריאה פורסמה" (סטטי בשלב הזה).

**הגדרת סיום:**
- [x] קריאה נשמרת ב-DB עם כל השדות כולל מיקום גיאוגרפי תקין — נבדק מקצה לקצה מול הסטאק הלוקאלי (התחברות OTP אמיתית → `insert` דרך PostgREST תחת RLS → קריאה חוזרת): ה-`geography` חוזר כ-`32.0853, 34.7818` דרך עמודות `latitude`/`longitude` הנגזרות
- [x] מדיה מועלית ונגישה רק לבעלים ולבעלי מקצוע רלוונטיים (RLS על ה-bucket) — bucket פרטי `job-media` עם 4 מדיניות; 9 בדיקות pgTAP חדשות (סה״כ 44) ובדיקת קצה לקצה: לקוח א׳ לא מצליח להעלות לתיקייה של לקוח ב׳, לא מצליח לחתום URL לקובץ שלו, ובעל מקצוע מאומת ברדיוס כן
- [x] הטופס עובד תקין ב-RTL, בדסקטופ ובמובייל — נבדק ברינדור אמיתי ב-Chrome headless ב-1280px וב-390px: אין גלישה אופקית באף מסך, ואין ולו utility פיזי אחד (`ml-`/`pr-`/`left-`…) בכל ה-diff של השלב
- [ ] Places Autocomplete ומפת המיקום במסך "הקריאה פורסמה" — **ממתין למשתמש**: הקוד בנוי במלואו מאחורי בדיקת קיום מפתח, אבל `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` ריק ואי אפשר לאמת מול Google בלי מפתח אמיתי. בלי מפתח הזרימה עובדת דרך הזנת כתובת ידנית וגיאוקוד מקומי (ראו `lib/maps/geocode.ts`)

**החלטות שנפלו בשלב הזה** (הפירוט ב-`docs/architecture.md`):
- **טופס עמוד אחד עם שלבים ממוספרים**, לא אשף עם "הבא". סעיף 3.2 מתאר רצף שלבים, והעיצוב (`customer-2.1-post-job.png`) פורש את אותו רצף על עמוד אחד עם סיכום חי לצידו. השלבים שמרו על הסדר והמספור; במובייל הטורים נערמים והסיכום נוחת בסוף, שם ממילא נמצא כפתור הפרסום.
- ~~**`jobs.search_radius_km`**~~ — הרדיוס שהלקוח בחר בשלב הכתובת. **נמחק ב-11.9.2026** (ראו Phase 13.6): הרדיוס היחיד הוא של בעל המקצוע.
- **`jobs.preferred_time` הפך לאוצר מילים סגור** (`asap`/`today`/…) עם `check`. כטקסט חופשי, ה-UI היה יכול רק להחזיר את מה שנשמר.
- **מדיה עולה ישירות מהדפדפן ל-Storage**, והטופס שולח נתיבים ולא קבצים — סרטון של 30 שניות גדול בהרבה ממגבלת הגוף של Server Action.
- **שני מפתחות Google Maps** (דפדפן מוגבל-referrer, שרת מוגבל-IP) ו-fallback מלא בלי מפתח בכלל, מאחורי `ALLOW_NO_MAPS_KEY`.
- **Vitest הוקם בשלב הזה** — זהו השלב הראשון עם לוגיקה שמצדיקה אותו (הגיאוקוד המקומי וסכמות ה-Zod). מה שתלוי ב-RLS נשאר ב-pgTAP.
- **עמוד הנחיתה של הלקוח נבנה כאן** (`design/README.md` ממפה את `customer-1.1-landing.png` ל-Phase 2), והחליף את דף הבית הזמני של Phase 0. עמודי התוכן/SEO נשארים ב-Phase 8.

**שתי סטיות מודעות מהעיצוב:**
- הנתונים בהירו (‎+4,200 קריאות, 12 דק׳, 4.9) הם מילוי של פרוטוטייפ. פרסום מדדים מומצאים כעובדה בעמוד ציבורי אינו פרט עיצובי שראוי להעתיק — אותה שורת שלושה נושאת במקומם את ההבטחות האמיתיות של המוצר.
- כרטיס "בעלי המקצוע שלי" באזור האישי הוא מצב ריק ולא שני שמות כמו במוק: ל-`pro_profiles` אין עדיין מדיניות קריאה ללקוחות, והרחבת RLS כדי למלא כרטיס היא הסיבה הלא נכונה להרחיב RLS.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 2: זרימת פרסום קריאה מצד הלקוח, לפי `docs/product-spec.md` סעיף 3.2. **תסתכל קודם על** `design/screens/customer-1.1-landing.png`, `customer-2.1-post-job.png` ו-`customer-5.1-my-account.png` (ראה `design/README.md`) — תבנה לפי מה שאתה רואה, לא רק לפי התיאור. בנה את הטופס הרב-שלבי, העלאת המדיה, וגיאוקוד הכתובת. אין עדיין מערכת הצעות — קריאה שפורסמה נשארת בסטטוס `open`. ודא responsive ו-RTL תקינים.

---

## Phase 3 — זרימת בעל מקצוע: Onboarding + פיד קריאות

**מטרה:** בעל מקצוע יכול להירשם, לעבור אימות, ולראות קריאות פתוחות רלוונטיות אליו.

**מה נכלל:** 5 שלבי ה-Onboarding מ-`product-spec.md` סעיף 4.2 (כולל העלאת מסמכי אימות), מסך אישור אדמין בסיסי (ידני, אפילו זמני, כדי שיהיה אפשר לבדוק את הזרימה מקצה לקצה), פיד קריאות פתוחות עם שאילתת PostGIS לפי רדיוס.

**הגדרת סיום:**
- [x] בעל מקצוע חדש עובר את כל 5 השלבים ומגיע לסטטוס `pending` — נבדק מקצה לקצה מול הסטאק הלוקאלי, מ-`draft` ריק: שליחה לאישור נדחית פעמיים (בלי תחום/כתובת, ואז בלי ת.ז), ורק אחרי שכל שלושת החלקים קיימים היא מחזירה `pending` עם `onboarding_step = 5` ו-`submitted_at`. השלמות נבדקת ב-`submit_pro_for_approval()` שבמסד הנתונים ולא בטופס
- [x] יש דרך (ולו זמנית) לאשר בעל מקצוע ל-`verified` — `/admin/pros` (`design/screens/admin-7.2`, בגרסה מינימלית). האישור עובר דרך `set_pro_verification()` שבודקת `is_admin()` בעצמה; לאף client role אין grant על `verification_status`, ונבדק שבעל מקצוע נכשל גם ב-PATCH ישיר (403) וגם בקריאה לפונקציה (403)
- [x] פיד הקריאות מציג רק קריאות בתוך הרדיוס שהוגדר, לפי שאילתת PostGIS אמיתית (לא סינון JS) — `open_jobs_for_pro()`: `ST_DWithin` מאונדקס מול `service_point` ו-`ST_Distance` למרחק שבכרטיס. השער עצמו במדיניות ה-RLS (`pro_serves_job`), ונבדק ששני רדיוסים חייבים להסכים, שבעל מקצוע לא מאומת מקבל פיד ריק, ושקריאה בתחום שלא נבחר לא מופיעה

**נבדק בנוסף (הגדרת הסיום הכללית, `CLAUDE.md` סעיף 7):**
- [x] `npm run db:reset` נקי, `npm run db:test` — 67 בדיקות pgTAP (23 מהן חדשות ל-Phase 3), `npm run test` — 45 בדיקות Vitest (28 חדשות)
- [x] `npm run lint` / `npm run typecheck` / `npm run build` עוברים
- [x] `npm run db:types` הורץ מחדש והטיפוסים מקומטים
- [x] RTL: כל מסכי השלב רונדרו בפועל (Chrome headless, 1280px ו-390px) ואין ולו utility פיזי אחד (`ml-`/`pr-`/`left-`/`text-right`…) בכל ה-diff של השלב
- [x] אין משתני סביבה חדשים, ולכן `.env.example` לא השתנה
- [ ] Places Autocomplete בכתובת הבסיס והמפה במסך הפיד — **ממתין למשתמש**: הקוד בנוי מאחורי בדיקת קיום מפתח, אבל `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` ריק. בלי מפתח הכתובת מוקלדת ידנית ומגואקדת מול ה-gazetteer, ובמקום המפה מוצג הסבר — המרחק בכל כרטיס מחושב ממילא ב-DB ואינו תלוי במפה
- [ ] SMS "הפרופיל אושר" כשאדמין מאשר — **ממתין למשתמש**: דורש חשבון Twilio ופרויקט Supabase Cloud מקושר, בדיוק כמו סעיף ה-OTP ב-Phase 1. המסך מבטיח SMS; המנגנון עצמו יתווסף עם טבלת `notifications`, בשלב שבונה התראות

**החלטות שנפלו בשלב הזה** (הפירוט ב-`docs/architecture.md`):
- ~~**שני הרדיוסים חייבים להסכים**~~ — ההכרעה ש-Phase 2 השאירה פתוחה, ושבוטלה ב-11.9.2026. קריאה נראית לבעל מקצוע רק במרחק `radius_km` שלו עצמו; האכיפה נשארה במדיניות ה-RLS ולא בשאילתה (כולל `can_read_job_media`).
- **`/pro` הפך לעמוד נחיתה ציבורי**, והבית של בעל המקצוע ירד ל-`/pro/dashboard` — שני ה-URL-ים שבעיצוב עצמו. route group לא מוסיף segment, ולכן `/pro` לא יכול היה להיות גם עמוד שיווקי פתוח וגם בית מוגן.
- **`verification_status` אינו ניתן לכתיבה לאף client role.** שני המעברים החוקיים הם `submit_pro_for_approval()` ו-`set_pro_verification()` — `security definer` שבודקות את הקורא בעצמן. grant רחב מספיק לאדמין היה מאפשר גם לבעל מקצוע לאמת את עצמו.
- **מצב `draft` חדש**, וברירת המחדל לבעל מקצוע חדש. בלעדיו "עוד לא מילא כלום" ו"ממתין לצוות Handy" היו אותו `pending`.
- **`pro_categories` הוא מסנן ולא גבול אבטחה** — קריאה בתוך הרדיוס אינה סוד מבעל המקצוע רק בגלל שהיא בתחום אחר, ולכן הצמצום לפי תחום חי בשאילתת הפיד ולא במדיניות.
- **רק 4 ספרות של חשבון הבנק נאספות.** מספר החשבון המלא נוגע בתנועת כסף אמיתית — החלטה למשתמש, בשלב התשלומים (`CLAUDE.md` סעיף 9).
- **תמונת הפרופיל יושבת ב-bucket הפרטי** `verification-docs` כ-`doc_type = 'profile_photo'`. bucket ציבורי לתמונות פרופיל שייך לשלב שבו נבנה הפרופיל הציבורי ללקוחות (Phase 8).

**שלוש סטיות מודעות מהעיצוב:**
- כרטיס ההכנסות השבועיות בפיד ובדשבורד, רשימת ההצעות, ומתגי ההתראות בהגדרות — כולם שייכים ל-Phase 4/6. הושמטו ולא מולאו במספרים מומצאים.
- כפתור "הגש הצעת מחיר" בכרטיס בפיד מרונדר **מושבת** ואומר זאת: מסך הגשת ההצעה הוא Phase 4, וקישור מת גרוע מ"עוד לא".
- ההכנסה החציונית בהירו של עמוד הנחיתה (‎₪2,450) היא מילוי של פרוטוטייפ. פרסום נתון הכנסה מומצא לאנשים ששוקלים לעבוד כאן אינו פרט עיצובי שראוי להעתיק — במקומו מוצגים תנאי ההתקשרות האמיתיים.

**הערה על 5 השלבים:** `product-spec.md` 4.2 מונה פרופיל → מסמכים → תרגול → תשלומים → שליחה לאישור, והעיצוב (`pro-1.4`) מונה ברוך הבא → פרופיל → מסמכים → תרגול → תשלומים ומוכנות. נבנה לפי העיצוב, ו"שליחה לאישור" היא כפתור הסיום של שלב 5 — כך שכל מה שהאפיון מונה קיים. `/pro/join` (`pro-1.3`) הוא מסלול מהיר שממלא את שלבים 2–3 במסך אחד ומחזיר לאשף בשלב 4.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 3: Onboarding לבעל מקצוע (5 שלבים, `docs/product-spec.md` סעיף 4.2) ופיד קריאות פתוחות (`docs/product-spec.md` סעיף 4.3) עם סינון רדיוס אמיתי דרך PostGIS. **תסתכל קודם על** `design/screens/pro-1.1-landing.png`, `pro-1.3-signup-verification.png`, `pro-1.4-onboarding.png`, `pro-2.2-job-feed.png` ו-`pro-5.2-availability-settings.png` (ראה `design/README.md`). בנה גם מסך אישור אדמין מינימלי כדי שאפשר יהיה לבדוק את כל הזרימה מקצה לקצה בלי לחכות ל-Phase 7.

---

## Phase 4 — הצעות מחיר בזמן אמת (הליבה של המוצר)

**מטרה:** בעל מקצוע מגיש הצעה, הלקוח רואה הצעות מגיעות בזמן אמת ובוחר אחת.

**מה נכלל:** מסך הגשת הצעה (`product-spec.md` 4.4, כולל תוקף 45 דקות וחישוב עמלה), מסך קבלת הצעות אצל הלקוח עם Realtime subscription (`architecture.md` סעיף 5), בחירת הצעה (שינוי סטטוס קריאה ל-`assigned`), צ'אט בסיסי בין הצדדים.

**הגדרת סיום:**
- [x] הצעה חדשה מופיעה אצל הלקוח **ללא רענון דף** (Realtime עובד בפועל) — נבדק מקצה לקצה ב-Chrome headless מול build של פרודקשן: מסך ההצעות של הלקוח פתוח, בעל מקצוע רביעי מגיש הצעה מתהליך נפרד דרך PostgREST תחת RLS, והמחיר החדש וגם המונה ("4 הצעות התקבלו") מופיעים על אותו `location.href` בלי ניווט
- [x] הצעה פגת תוקף אחרי 45 דקות (נבדק, ולו ידנית עם ערך זמן קצר יותר בסביבת פיתוח) — נבדק בשלוש שכבות: pgTAP מוכיח ש-`select_bid()` דוחה הצעה שפגה **בזמן שהשורה עדיין אומרת `pending`** (השעון נקרא מחדש, לא סומכים על ה-sweep), ש-`expire_stale_bids()` מסדרת את השורה, ושהמסך מדווח `expired`; ובדפדפן — הצעה שנדחפה אל מעבר לתוקפה עוברת מטאב "ממתינות" לטאב "נדחו / פגו" עם "פג תוקף — לא נענה"
- [x] בחירת הצעה נועלת את שאר ההצעות ומעדכנת סטטוס קריאה — `select_bid()` בודקת בעלות, שהקריאה עדיין פתוחה ושההצעה לא פגה, ואז דוחה כל הצעה יריבה ומשבצת את הקריאה **בהצהרה אחת**. ללקוח אין grant על `jobs.selected_bid_id` בכלל, ו-pgTAP מוכיח גם את זה, גם את "בחירה שנייה נדחית", וגם ש"לקוח אחר לא יכול לבחור על קריאה שאינה שלו"

**נבדק בנוסף (הגדרת הסיום הכללית, `CLAUDE.md` סעיף 7):**
- [x] `npm run db:reset` נקי, `npm run db:test` — 104 בדיקות pgTAP (37 מהן חדשות ל-Phase 4)
- [x] `npm run test` — 80 בדיקות Vitest (35 חדשות: סכמות ההצעה וההודעה, חישוב העמלה, והמיון והתגיות של מסך ההשוואה)
- [x] `npm run lint` / `npm run typecheck` / `npm run build` עוברים
- [x] `npm run db:types` הורץ מחדש והטיפוסים מקומטים
- [x] כל טבלה שהשתנתה מקבלת מדיניות **וגם** טענה: `messages` (השיחה היא (קריאה, בעל מקצוע)), `bids` (INSERT מצומצם לעמודות, אין grant על `status`), `jobs` (אין grant על `selected_bid_id`)
- [x] RTL: 36 בדיקות מקצה לקצה ב-Chrome headless ב-1280px וב-390px — אפס גלישה אופקית בכל מסכי השלב, ואין ולו utility פיזי אחד (`ml-`/`pr-`/`left-`/`text-right`…) בכל ה-diff
- [x] אין משתני סביבה חדשים, ולכן `.env.example` לא השתנה
- [ ] הצגת המפה במסך ההצעות ("map · 4 pros nearby" שבעיצוב) — **ממתין למשתמש**: הקוד בנוי מאחורי בדיקת קיום מפתח, אבל `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` ריק. במקום המפה מוצג הסבר; המספר עצמו ("נמצאו N בעלי מקצוע ברדיוס X ק״מ") הוא ספירת PostGIS אמיתית ואינו תלוי במפה
- [ ] התראה ללקוח כשמגיעה הצעה חדשה בזמן שהטאב סגור — **ממתין למשתמש**: SMS דורש חשבון Twilio ופרויקט Supabase Cloud מקושר (כמו סעיף ה-OTP ב-Phase 1), והתראות דפדפן הן החלטה פתוחה ב-`CLAUDE.md` סעיף 9. בזמן שהטאב פתוח, Realtime עושה את העבודה

**החלטות שנפלו בשלב הזה** (הפירוט ב-`docs/architecture.md`):
- **מה שנוגע בכסף איננו grant.** ההצעה קובעת את המחיר, ולכן: INSERT על `bids` מצומצם לחמש עמודות (`expires_at` ו-`status` נשארים לברירות המחדל), `status` איבד את ה-UPDATE grant, ו-`jobs` איבד `update (status, selected_bid_id)`. שני המעברים החוקיים הם `select_bid()` ו-`expire_stale_bids()`.
- **`can_bid_on_job()` במדיניות ה-INSERT.** מדיניות Phase 1 שאלה רק "אתה מאומת וכותב את עצמך", מה שהתיר הצעה על כל `job_id` — כולל מחוץ לרדיוס. ההגשה מגיעה עכשיו בדיוק עד לאן שהפיד מגיע.
- **תוקף שאינו תלוי ב-sweep.** `expire_stale_bids()` מתוזמנת ב-**pg_cron** (ולא Edge Function — cron במסד הנתונים לא דורש יעד דיפלוי) ונקראת גם מהמסכים; אבל `select_bid()` קוראת את השעון בעצמה וכל פונקציות הקריאה מדווחות הצעה שפגה כ-`expired`. אף חלק במוצר לא תלוי בכך שה-sweep רץ.
- **שיחה היא (קריאה, בעל מקצוע), לא (קריאה).** בלי `messages.pro_id`, בקריאה עם שלוש הצעות כל בעל מקצוע קרא את שתי השיחות של המתחרים שלו. `read_at` נוסף לצידו, וניתן לכתיבה רק לצד שלא שלח.
- **הנתונים שהמסכים צריכים מגיעים בפונקציות `security definer`, לא בהרחבת RLS.** ל-`profiles`/`pro_profiles` אין (ובכוונה) מדיניות קריאה ללקוחות; `bids_for_job` מחזירה בדיוק שם, דירוג, מספר עבודות ותג מאומת. אותו שיקול כמו `job_bid_count` ב-Phase 3.
- **Realtime בדפדפן חייב `setAuth()` לפני `subscribe()`.** האפליקציה משחזרת סשן מ-cookie, ולכן שום אירוע אימות לא דוחף את הטוקן לסוקט וה-join מקדים אותו. סוקט לא מאומת לא "רואה פחות" — הוא נדחה עם `invalid column for filter job_id`, כי ל-`anon` אין הרשאה על הטבלה ולכן הוא לא רואה את העמודה. זה היה הבאג האמיתי היחיד בשלב, ומצאנו אותו רק בבדיקת דפדפן.
- **ה-subscription לא מעביר נתונים לדף** — הוא רק מסמן שהתשובה של השרת התיישנה, והשרת מרנדר מחדש תחת ה-RLS של הקורא.

**שלוש סטיות מודעות מהעיצוב:**
- "הלקוח בחר אחר (280 ₪)" מוצג עם **המחיר בלבד, בלי הזהות** שמאחוריו — אותה עסקה ש-`job_bid_count` עושה כשהיא מספרת שיש תחרות בלי לנקוב בשמה.
- "טווח מחירים לקריאות דומות באזור" מחושב מהצעות אמיתיות בתחום וברדיוס 15 ק״מ ב-90 הימים האחרונים, ו**שותק** מתחת לשלוש דגימות במקום לצטט "טווח" שנגזר מהצעה אחת. אותה עמדה שננקטה בשלבים 2 ו-3 מול המדדים המומצאים של הפרוטוטייפ.
- שורת "תמיכת Handy" ברשימת השיחות של `pro-5.3` הושמטה: אין תיבת תמיכה במודל הנתונים, והמצאת אחת היא פיצ׳ר של Phase 8 שנבנה מוקדם.

**הערה על הבדיקה בדפדפן:** הבדיקות רצו מול `next start` (build של פרודקשן) ולא מול `next dev`. ב-Chrome headless בסביבה הזו ה-HMR websocket של Turbopack נכשל, ואיתו נופלת ההידרציה כולה — כלומר `next dev` נראה כאילו הכל עובד (טפסים עוברים כ-POST רגיל, בזכות progressive enhancement) בזמן ששום קוד צד-לקוח לא רץ. זו הסיבה שבדיקת Realtime חייבת לרוץ מול build.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 4: מערכת ההצעות בזמן אמת, לפי `docs/product-spec.md` סעיפים 3.3 ו-4.4, ו-`docs/architecture.md` סעיף 5 למנגנון ה-Realtime. **תסתכל קודם על** `design/screens/customer-2.2-compare-bids.png`, `pro-2.3-submit-bid.png`, `pro-2.4-my-bids.png` ו-`pro-5.3-messages.png` (ראה `design/README.md`). כלול את תפוגת ההצעה אחרי 45 דקות ואת חסימת שאר ההצעות עם בחירה.

---

## Phase 5 — מעקב חי ועדכון מחיר בשטח (הכלל המרכזי)

**מטרה:** מימוש מלא של חוק השקיפות שמגדיר את המוצר — הליבה העסקית האמיתית.

**מה נכלל:** שידור מיקום חי מבעל המקצוע (`architecture.md` סעיף 5), מסך מעקב חי אצל הלקוח, זרימת עדכון מחיר בשטח המלאה בשני הצדדים (`product-spec.md` סעיפים 3.5 ו-4.5) — **כולל האכיפה שמחיר לא משתנה בלי `price_updates` מאושר**, לא רק ב-UI אלא ברמת השאילתות/RLS.

**הגדרת סיום:**
- [x] ניסיון לשנות מחיר ישירות בלי דרך `price_updates` נכשל ברמת ה-DB (נבדק בפועל, לא רק "לא חשוף ב-UI") — ל-`jobs` אין עמודת מחיר בכלל, ה-INSERT וה-UPDATE על `price_updates` נשללו משני הצדדים, ושורה שהוכרעה קפואה גם מול קורא שעוקף RLS לגמרי (טריגר, לא מדיניות). מוכח ב-`supabase/tests/rls_test.sql` סעיף 12
- [x] אם הלקוח לא מאשר — הקריאה ממשיכה במחיר המקורי, ונבדק את זה — ב-pgTAP (`decide_price_update(..., false)` ואז `job_effective_price()` עדיין 390) וגם מקצה לקצה מול האפליקציה הרצה: אחרי דחייה שני המסכים מציגים את המחיר המקורי, ובקשה שהוכרעה לא ניתנת להכרעה שנייה
- [x] מעקב מיקום חי עובד עם עדכון תדיר סביר (לא מכביד על הביצועים) — `job_locations`, שורה אחת לכל קריאה בלי היסטוריה, upsert כל 15 שניות רק כשהטאב פתוח, הקריאה פעילה ובעל המקצוע הדליק את המתג בעצמו
- [x] `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test`, `npm run db:reset`, `npm run db:test` — כולם עוברים (149 טענות pgTAP, 112 טסטי Vitest)
- [x] RTL: כל הקוד החדש בלוגיות בלבד (`ms`/`me`/`ps`/`pe`/`start`/`end`) — נסרק על ה-diff
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**: הזרימה נבדקה אוטומטית מול `next dev` (התחברות OTP אמיתית, 25 טענות על ה-HTML המרונדר של שני המסכים, כולל 404 ללקוח זר ולבעל מקצוע שאינו המשובץ ו-307 למבקר אנונימי), אבל שיפוט חזותי מול `design/screens/` הוא של אדם
- [ ] המפה עצמה מול Google Maps — **ממתין למשתמש**: `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` ריק. `LiveMap` בנוי מאחורי בדיקת מפתח ונופל למצייני המיקום המקווקווים של העיצוב; המרחק, ה-ETA וזמן העדכון האחרון מחושבים מנתונים אמיתיים בשני המצבים

**מה נבנה בפועל:** מסך המעקב של הלקוח (`/requests/[jobId]/track`), מסך ניהול העבודה של בעל המקצוע (`/pro/jobs/[jobId]`) ו"העבודות שלי" (`/pro/my-jobs`, לשונית "פעילות" בלבד). לשונית ההיסטוריה, הקבלות וכפתור "סיימתי — עדכן גבייה" שבעיצוב שייכים ל-Phase 6 ולא נבנו — פס ההתקדמות מצייר את השלב "הושלם" בלי כפתור, כי כפתור שעובד חצי גרוע מהשורה הכנה שמתחתיו.

**החלטה שתועדה:** מיקום חי נבנה כטבלה (`job_locations`) ולא כ-broadcast channel כפי ש-`architecture.md` סעיף 5 שרטט. הנימוק המלא ב-`docs/architecture.md`, "מה נוסף ב-Phase 5", ותקציר ב-`CLAUDE.md` סעיף 3.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 5: מעקב חי (`docs/architecture.md` סעיף 5) וזרימת עדכון מחיר בשטח (`docs/product-spec.md` 3.5 ו-4.5). **תסתכל קודם על** `design/screens/customer-3.1-tracking-chat.png` ו-`pro-3.1-manage-job-price-update.png` (ראה `design/README.md`) — שני הצדדים של אותה זרימה. זהו הכלל העסקי המרכזי של המוצר — ודא שהאכיפה קיימת ברמת ה-DB/RLS ולא רק ב-UI, ובנה טסט (יחידה או E2E) שמוכיח שאי אפשר לעקוף אותו.

---

## Phase 6 — סיום עבודה, תשלום, עמלה וקבלה

**מטרה:** סגירת המעגל הכלכלי של המוצר.

**מה נכלל:** מסך סיום עבודה + דירוג (`product-spec.md` 3.6), רישום אמצעי תשלום שנבחר, יצירת `commission_charges` (חישוב 12% אוטומטי), הפקת קבלה כ-PDF (יש להחליט על הספרייה — ראה `CLAUDE.md` סעיף 9), מסך הכנסות לבעל מקצוע (`product-spec.md` 4.6).

**הגדרת סיום:**
- [x] סיום עבודה יוצר רשומת עמלה עם חישוב נכון (בדוק גם עם ובלי `price_updates` מאושרים) — `complete_job()` כותבת את השורה באותה הצהרה שסוגרת את הקריאה, וה-12% מחושב במסד הנתונים מ-`job_effective_price()`. שני המקרים מוכחים ב-`supabase/tests/rls_test.sql` סעיף 13: קריאה עם עדכון מאושר נסגרת על 520 (עמלה 62.40) וקריאה עם בקשה שלא נענתה נסגרת על 320 שסוכמו (עמלה 38.40), כשהבקשה הפתוחה מוכרעת כ"לא אושרה" בדרך החוצה
- [x] קבלת PDF ניתנת להורדה ומכילה פירוט נכון (מחיר בסיס + תוספות) — `/api/receipts/[jobId]`, ‏`@react-pdf/renderer` עם Heebo מוטמע. השורות נבנות מרשומות ה-`price_updates` המאושרות עצמן (380 בסיס + 140 מאושר = 520), והקבלה של בעל המקצוע נושאת גם את העמלה והנטו — של הלקוח לא, כי ה-12% אינו עניינו. אומת ברינדור הקובץ ובהסתכלות עליו, לא רק בקריאת הקוד
- [x] מסך הכנסות בעל מקצוע מציג נתונים אמיתיים מה-DB, לא מדומים — `/pro/wallet` קורא `my_earnings_stats()` ו-`my_completed_jobs()`, והדירוג מחושב מ-`reviews`. מדד ה"ביטולים" שבעיצוב לא צויר: אין בקוד שום מעבר ל-`cancelled`, ואפס קבוע שמוצג כמדד הוא בדיוק מה שהריפו הזה נמנע ממנו מ-Phase 2
- [x] `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test`, `npm run db:reset`, `npm run db:test` — כולם עוברים (196 טענות pgTAP, 133 טסטי Vitest)
- [x] RTL: כל הקוד החדש בלוגיות בלבד (`ms`/`me`/`ps`/`pe`/`start`/`end`) — נסרק על ה-diff. היוצא מן הכלל היחיד הוא `lib/pdf/`, שבו לגיליון הסגנונות של react-pdf אין תכונות לוגיות ולעמוד PDF אין `dir`; שם הערכים סימטריים ו-`textAlign` מפורש
- [x] הזרימה נבדקה אוטומטית מול האפליקציה הרצה — 48 טענות על ה-HTML המרונדר ועל קובצי ה-PDF, גם מול `next dev` וגם מול `next build && next start` (מה שמאמת את `serverExternalPackages` ואת מעקב הקבצים אחרי הפונט): מסך הסיכום, שתי הקבלות, הארנק בשני טווחים, שתי לשוניות "העבודות שלי" וטופס הסגירה — לצד 404 ללקוח זר ולבעל מקצוע שאינו המשובץ, 307 לתפקיד הלא נכון ולמבקר אנונימי, ו-401 על בקשת קבלה בלי סשן
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**: הזרימה נבדקה אוטומטית כאמור, אבל שיפוט חזותי מול `design/screens/customer-4.1-summary-receipt-rating.png`, `pro-3.2-my-jobs.png` ו-`pro-4.1-earnings-wallet.png` הוא של אדם
- [ ] גביית העמלה בפועל — **ממתין למשתמש**: `commission_charges` הוא פנקס בלי סליקה מאחוריו. חיוב אמיתי של בעל המקצוע הוא תנועת כסף, ולפי `CLAUDE.md` סעיף 8 הוא הכרעה של המשתמש — יחד עם השאלה הפתוחה איך (ואם) נשמר מספר חשבון בנק מלא

**מה נבנה בפועל:** מסך הסיכום, הקבלה והדירוג של הלקוח (`/requests/[jobId]/summary`), כרטיס "סיימתי — עדכן גבייה" במסך ניהול העבודה, לשונית ההיסטוריה ב-"העבודות שלי", מסך הארנק (`/pro/wallet`), ו-`/api/receipts/[jobId]` — ה-Route Handler היחיד באפליקציה. "שמור לפעם הבאה" ממלא סוף-סוף את פאנל "בעלי המקצוע שלי" שהאזור האישי הבטיח מאז Phase 2.

**החלטות שתועדו:**

1. **ספריית ה-PDF: `@react-pdf/renderer`** (`CLAUDE.md` סעיף 2, `architecture.md` סעיף 1). קבלה בעברית היא בעיית bidi ולא בעיית פונט, וכותב PDF קל יותר היה מחייב היפוך ידני של רצפים עבריים. פונט Heebo מאוחסן בריפו כ-TTF ב-`assets/fonts/`, כי `next/font/google` מייצר WOFF2 שמנוע ה-PDF לא קורא.
2. **בעל המקצוע מצהיר איך שולם, הלקוח רואה מה נרשם.** הכפתור בעיצוב הוא "סיימתי — **עדכן גבייה**", ומי שקיבל את הכסף לידיים הוא בעל המקצוע; Handy אינה צד לתשלום (חוק עסקי 4). ארבעת השבבים במסך של הלקוח מסמנים את מה שנרשם ואינם טופס.
3. **סגירת עבודה מכריעה בקשת מחיר שנשארה פתוחה, כ"לא אושרה".** לא כדי לשנות מספר — `job_effective_price()` מעולם לא ספרה בקשה ממתינה — אלא כדי שלא תיוותר שאלה פתוחה על עבודה שהסתיימה, ושבעל מקצוע לא ייתקע בהמתנה לתשובה שלא תגיע.
4. **`transfer` → `bank_transfer`.** Phase 1 ו-Phase 3 אייתו אותו אמצעי תשלום בשתי דרכים; זה השלב שהציב את שתיהן על אותו מסך. האיות שמגיע לקבלה ניצח.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 6: סיום עבודה, חישוב עמלה, והפקת קבלה (`docs/product-spec.md` 3.6 ו-4.6). **תסתכל קודם על** `design/screens/customer-4.1-summary-receipt-rating.png`, `pro-3.2-my-jobs.png` ו-`pro-4.1-earnings-wallet.png` (ראה `design/README.md`). לפני שמתחילים, תעדכן את `docs/architecture.md`/`CLAUDE.md` עם ההחלטה על ספריית ה-PDF שנבחרה, ואז תמשיך למימוש.

---

## Phase 7 — לוח ניהול (Admin)

**מטרה:** יכולת פיקוח ואכיפה בסיסית למפעילי הפלטפורמה.

**מה נכלל:** כל הזרימות מ-`product-spec.md` סעיף 5 — סקירה כללית, אישור בעלי מקצוע (גרסה מלאה, לא הזמנית מ-Phase 3), ניהול קריאות, מחלוקות ובקרה, מדדי אמון.

**הגדרת סיום:**
- [x] רק משתמש עם `role = admin` יכול לגשת לנתיבי `/(admin)` (נבדק RLS + middleware) — בשלוש שכבות, וכל אחת נבדקה בנפרד: `proxy.ts` מחזיר 307 למבקר אנונימי, `requireRole("admin")` שולח לקוח ל-`/account` ובעל מקצוע ל-`/pro/dashboard`, ומתחת לשתיהן כל פונקציית קריאה של הלוח בודקת `is_admin()` בעצמה ומחזירה 42501 לכל אחד אחר. הקריאות המצטברות של הלוח הן צבירות — "כמה קריאות היום" אינה שורה ששייכת למישהו — ולכן אי אפשר לבטא אותן כמדיניות שורה, וזו הסיבה שיש להן שער משלהן. 16 טענות ב-`supabase/tests/rls_test.sql` סעיף 14 ו-16 טענות מול האפליקציה הרצה
- [x] אישור/דחיית בעל מקצוע מהלוח משנה בפועל את הסטטוס במסד הנתונים — `set_pro_verification()` מ-Phase 3, ללא שינוי: זו הייתה הפונקציה הנכונה מלכתחילה, כי היא בודקת `is_admin()` בתוך המסד ולא מסתמכת על הרשאת עמודה. אומת מקצה לקצה מול האפליקציה הרצה: הלוח מאשר, התור מתרוקן, ואותה קריאה בדיוק מהטוקן של לקוח נדחית עם `admin only`
- [x] מחלוקת נפתחת עם קישור מלא לתיעוד הקריאה (הצעה, תמונות, אישורי מחיר) — `/admin/jobs/[jobId]`: הקריאה עצמה והמדיה שלה, **כל** ההצעות (לא רק שנבחרה), כל עדכון מחיר עם התמונה שצורפה לו ועם ההכרעה שלו, ההתכתבות מול כל בעל מקצוע בנפרד, סיכום החיוב והקבלה. הכול נקרא כשורות תחת ה-RLS של המנהל, דרך אותם מודולים שהלקוח ובעל המקצוע קוראים בהם — אין בריפו הזה שום הטלה (projection) של קריאה שקיימת רק למנהל
- [x] `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test`, `npm run db:reset`, `npm run db:test` — כולם עוברים (244 טענות pgTAP, 158 טסטי Vitest)
- [x] RTL: כל הקוד החדש בלוגיות בלבד (`ms`/`me`/`ps`/`pe`/`start`/`end`/`text-start`) — נסרק על ה-diff
- [x] הזרימה נבדקה אוטומטית מול האפליקציה הרצה — 59 טענות, גם מול `next dev` וגם מול `next build && next start`: ארבעת המסכים, טבלת הקריאות על שלושת הפילטרים והחיפוש, תיעוד הקריאה, יצוא ה-CSV, ושתי דרכי הכתיבה (אישור בעל מקצוע והכרעת מחלוקת) — לצד 307 לכל תפקיד שאינו מנהל בכל אחד מארבעת הנתיבים, 401 על יצוא בלי סשן, 403 על יצוא של לקוח, ו-`admin only` על קריאה ישירה ל-RPC משני התפקידים האחרים
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**: הזרימה נבדקה אוטומטית כאמור, אבל שיפוט חזותי מול ארבעת הקבצים ב-`design/screens/admin-*.png` הוא של אדם
- [ ] תמונת התקלה בתוך תיעוד הקריאה — **ממתין למשתמש**: ה-seed רושם את נתיב הקובץ על שורת `price_updates` אבל אינו יכול להעלות בתים מ-SQL, ולכן בהתקנה מקומית טרייה המסך אומר "התמונה רשומה על הבקשה אך הקובץ אינו זמין" במקום לקשר לקובץ ש-404. עדכון מחיר שנוצר דרך הדפדפן מעלה קובץ אמיתי ומקבל קישור חתום

**מה נבנה בפועל:** ארבעת מסכי הקונסולה (`/admin`, `/admin/pros`, `/admin/jobs`, `/admin/disputes`), מסך תיעוד הקריאה (`/admin/jobs/[jobId]`) שהוא הלב של סעיף 5.4, יצוא CSV ב-`/api/admin/report`, וה"דלת" היחידה משני צדי העסקה אל הקונסולה — פתיחת מחלוקת ממסך הסיכום של הלקוח ומהיסטוריית העבודות של בעל המקצוע.

**החלטות שתועדו:**

1. **קריאה של מנהל היא צבירה, ולכן פונקציה — תיעוד של קריאה הוא שורות, ולכן RLS.** שתי חצאי ההחלטה הן אותה החלטה: מדיניות RLS בוחרת שורות, ולכן "כמה קריאות נפתחו היום" לא ניתנת לביטוי כמדיניות ואילו "מי רשאי לקרוא את הקריאה הזו" כן. כל פונקציית צבירה שואלת `is_admin()` בדלת שלה; מסך התיעוד לא מוסיף שום פונקציה, כי `jobs`, `bids`, `price_updates`, `messages`, `commission_charges` ו-`reviews` נושאות מדיניות "admin reads all" מאז השלב שיצר אותן.
2. **חור בהרשאת ה-INSERT של `disputes` מ-Phase 1, שנסגר כאן.** Phase 1 נתן `grant select, insert on public.disputes` — כלומר על כל העמודות — כך שמתלונן היה יכול לפתוח תיק שכבר מסומן `resolved` עם `credit_amount` לבחירתו. שלוש עמודות מוענקות עכשיו: `job_id`, `opened_by`, `reason`.
3. **ארבעת כלי האכיפה של סעיף 5.4 אמיתיים ולא כיתוב.** השעיה היא `set_pro_verification()` מ-Phase 3; חסימת עדכוני מחיר נבדקת **בתוך** `request_price_update()`, הפונקציה היחידה שיכולה לכתוב לטבלה; דרישת מסמכים מחודשת מחזירה את בעל המקצוע ל-`pending`, שזה בדיוק מה ש-`is_verified_pro()` עונה עליו "לא" — ולכן היא באמת עוצרת עבודה חדשה; והזיכוי ללקוח נכתב באותה הצהרה שסוגרת את המחלוקת.
4. **`price_updates_blocked` ו-`documents_required_at` הן עמודות בלי שום grant ללקוח.** אותו טיפול ש-`verification_status` מקבל מ-Phase 1: בעל מקצוע שיכול לנקות את החסימה של עצמו אינו אכיפה.
5. **עיר נגזרת מ-`address_text` ואינה עמודה.** `job_city()` לוקחת את המקטע האחרון בכתובת. עמודה הייתה דבר שני שיכול לסתור את מה שהלקוח הקליד — אותו נימוק בדיוק שבגללו לקריאה אין עמודת מחיר.
6. **טוקן עיצוב משלו לקונסולה (`--color-admin`, ענבר).** נדגם מארבעת ה-PNG-ים כמו כל טוקן אחר. הוא במכוון לא הכחול של הלקוח ולא האינדיגו של בעל המקצוע, כדי שיהיה ברור במבט אחד לאיזה צד של השוק שייך המסך.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 7: לוח הניהול המלא, לפי `docs/product-spec.md` סעיף 5. **תסתכל קודם על** כל 4 הקבצים תחת `design/screens/admin-*.png` (ראה `design/README.md`). ודא הרשאות admin-only מוקשחות בכל שכבה (RLS + middleware, לא רק הסתרת UI).

---

## Phase 8 — עמודי תוכן ו-SEO

**מטרה:** נראות אורגנית ואמון — עמודים ציבוריים שלא דורשים התחברות.

**מה נכלל:** עמודי קטגוריה+עיר, מדריך עלויות, מרכז עזרה/תמיכה, מדריכי תחזוקה, פרופיל ציבורי של בעל מקצוע (`product-spec.md` סעיף 3.8).

**הגדרת סיום:**
- [x] עמודים ציבוריים נגישים ללא התחברות, עם מטא-תגיות SEO בסיסיות — 14 כתובות ציבוריות (`/how-it-works`, `/pricing`, `/help`, `/contact`, `/terms`, `/privacy`, `/cancellation`, `/guides` + מאמר, `/services` + תחום + תחום×עיר, `/pro/[slug]`), כל אחת עם `title`, `description`, `canonical` ו-Open Graph מ-`lib/seo.tsx`, ובנוסף `robots.txt`, `sitemap.xml` (180 עמודי תחום×עיר, המדריכים, וכל בעל מקצוע מאומת) ו-JSON-LD: `FAQPage`, `BreadcrumbList`, `Service`, `Article`, `LocalBusiness` עם `AggregateRating`. נבדק אוטומטית מול האפליקציה הרצה
- [x] פרופיל ציבורי מציג נתונים אמיתיים (דירוג, ביקורות, מסמכים מאומתים — לא הקבצים עצמם) — `pro_public_profile()` ו-`pro_public_reviews()`, שתי פונקציות `security definer` שמונות את העמודות שהן מחזירות. הדירוג מחושב מ-`reviews`, הביקורות קשורות לעבודות שנסגרו, ו"מסמכים שאומתו" הם שלושה בוליאנים — אילו **סוגי** מסמכים אושרו, לעולם לא הקובץ. נבדק אוטומטית שהעמוד לא מכיל את חשבון הבנק, את ארבע הספרות, את הטלפון ואת ה-`service_point`
- [x] `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test`, `npm run db:reset`, `npm run db:test` — כולם עוברים (282 טענות pgTAP, 206 טסטי Vitest)
- [x] לכל טבלה חדשה יש RLS וטענות ב-`supabase/tests/rls_test.sql` — `support_tickets` (הטבלה החדשה היחידה) ואיתה ה-bucket הציבורי, ה-slug, ומענה לביקורות: 38 טענות חדשות בסעיף 15
- [x] `npm run db:types` הורץ מחדש והטיפוסים מקומטים
- [x] RTL: כל הקוד החדש בלוגיות בלבד (`ms`/`me`/`ps`/`pe`/`start`/`end`/`text-start`) — נסרק על ה-diff
- [x] הזרימה נבדקה אוטומטית מול האפליקציה הרצה — 137 טענות, גם מול `next dev` וגם מול `next build && next start`: 14 העמודים הציבוריים ללא סשן, ה-JSON-LD, ה-404 לבעל מקצוע לא מאומת / slug לא קיים / תחום ועיר לא קיימים, `robots.txt` ו-`sitemap.xml`, מסך עריכת הפרופיל ומרכז העזרה של בעל המקצוע, ושלוש דרכי הכתיבה (עריכת פרופיל, מענה לביקורת, פנייה לתמיכה) — לצד סירוב ל-slug שמור, ל-slug תפוס, לכתיבת `verification_status` על ידי בעל המקצוע, למענה של בעל מקצוע אחר על ביקורת, לפנייה אנונימית בשם משתמש רשום, ולקריאת פניות של מישהו אחר
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**: הזרימה נבדקה אוטומטית כאמור, אבל שיפוט חזותי מול שבעת הקבצים ב-`design/screens/content-*.png` ומול `customer-5.2`, `customer-5.3`, `pro-5.1` ו-`pro-5.5` הוא של אדם
- [ ] תמונת פרופיל וגלריית עבודות בעמוד הציבורי — **ממתין למשתמש**: ה-seed לא יכול להעלות בתים מ-SQL, ולכן בהתקנה מקומית טרייה הפרופיל מציג את ראשי התיבות במקום תמונה. תמונה שמועלית דרך `/pro/profile` נכנסת ל-bucket הציבורי ומוצגת מיד
- [ ] בדיקה מול Google Search Console (אינדוקס בפועל, מבנה ה-sitemap) — **ממתין למשתמש**: דורשת דומיין חי ובעלות מאומתת עליו. `NEXT_PUBLIC_SITE_URL` הוא המשתנה היחיד שצריך להיקבע כדי שכל ה-canonical וה-sitemap יצביעו על הדומיין האמיתי
- [ ] בדיקה משפטית של `/terms`, `/privacy` ו-`/cancellation` — **ממתין למשתמש**: `CLAUDE.md` סעיף 8 מחזיק טקסט משפטי אצל המשתמש. מה שנכתב הוא תיאור בשפה פשוטה של מה שהקוד באמת עושה, סעיף אחרי סעיף, כדי שלבדיקה יהיה מה לתקן; כל אחד משלושת העמודים אומר על עצמו שהוא טיוטה (`DRAFT_NOTICE` ב-`lib/content/legal.ts`), והשורה הזו יורדת באותו commit שבו הבדיקה נוחתת

**מה נבנה בפועל:** שבעת מסכי התוכן (`content-6.1` עד `6.7`), עמודי תחום ותחום×עיר, הפרופיל הציבורי של בעל המקצוע ב-`/pro/[slug]`, ובצד בעל המקצוע — מסך עריכת הפרופיל הציבורי (`/pro/profile`, כולל גלריה ומענה לביקורות) ומרכז העזרה (`/pro/help`). בנוסף: `robots.ts`, `sitemap.ts`, ה-404 של האפליקציה, פוטר התוכן בכל מסך ציבורי, ושלושת הקישורים שהעיצוב תמיד הראה בכותרת (איך זה עובד / מחירים / עזרה) ושהושמטו עד עכשיו כי לא היה לאן להוביל.

**החלטות שתועדו:**

1. **עמוד ציבורי הוא פונקציה, לא מדיניות שהורחבה.** Phase 1 כתבה את זה מראש, והיא צדקה: RLS בוחרת שורות ולא עמודות, ו-`pro_profiles` מחזיקה חשבון בנק, טלפון ו-`service_point` ליד ה-bio. חמש פונקציות `security definer` מונות את העמודות שהן מחזירות ומוענקות ל-`anon`; `pro_profiles` לא קיבלה שום מדיניות `select` חדשה.
2. **ה-slug של בעל המקצוע הוא grant עמודה, כי הוא ההגדרה שלו את עצמו** — אותה משפחה של `bio`, לא של `verification_status`. מה שחייב להתקיים הוא הצורה, ולכן הכללים הם check constraint ואינדקס ייחודי: slug לעולם לא יתנגש באחד מהנתיבים שהאפליקציה עצמה מגישה תחת `/pro/`.
3. **`pro-media` הוא ה-bucket הציבורי הראשון בפרויקט**, וזו ההכרעה הפתוחה מ-`CLAUDE.md` סעיף 9. חתימת URL רצה תחת ה-RLS של קורא, ולעמוד ציבורי אין קורא. מה שיושב שם הוא רק מה שבעל המקצוע פרסם בכוונה; מסמכי האימות נשארים ב-bucket הפרטי.
4. **אין מספר מומצא בעמוד ציבורי.** "97% אחריות על העבודה" מהעיצוב ועמודת "זמן ביצוע" בטבלת המחירים פשוט חסרים — המוצר לא מודד אף אחד מהם. מדריך העלויות הוא לפי **תחום** ולא לפי משימה, כי קריאה היא טקסט חופשי בתוך תחום, וכל מספר בו מגיע מ-`commission_charges`.
5. **`support_tickets` היא הטבלה היחידה ש-`anon` כותב אליה, ואף אחד לא מעדכן.** טופס תמיכה מאחורי התחברות אינו תמיכה. הצרופות שבעיצוב הושמטו: `product-spec.md` 3.8 מבקש טופס פנייה, ולא bucket רביעי עם מדיניות כתיבה אנונימית.
6. **תוכן עריכתי (ערים, קופי לתחומים, שאלות נפוצות, מדריכים, טקסט משפטי) יושב ב-`lib/content/` ולא בטבלה.** תשובה בשאלות הנפוצות מנסחת מחדש כלל שחי במיגרציה, והשניים צריכים לזוז באותו commit.
7. **תקנון, פרטיות וביטולים הם שלושה נתיבים ולא לשונית אחת.** העיצוב מציג את שלושתם מאחורי `/terms` עם מחליף; מסמך שאין לו כתובת משלו הוא מסמך שאי אפשר לצטט.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 8: עמודי התוכן והשיווק הציבוריים, לפי `docs/product-spec.md` סעיף 3.8. **תסתכל קודם על** כל הקבצים תחת `design/screens/content-*.png`, וגם `customer-5.2-pro-public-profile.png`, `customer-5.3-category-page.png`, `pro-5.1-public-profile-edit.png` ו-`pro-5.5-help-center.png` (ראה `design/README.md`). שים לב ל-SEO בסיסי (meta tags, מבנה URL נקי לפי תחום+עיר).

---

## Phase 9 — חיזוק, בדיקות, והכנה להשקה

**מטרה:** לוודא שהמערכת שנבנתה ב-9 השלבים הקודמים באמת עמידה, לא רק "עובדת פעם אחת אצלי".

**מה נכלל:** כיסוי E2E ב-Playwright לזרימות הקריטיות (פרסום קריאה → הצעה → בחירה → עדכון מחיר → סיום → תשלום), בדיקת עומס בסיסית על שאילתות ה-PostGIS, בדיקת נגישות (a11y) בסיסית, בדיקת RTL מקיפה, מדיניות גיבוי ל-Supabase, checklist אבטחה (RLS על כל טבלה, אין secrets בקוד, rate limiting על OTP).

**הגדרת סיום:**
- [x] כל הזרימות הקריטיות מכוסות ב-E2E ועוברות ב-CI — 139 בדיקות Playwright (`npm run test:e2e`): הזרימה המלאה מפרסום קריאה ועד קבלה, סירוב לעדכון מחיר שלא משנה כלום, axe על כל מסך, ו-RTL בשני ה-viewports. ג'וב `e2e` ב-`.github/workflows/ci.yml` מריץ אותן על כל push ו-PR. **הריצה הירוקה נמדדה מקומית; התוצאה ב-GitHub Actions עצמה עדיין לא נצפתה** (ראו הערה למטה)
- [x] checklist אבטחה עבר בשלמותו — לא כמסמך אלא כקוד שנכשל כשהכלל נשבר: RLS על כל טבלה ב-pgTAP (`npm run db:test`), אין סודות בקוד ב-`tests/secrets.test.ts`, כלל ה-RTL ב-`tests/rtl.test.ts`, ו-rate limit על OTP ב-`supabase/config.toml` (`[auth.rate_limit]`, `max_frequency`). הסריקה מצאה ותיקנה בפועל ארבע הרשאות INSERT שלקוח יכול היה לזייף דרכן ערך — ראו `fix(security): narrow four INSERT grants a client could forge a value through`
- [x] בדיקת עומס על שאילתות PostGIS — `npm run perf:postgis`. שני האינדקסים המרחביים תקינים (8.9ms ו-5.1ms על 10,000 קריאות). **פיד בעלי המקצוע לינארי ולא משתמש באינדקס** (1265ms על 50,000 קריאות) — תועד ולא תוקן, כי מדיניות ה-SELECT על `jobs` היא כלל האבטחה הנושא של המוצר
- [ ] יש תוכנית גיבוי/שחזור למסד הנתונים — **נדחה בהחלטת המשתמש (3.9.2026)**: אין פרויקט Supabase Cloud ואין משתמשים אמיתיים, כך שאין היום נתונים שאפשר לאבד. הפריט נפתח מחדש ביום שעולים לענן, לפני המשתמש האמיתי הראשון. הפער שחייב להיפתר אז — ואף מסלול לא פותר אותו — רשום ב-`CLAUDE.md` סעיף 9, "גיבוי ה-Storage"

**הערה על אימות ה-CI:** ג'וב ה-e2e נוסף ונבדק תחבירית, והחבילה עוברת מקומית במלואה. אף אחד עדיין לא ראה אותו ירוק ב-GitHub Actions, כי `gh` לא מחובר במכונה הזו. הריצה הראשונה על `main` היא מה שיאשר את זה — כדאי להסתכל.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 9: חיזוק ובדיקות לפני השקה. הרץ/בנה טסטי E2E לזרימה המלאה (Phase 2 עד Phase 6), עבור על checklist האבטחה ב-`CLAUDE.md`, ותן לי דו״ח סיכום של כל מה שנמצא.

---

## Phase 10 — אישור בעל המקצוע ודמי קבלת עבודה קבועים

**מטרה:** שני שינויים במודל העסקי, שנפגשים בנקודה אחת. (1) בחירת הלקוח כבר לא משבצת עבודה — היא **מציעה** אותה, ובעל המקצוע צריך לאשר שהוא לוקח אותה. (2) Handy כבר לא גובה 12% מהעבודה שנסגרה אלא **סכום קבוע ברגע האישור**. הרגע שבו נכנס כסף עובר מסוף הזרימה לתחילתה, ולכן זה שלב אחד ולא שניים.

**ההחלטות שננעלו לפני הבנייה** (המשתמש, 8.9.2026):

| | |
|---|---|
| דמי קבלת עבודה | **35 ₪**, אחיד לכל תחום ולכל גובה הצעה |
| מתי נגבים | ברגע שבעל המקצוע מאשר. לא בסגירת העבודה |
| החזר | אין. בשום מקרה — לא בביטול ולא במחלוקת שהוכרעה לטובת הלקוח |
| חלון אישור | **שעתיים** מרגע שהלקוח בחר |
| הצעות מתחרות | נשארות חיות; הקריאה נשארת בפיד וממשיכה לקבל הצעות חדשות |
| הלקוח בהמתנה | יכול לבטל בחירה או לעבור לבעל מקצוע אחר בכל רגע |
| מע"מ | לא מוזכר, כמו שה-12% לא הוזכר |

**מה נכלל:**

*מסד הנתונים — מיגרציה אחת, `..._pro_acceptance_and_flat_fee.sql`:*
- `jobs.status`: סטטוס חדש `awaiting_pro`, בין `bidding` ל-`assigned`.
- `bids.status`: `selected` משנה משמעות ל"הלקוח בחר, ממתין לבעל המקצוע", ונוספים `accepted` ו-`declined`. `expired` מכסה גם בחירה שלא נענתה בשעתיים.
- `bids.accept_deadline`, ואינדקס ייחודי חלקי: הצעה `selected` אחת לקריאה.
- `select_bid()` — מסמנת `selected`, קובעת מועד, מעבירה ל-`awaiting_pro`. **לא** כותבת `selected_bid_id` ו**לא** דוחה מתחרים. אם כבר יש בחירה, מחזירה אותה ל-`pending` (או `expired` אם 45 הדקות שלה חלפו) ובוחרת מחדש — התחרטות הלקוח היא אותה הצהרה.
- `accept_job(bid_id)` — חדשה. בעל ההצעה בלבד, `is_verified_pro()`, לא חסום, המועד לא פג. כותבת `selected_bid_id`, `status='assigned'`, דוחה את כל המתחרים, ורושמת את החיוב — הצהרה אחת, אידמפוטנטית.
- `decline_job(bid_id)` — חדשה. `declined`, הקריאה חוזרת ל-`bidding`, אין חיוב.
- `withdraw_bid_selection(job_id)` — חדשה. הלקוח בלבד, רק ב-`awaiting_pro`.
- `expire_stale_selections()` — חדשה. sweep ב-pg_cron **ובדיקה חוזרת בכל קריאה ובתוך `accept_job()`**, אותה תבנית כמו `expire_stale_bids()`: שום דבר במוצר לא תלוי בכך שה-sweep רץ.
- `complete_job()` — כבר לא מחשב ולא מוסיף שורת חיוב; **מעדכן** את השורה הקיימת ב-`total_price`, `payment_method` ו-`completed_at`. הכרעת בקשת מחיר תלויה ומונה העבודות — ללא שינוי.
- `commission_rate()` יורדת; `job_acceptance_fee()` נכנסת (`immutable`, מוענקת גם ל-`anon` בשביל דף התמחור).
- `commission_charges` → `job_fees` (`alter table rename`): `commission_amount` → `fee_amount`, `charged_at` = רגע האישור, `total_price`/`payment_method`/`completed_at` הופכים nullable ונכתבים בסגירה. שורות היסטוריות עוברות עם הסכום שבאמת נגבה מהן.
- RLS: `"jobs: verified pro reads open jobs in radius"` ו-`can_bid_on_job()` מקבלות גם `awaiting_pro` — זו ההחלטה "נשארת בפיד". `selected_bid_id is null` נשאר כשומר, ועכשיו הוא מבדיל בין קריאה תפוסה לקריאה שממתינה.

*קוראים שצריכים לדעת ששורת חיוב יכולה להיות "נגבתה אך לא הושלמה":* `job_receipt()`, `my_completed_jobs()`, `my_earnings_stats()`, `admin_overview()` (ההכנסה נמדדת לפי אישורים, לא סגירות), `admin_jobs()`, `pricing_guide()` ו-`category_stats()` — שתי האחרונות חייבות `total_price is not null`, אחרת מדריך העלויות יספור עבודות שטרם נגמרו.

*מסכים:* כרטיס "נבחרת" עם ספירה לאחור ב-`/pro/offers` וב-`/pro/dashboard` (אישור / ויתור); `/pro/jobs/[jobId]` מסרב עד האישור; `/requests/[jobId]/offers` מציג "ממתין לאישור" עם ביטול בחירה, ושאר הכרטיסים נשארים ניתנים לבחירה; `/requests/[jobId]/track` מחזיר להשוואה במצב המתנה; הארנק, "העבודות שלי", לוח הניהול וטופס ההצעה מחליפים "עמלת Handy (12%)" ב"דמי קבלת עבודה — נגבים רק אם תאשר".

*קופי:* כל מקום שכתוב בו 12% — דף הבית, `/pricing`, `/how-it-works`, מרכז העזרה, המדריכים, שלושת המסמכים המשפטיים, דף הנחיתה וההתחברות של בעלי מקצוע, שלבי 4–5 בהרשמה, וה-PDF.

*בדיקות ומסמכים:* סעיף pgTAP חדש לזרימת האישור + עדכון סעיפים 12–14; Vitest ב-`completion`/`bids`/`pros`; `e2e/critical-flow.spec.ts` מקבל את שלב האישור והציפייה ל-62.40 מתחלפת; `product-spec.md` (חוקים 3 ו-6, זרימות 3.3/3.6/4.4), `architecture.md`, `CLAUDE.md` סעיפים 2–4; seed עם שורות חיוב חדשות ועם עבודה אחת שנשארת ב-`awaiting_pro` כדי שיהיה מה להדגים.

**למה `selected_bid_id` נכתב רק באישור:** `is_assigned_pro()` מוגדרת דרכו, ודרכה עוברות מדיניות הצ׳אט, המעקב החי, `request_price_update()`, `complete_job()` והקבלה. אילו נכתב בבחירת הלקוח, בעל מקצוע שעדיין לא אישר היה מקבל את מלוא סמכויות המשובץ. כך אף אחת מהפונקציות האלה לא משתנה.

**שעון אחד מחליף שעון אחר:** ברגע שהצעה עוברת ל-`selected`, 45 הדקות שלה כבר לא רלוונטיות — `expire_stale_bids()` נוגעת רק ב-`pending`, ולכן הבחירה לא תפוג תחת בעל המקצוע באמצע השעתיים. `accept_job()` בודקת `accept_deadline`, לעולם לא `expires_at`.

**הגדרת סיום:**
- [x] בעל מקצוע שאינו הזוכה, בעל מקצוע שהושעה בין הבחירה לאישור, ובחירה שפג מועדה — כולם נדחים **במסד הנתונים**: `accept_job()` בודקת בעלות על ההצעה, `is_verified_pro()` מחדש, ואת `accept_deadline` בעצמה. סעיף 16 ב-`supabase/tests/rls_test.sql` מוכיח את שלושתם, והשלישי מוכח **בלי שה-sweep רץ**
- [x] אישור חוזר (כפתור שנלחץ פעמיים) לא יוצר חיוב שני — `accept_job()` אידמפוטנטית ומחזירה את השורה הקיימת; נבדק בסעיף 11 (שתי קריאות רצופות, ואז ספירה של שורה אחת)
- [x] ויתור והתחרטות הלקוח מחזירים את הקריאה למצב שאפשר לבחור בו שוב, וההצעות המתחרות עדיין חיות — סעיף 16 מוליך קריאה אחת דרך כל המסלול: הוצעה, ויתרו עליה, הוצעה שוב, בוטלה, פגה, ולבסוף נלקחה. גם ב-Playwright, על המסכים עצמם
- [x] אף שורת חיוב לא נוצרת לפני אישור, ואף אחת לא מוחזרת אחריו — שתי הטענות בסעיף 16, האחרונה מול `resolve_dispute()` שמזכה את הלקוח ולא נוגעת ב-`job_fees`
- [x] `npm run db:reset` נקי, `npm run db:types` הורץ מחדש והטיפוסים מקומטים
- [x] אין בקוד ובקופי שום שריד ל-12% — נסרק על כל הריפו; שתי ההופעות שנשארו הן הערת קוד שמתארת את מה שהיה, ו"+12% מאתמול" שהוא דוגמה בכרטיס סטטיסטיקה ואינו עמלה
- [x] `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test` (330), `npm run db:test` (360), `npm run test:e2e` (139) — כולם עוברים
- [x] RTL: כל הקוד החדש בלוגיות בלבד — נסרק על ה-diff ועל ידי `tests/rtl.test.ts`
- [x] הקבלה אומתה ברינדור ובהסתכלות, לא בקריאת קוד — וכך נמצא באג: "נסגר בתאריך" הדפיס את `charged_at`, שהוא מעכשיו היום שבו העבודה **נלקחה**. עכשיו הוא `completed_at`, ולדמים יש שורת תאריך משלהם
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**: הזרימה נבדקה אוטומטית מול `next build && next start` (139 בדיקות Playwright, כולל axe על כרטיס האישור החדש), אבל שיפוט חזותי הוא של אדם
- [x] `docs/product-spec.md`, `docs/architecture.md` ו-`CLAUDE.md` מעודכנים באותו PR

**מה שמסומן בשלב הזה ולא נבנה בו:**
1. **אין push, והמודל החדש תלוי בכך שבעל המקצוע ישים לב.** עד היום הוא היה מגלה שנבחר מתי שנוח לו; עכשיו יש לו שעתיים, ואם הטאב סגור הלקוח ממתין לחינם ובעל המקצוע מפסיד עבודה. השאלה פתוחה ב-`CLAUDE.md` סעיף 9 מאז Phase 4 — זה השלב שהופך אותה לחסם. SMS דרך Twilio הוא הקו הקצר, והוא כסף ולכן הכרעה של המשתמש (סעיף 8).
2. **ביטול עבודה משובצת.** אין בקוד שום מעבר ל-`cancelled`, וכשהדמים נגבים מראש ואינם מוחזרים, בעל מקצוע ששילם והלקוח נעלם הוא תלונה שתגיע. נשאר בסעיף 9 של `CLAUDE.md`, וחשוב יותר מקודם.

**מה נבנה בפועל:** מיגרציה אחת (`20260911120000_pro_acceptance_and_flat_fee.sql`), כרטיס האישור של בעל המקצוע ב-`/pro/offers` וב-`/pro/dashboard`, מצב ההמתנה ו"בטלו את הבחירה" במסך ההשוואה של הלקוח, סימון "כבר נבחרה הצעה" בפיד, והחלפת ה-12% בכל מסך, מסמך וקובץ תוכן. סעיף 16 חדש ב-pgTAP (52 טענות) וצעד האישור נכנס לשתי הזרימות ב-Playwright.

**החלטות שנפלו בשלב הזה:**
1. **`selected_bid_id` נכתב רק באישור**, ולכן אף פונקציה מ-Phase 4–8 לא השתנתה. ההסבר המלא ב-`docs/architecture.md`, "מה נוסף ב-Phase 10".
2. **הקריאה נשארת בפיד בזמן ההמתנה** (הכרעת המשתמש): קריאה שלא אושרה אינה מחויבת לאיש, וזה מה שנותן ללקוח למי לעבור אם יתחרט.
3. **שני שעונים שלא רצים יחד** — 45 דקות עד שבוחרים, שעתיים לענות. `expire_stale_bids()` נוגעת רק ב-`pending`, `expire_stale_selections()` רק ב-`selected`.
4. **`commission_charges` שונתה ל-`job_fees`** ולא נוספה טבלה שנייה: זו אותה עובדה שנמדדת אחרת.
5. **הדמים אינם מוחזרים** (הכרעת המשתמש), ולכן `resolve_dispute()` לא נגעה בפנקס בכלל.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 10 מתוך `docs/roadmap.md`: שלב אישור של בעל המקצוע אחרי בחירת הלקוח, ודמי קבלת עבודה קבועים של 35 ₪ במקום עמלת 12%. ההחלטות כולן נעולות בטבלה שבראש השלב — אל תשנה אותן ואל תוסיף עליהן. התחל מהמיגרציה ומ-pgTAP, ורק אחר כך מהמסכים ומהקופי: הכלל שאסור לשבור הוא ששום חיוב לא נוצר לפני אישור, ושבעל מקצוע שאינו הזוכה לא יכול לאשר — שניהם נבדקים במסד הנתונים, לא ב-UI.

---

## Phase 11 — זיהוי כתובות

**מטרה:** להפסיק לבקש מהלקוח להקליד את מה שהמכשיר שלו כבר יודע, ולהפסיק להאשים אותו בתקלה שהיא שלנו.

**מה הביא לזה:** באתר הפרוס ב-Vercel כמעט כל כתובת נענתה ב"לא הצלחנו לאתר את הכתובת על המפה". זו לא הייתה בעיית זיהוי אלא קונפיגורציה: `mapsFallbackAllowed()` שקרי בבילד פרודקשן בלי `ALLOW_NO_MAPS_KEY`, ולכן `geocodeAddress()` זרק `MapsNotConfiguredError` על **כל** כתובת ו-`postJob` לא הצליח אף פעם. `playwright.config.ts` כבר תיעד בדיוק את זה בהערה משלו, ומדליק את הדגל כדי שהסוויטה תעבור.

**ההחלטה שננעלה לפני הבנייה** (המשתמש, 9.9.2026): **להישאר בלי מפתח Google.** מפתח דורש חשבון חיוב עם כרטיס אשראי גם במכסה החינמית, והמוצר צריך היום לדעת באיזה יישוב הקריאה — לא באיזו דלת. רדיוס חיפוש של 10–30 ק״מ לא מבחין בין מרכז העיר לרחוב.

**מה נבנה:**

*הגזטיר — `scripts/build-gazetteer.mjs` ו-`lib/maps/localities.data.ts`:*
- 313 יישובים במקום 31, **נוצרים** מ-Wikidata ולא נכתבים ביד: קואורדינטה היא מספר, ו-CLAUDE.md סעיף 3 אוסר מספר מומצא. שתי שאילתות, כי יו״ש רשומה תחת מדינה אחרת ומסנן מדינה לבדו מאבד את אריאל, מעלה אדומים, ביתר עילית ומודיעין עילית.
- כינויים מ-`skos:altLabel` — משם מגיעים "פתח תקוה" לצד "פתח תקווה" ו"קרית גת" לצד "קריית גת". אבל altLabel מערבב כתיב וכינוי, והכינויים של ירושלים כוללים את "אריאל", שהיא עיר אחרת: כינוי נשמר רק כשהוא **כתיב של השם** (מרחק עריכה קטן, חצי של שם מקוקף, או ראשי תיבות).
- ראשי תיבות בגרשיים נשמרים בנפרד ומותאמים רק לקלט שנוקד כך, כי "פת" היא מילה בעברית.

*ההתאמה — `lib/maps/gazetteer.ts`:*
- רצף מילים שלמות במקום `String.includes`, שהחזיר "יפו" מתוך "יפואית".
- **החלק האחרון אחרי פסיק נבדק ראשון**, בדיוק כמו `job_city()` ב-SQL, כך שהנקודה שנשמרת והעיר שהקונסולה מקבצת לפיה מסכימות בבנייה.
- נרמול: ניקוד, גרשיים, מקף עברי, "רחוב"/"שדרות" בתחילת חלק, "דירה 4"/"קומה 2", ומספרי בית.
- תחיליות ב/ל/מ/ו/כ/ש רק במעבר שני, כדי ש"בת ים", "בני ברק" ו"לוד" לא ייהרסו.

*הסירוב — `lib/maps/geocode.ts`:*
- כתובת בלי יישוב מזוהה מחזירה `null` במקום את מרכז תל אביב.
- שני סוגי הכישלון הופרדו: תקלה שלנו (`logServerError`, "תקלה זמנית") מול כתובת שלא זוהתה (`logExpectedRefusal`, "הוסיפו עיר בסוף הכתובת").
- `addressToStore()` מוסיף את היישוב שזוהה כשהכתובת לא נגמרת בו — מתקן באג קיים שבו `/admin/jobs` קיבץ עבודות תחת "דירה 4".

*המסכים — `components/ui/AddressField.tsx`:*
- זיהוי תוך כדי הקלדה, `<datalist>` של כל היישובים, ובורר עיר כשלא זוהה כלום — במקום לגלות את זה בשרת אחרי שליחת הטופס.
- **"השתמשו במיקום הנוכחי"**: נקודת GPS נשמרת כמו שהיא (`geocodeAddress` כבר מעדיף `clientPoint` תקף), ושם היישוב הקרוב ממולא כטקסט. מיקום מקורב אומר זאת, הרשאה שנדחתה משאירה טופס עובד, ונקודה מחוץ לתיבת ישראל נדחית באותה בדיקה שהשרת עושה.

*כתובות שמורות — `20260912120000_saved_places.sql`:*
- "בית", "עבודה". `customer_id` בלי grant, עם `auth.uid()` כברירת מחדל; `location` **כן** ניתן לכתיבה, כי נקודה שנשמרה מ-GPS היא כל הערך — והיא רמז שנבדק מחדש בכל שימוש.
- `savePlace` עובר באותו `geocodeAddress` שקריאה עוברת בו, כדי שלא תישמר כתובת שטופס הקריאה יסרב לה.
- ניהול ב-`/account`, שימוש בטופס הקריאה.

**הגדרת סיום:**
- [x] `ALLOW_NO_MAPS_KEY=1` מתועד ב-`.env.example` כמצב הנבחר, לא כמצב חירום — **וצריך להיות מוגדר ב-Vercel ולעבור פריסה מחדש**
- [x] `npm run db:reset` נקי, `npm run db:test` (372 טענות, 12 חדשות ל-`saved_places`), `npm run db:types` מחודש ומקומט
- [x] `npm run lint`, `npm run typecheck`, `npm run build`, `npm run test` (388), `npm run test:e2e` (143, מהן ארבע ב-`e2e/address.spec.ts`) — כולם עוברים
- [x] RTL: לוגיות בלבד, נסרק על ידי `tests/rtl.test.ts`
- [x] `lib/content/cities.ts` הבטיח מאז Phase 8 שהקואורדינטות שלו תואמות לגזטיר; `citiesGazetteer.test.ts` הופך את זה לבדיקה
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**: המיקום הנוכחי דורש הרשאה אמיתית ומכשיר אמיתי, ואי אפשר לשפוט אותו מבדיקה אוטומטית

**מה שהתגלה בשימוש, ותוקן מיד אחרי:**
- **`mySavedPlaces()` החזיר רשימה ריקה מאז ומעולם.** `saved_places.location` הוא
  geography, ו-PostgREST מוסר אותו כמחרוזת hex EWKB; הקורא ציפה ל-GeoJSON, לא מצא
  `coordinates`, והשמיט כל שורה. כתובת נשמרה נכון, אוחסנה נכון, הוגנה נכון על ידי
  RLS — ומעולם לא הוצגה. `jobs` נתקל בזה ב-Phase 2 ופתר בעמודות מיוצרות
  (`20260902121000_job_coordinates.sql`); אותו פתרון הוחל כאן
  (`20260913120000_saved_places_coordinates.sql`) עם שתי טענות pgTAP.
- **הלולאה הייתה שבורה בהתחלה.** הדרך היחידה ליצור כתובת הייתה `/account`, ושם היא
  ישבה שלישית בסיידבר. עכשיו אפשר לשמור מטופס הקריאה עצמו — הרגע היחיד שבו שמירה
  היא פעולה טבעית — והניהול עלה לעמודה הראשית של האזור האישי.

**מה שמסומן בשלב הזה ולא נבנה בו:**
1. **דיוק ברמת רחוב.** הגזטיר מזהה יישוב, לא דלת. GPS סוגר את רוב הפער בחינם, אבל השלמה אוטומטית ברמת רחוב ופין שאפשר לנווט אליו דורשים מפתח Google — נשאר פתוח ב-`CLAUDE.md` סעיף 9.
2. **שכונות.** הגזטיר כולל שכונות שיש להן ערך ב-Wikidata עם אוכלוסייה, אבל לא באופן שיטתי.

---

# חלק ב׳ — לקראת השקה אמיתית בעיר אחת (Phases 12–17)

Phases 0–11 בנו את המוצר. השלבים שלהלן נגזרו מ**סקירה מלאה של שתי הזרימות מקצה לקצה** (ספטמבר 2026), ומטרתם אחת: להביא את Handy למצב שאפשר להשיק בו בעיר אחת מול משתמשים אמיתיים.

**מה שהסקירה מצאה, בשורה אחת:** הקוד במצב טוב חריג (0 lint, 0 `any`, 0 TODO, 372 טענות pgTAP) — **החוב אינו חוב קוד אלא חוב מוצרי**. שלושה דפוסים חוזרים בשני הצדדים:

1. **אין שום התראה מחוץ לטאב**, ומאז Phase 10 המוצר *תלוי* בכך שבעל המקצוע ישים לב תוך שעתיים — אחרת הוא מפסיד עבודה והלקוח ממתין לחינם.
2. **הבטחות בקופי בלי קוד מאחוריהן** — "נעדכן ב-SMS" (×4), "הזמנה חוזרת בלחיצה אחת" (×3), "רק קריאות שמתאימות להגדרות האלה יגיעו אליך" (`work_days` לא מסנן כלום), ושני מספרים מומצאים (64%, "פי 3") שסותרים את כלל "אין מספר מומצא" של `CLAUDE.md` סעיף 3.
3. **מסכים שלא חוברו למה שכבר קיים** — `BidCard` לא מקשר ל-`/pro/[slug]` שנבנה ב-Phase 8, והדשבורד של בעל המקצוע חסר את `my_earnings_stats()` שנבנה ב-Phase 6.

**החלטות שננעלו עם המשתמש לפני כתיבת החלק הזה:**
- **התראות: ערוצים חינמיים בלבד** (טבלת `notifications` + מרכז התראות + Web Push), **מאחורי אדפטר ספק**, כך שחיבור גייטוויי SMS ישראלי בעתיד הוא קובץ אחד ולא שכתוב.
- **המספרים המומצאים** מומרים בנתון אמיתי מה-DB היכן שאפשר, ובעצה איכותנית בלי אחוז היכן שאין נתונים.

| שלב | מה | גודל | חוסם השקה? | דורש הכרעת משתמש |
|---|---|---|---|---|
| 12 | כנות, כוונה ונזילות | קטן | **כן** | לא |
| 13 | התראות + Web Push + אדפטר | **גדול** | **כן** | רק אם `pg_net` לא זמין |
| 13.7 | משפך הלקוח: פרסום לפני הזדהות, תקלות נפוצות, סימני חיים, מועד מוסכם | **גדול** | קובע המרה | לא |
| 13.8 | חזרה וצמיחה: תיק הבית, קישור אישי לבעל מקצוע | בינוני | לא | הוכרע 14.9.2026 (ניסוי ה-AI נדחה) |
| 14 | אמון ברגע הבחירה + הזמנה חוזרת | בינוני | קובע המרה | לא |
| 15 | ביטול, אי-הגעה, הוגנות הכסף | בינוני | **כן**, לטווח בינוני | **כן — לפני קוד** |
| 16 | שימור בעל מקצוע | בינוני | לא | לא |
| 17 | צמיחה ורכישה | בינוני | לא | לא |

**אם צריך לקצר:** 12 ו-13 הם המינימום. 15 יכול לחכות לתלונה הראשונה — אבל לא יותר, כי מרגע ש-Phase 10 גובה 35 ₪ מראש ולא מחזיר, התלונה הזו תגיע.

---

## Phase 12 — כנות, כוונה ונזילות

**מטרה:** להסיר מהמוצר כל הבטחה שאין מאחוריה קוד, ולתקן את שתי נקודות הנטישה הראשונות במשפך — לפני שמשתמש אמיתי רואה אותן.

**למה ראשון:** השלב הזול ביותר עם ההשפעה המיידית הגבוהה ביותר. כמעט לא מוסיף טבלאות, ורובו מחיקות ותיקוני קופי.

**מה נכלל:**
- **העברת כוונת הקטגוריה.** `/new-request?category=<slug>` — `PostJobForm` קורא את הפרמטר ובוחר מראש. לעדכן את `app/(marketing)/page.tsx` ואת `components/marketing/CategoryLanding.tsx`, שהיום שולחים `href="/new-request"` בלי פרמטר וזורקים את הבחירה.
- **שימור יעד בהתחברות.** `proxy.ts` עושה היום `url.search = ""` ואין `?next=`, ולכן `verifyOtp` תמיד מנחית ב-`ROLE_HOME` — כל deep link נוחת באזור האישי. להוסיף `?next=`, **ולאמת בשרת שהיעד שייך לתפקיד שהתחבר** (`loginPathFor(next) === ROLE_LOGIN[role]`), אחרת זו פרצת הפניה פתוחה.
- **מצב אפס אמיתי.** מסך ההצעות מדפיס היום "נשלחה ל-0 בעלי מקצוע מאומתים בסביבה. אין צורך לרענן" — וזה מצב ברירת המחדל ביום ההשקה. להחליף בהודעה כנה עם פעולה: הרחבת רדיוס בלחיצה, ודגל "נודיע לכם כשיצטרף בעל מקצוע באזור" (ההתראה עצמה נשלחת ב-Phase 13). ולחשוף את אותה ספירה **בתוך טופס הקריאה**, בשלב הכתובת.
- **המרת שני המספרים המומצאים.** `64%` מופיע בארבעה מסכים ו"פי 3" בשניים. להמיר בנתון שנגזר מ-`bids`, ואם אין מדגם — בעצה איכותנית בלי אחוז, באותה תבנית שבה `similar_bid_range` כבר שותקת מתחת ל-3 דגימות.
- **תיקון ההבטחות.** "נעדכן ב-SMS" ו"כיבוי מפסיק את ההתראות" מנוסחים לפי מה שקיים (ומוחלפים שוב ב-Phase 13). **`work_days`/`work_hours`:** או אכיפה ב-`open_jobs_for_pro()`, או תיקון הקופי — ראו ההחלטה למטה.
- **הוצאת משטחי הפיתוח מהממשק הצרכני:** שורת "מיקום שנשמר" שמדפיסה קואורדינטות ללקוח, וה-`CurrentUserCard` שמסביר ללקוח מה זה RLS ומדפיס את ה-UUID שלו.
- **חיכוך הטופס:** רמז חי למינימום 15 התווים (נאכף היום בשרת בלבד), `scrollIntoView` + פוקוס לשגיאה הראשונה, וולידציה נייטיבית ל-`categoryId`/`preferredTime` שהם היום `<input type="hidden">`.
- **שיתוף (OG):** `app/icon.png`, `app/apple-icon.png` ו-`opengraph-image`. `lib/seo.tsx` מגדיר היום `openGraph` בלי `images`, ולכן **שיתוף בוואטסאפ — ערוץ ההפצה המרכזי בישראל — נראה כטקסט ריק**. ה-`manifest.ts` נשאר ל-Phase 13, כי הוא חלק מ-Web Push.
- **ספירת הצעות באזור האישי:** `bidsCount` על `JobSummary`. `job_bid_count()` כבר קיימת.

**הגדרת סיום:**
- [x] אין בריפו אף מחרוזת שמבטיחה SMS, התראה או הזמנה חוזרת שאין מאחוריה קוד — `tests/copyPromises.test.ts` קורא ליטרלים של מחרוזות בלבד (כך שההסבר שמעל כל הסרה, שחייב לצטט את הטענה שהוסרה, לא מפיל אותו) ומדווח קובץ ושורה. אומת בכך שהוחזרה טענה ונצפה הכישלון
- [x] `?next=` נבדק ב-Playwright, **כולל ניסיון הפניה לתפקיד זר ולאתר חיצוני** — ובנוסף שבע טענות Vitest על `postLoginPath()`
- [x] מצב "0 בעלי מקצוע ברדיוס" נבדק ב-E2E ומציג פעולה, לא מספר — נשען על קריאה שנזרעה **באילת**, מרחק שאף רדיוס בסולם לא מכסה, כך שהכרטיס חייב להציע את הפעולה בלי להבטיח שהיא תעזור
- [x] `npm run db:reset`, `db:test` (380), `lint`, `typecheck`, `build`, `test` (412), `test:e2e` (149) — כולם עוברים
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **ל-satori אין מנוע דו-כיווניות.** כרטיס ה-OG הראשון רינדר את "בעל מקצוע אמין ליד הבית, היום" כהיפוך של עצמו, ונראה סביר לגמרי עד שמישהו שקורא עברית הסתכל. לא `dir="rtl"` ולא `direction: rtl` משנים דבר. זו בדיוק הבעיה שה-PDF עקף בבחירת מנוע עם textkit מאחוריו, ולכרטיס OG אין בחירה כזו. הפתרון (`lib/og/bidi.ts`) מצומצם בכוונה: עבור שורה בלי תווים חזקים משמאל-לימין, הפלט של אלגוריתם ה-UBA **הוא** רצף התווים ההפוך, ולכן ההיפוך אינו קירוב אלא התשובה. על כל דבר אחר הפונקציה מסרבת, ו-`lib/og/copy.ts` מרכז כל שורה עברית שכרטיס מצייר כדי שאפשר יהיה לבדוק את כולן.
2. **`pageMetadata()` דחק החוצה את הכרטיס שהיה אמור לרשת.** Next ממלא `openGraph.images` מקונבנציית הקובץ רק בעמוד שלא כתב `openGraph` בעצמו — וכל 14 העמודים הציבוריים כתבו. כך שדף הבית קיבל כרטיס וכל מי שהכי צריך אותו לא. התגלה בקריאת ה-HTML המרונדר של ארבעה מסלולים.
3. **`bids(...)` ב-PostgREST מחזיר את ההצעה הנבחרת, לא את כולן.** `jobs` ו-`bids` מחוברים פעמיים (`bids.job_id` וגם `jobs.selected_bid_id`), ולכן embed בלי שם אילוץ נפתר לצד ה-many-to-one. הספירה "הצעות ממתינות" הייתה נחסמת על 1 ונכונה בשקט בכל קריאה עם הצעה אחת. `npm run db:types` הוא מה שתפס.
4. **`redirectIfSignedIn()` התעלם מ-`next`**, כך שמשתמש מחובר שהגיע ל-`/login?next=…` נחת בבית שלו. עכשיו הוא עובר באותו `postLoginPath()`.
5. **טענת pgTAP אחת נכתבה מחדש אחרי שתפסה את האמת:** בעל מקצוע שמנסה לצמצם רדיוס של קריאה שאינה שלו **לא** מקבל שגיאה — RLS מסנן את השורה וה-UPDATE מצליח על אפס שורות. הטענה עברה מהמנגנון לתוצאה.
6. **זריעת קריאה אמיתית הזיזה ארבע ספירות ב-pgTAP**, וכולן עודכנו — כולל התראת "קריאות ללא הצעות מעל שעה" בקונסולה, שסופרת עכשיו את הקריאה מאילת. זו בדיוק מטרתה.

**החלטה שצריכה ליפול בתוך השלב:** `pros_in_range()` מקבלת `p_job_id` ובודקת בעלות — היא **לא שמישה לפני שהקריאה קיימת**. הספירה בטופס דורשת אחות חדשה `pros_near_point(lat, lng, radius_km)`, `security definer`, **מוענקת ל-`authenticated` בלבד**. היא מחזירה ספירה ולא שורות, באותה משפחה של `category_stats()` — כלומר `pro_profiles` לא מקבל שום מדיניות קריאה חדשה.

**אזהרת עיצוב על `work_days`:** אסור פשוט לרוקן את הפיד מחוץ לשעות. `open_jobs_for_pro()` כבר מסנן לפי `pro_categories` ומוגבל ל-100 — מסנן זמן גורף יסתיר גם קריאה `this_week` או `flexible` שבעל המקצוע דווקא רוצה לתמחר ביום שישי. הכלל הנכון: **מחוץ לשעות מסתירים רק `asap`/`today`**. אם זה נראה גדול מדי לשלב — לתקן את הקופי בלבד ("שעות העבודה מוצגות ללקוחות בפרופיל שלך") ולהעביר את האכיפה ל-Phase 16.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 12 מתוך `docs/roadmap.md`: הסרת כל הבטחה שאין מאחוריה קוד, שימור כוונת המשתמש בדרך לקריאה הראשונה, ומצב קור-סטארט כן. אין כאן פיצ׳ר חדש — זה שלב של כנות. התחל מסריקת הקופי (חפש בעצמך את כל המחרוזות שמבטיחות SMS, התראה או הזמנה חוזרת), ורק אחר כך מהקוד. הכלל שאסור לשבור: אף מסך לא נשאר מבטיח משהו שהמוצר לא עושה.

---

## Phase 13 — התראות: מרכז התראות, Web Push ואדפטר ספק

**מטרה:** שבעל מקצוע ידע שנבחר בזמן שיש לו שעתיים, ושלקוח ידע שנשאלה לו שאלה על כסף — גם כשהטאב סגור.

**למה זה חסם ולא שיפור:** `select_bid()` נותן שעתיים, `accept_job()` מחייב 35 ₪ **לפני** העבודה, והחיוב אינו מוחזר. `lib/validation/bids.ts` אומר את זה בהערה משלו: *"Two hours because nothing notifies a pro whose tab is closed"*. **המסך כבר מעוצב** — `design/screens/pro-5.4-notifications.png`.

**התזה במשפט אחד:** *שורת ההתראה היא עובדה במסד הנתונים, שנכתבת בתוך הטרנזקציה שיצרה אותה. ה-push הוא מאיץ מיטבי-מאמץ מעליה, ושום נכונות במוצר לא תלויה בכך שהוא נמסר.* זה מה שמאפשר לשלב לעמוד בכלל של `CLAUDE.md` ש-cron הוא "רק משק בית" — בלי להחליש אותו.

**מה נכלל:**

*מסד הנתונים — מיגרציה אחת:*
- **`notifications`** — `user_id`, `kind` (אוצר מילים סגור עם `check`), `job_id`, `actor_id`, `payload jsonb`, `read_at`, `pushed_at`. **INSERT לאף client role, לעולם** (תבנית `job_locations`), `grant update (read_at)` בלבד (תבנית `messages.read_at`), ואין DELETE. שיפור אחד על `messages`: טריגר `before update` שקובע `new.read_at = now()` ומסרב לכל שינוי אחר — הטכניקה של `price_updates`, שמחזיקה את הקו גם מול קורא שעוקף RLS.
- **מי כותב את השורה: מי שכבר מחזיק במעבר.** את הרוב כותבות פונקציות ה-definer הקיימות (`select_bid`, `accept_job`, `decline_job`, `withdraw_bid_selection`, `expire_stale_selections`, `set_pro_verification`, `request_price_update`, `decide_price_update`, `mark_job_in_progress`, `complete_job`, `submit_job_review`). **הנימוק המכריע:** `select_bid()` משחרר את המחזיק הקודם ב-`update ... where status = 'selected'` — הצהרה **זהה בייטים** לזו של `withdraw_bid_selection()`. טריגר שורה יראה `selected → pending` ולא יוכל להבחין בין "הלקוח בחר מחדש" לבין "הלקוח ביטל". לפונקציה יש את ההחלטה ביד.
- **שני טריגרי `after insert` בלבד** — על `bids` ועל `messages`, **שתי הטבלאות היחידות שקליינט כותב אליהן ישירות**. אין שם פונקציית definer, וקריאה משכבת האפליקציה אפשר לדלג עליה.
- **הפיזור לפיד הוא המקרה החריג:** ל"קריאה חדשה ברדיוס שלך" אין נמען על השורה. `pro_serves_job()` לא שמישה — היא כתובה מול `auth.uid()` ועונה "האם *הקורא* משרת". צריך פונקציה הפוכה, `pros_serving_job(p_job_id) returns setof uuid`, וטריגר `after insert on jobs` עם `insert … select` אחד. **טריגר ולא סחיפה:** סחיפה תצטרך סמן, ובעל מקצוע שירחיב רדיוס יוצף פתאום בקריאות מלפני שבוע. **כאן `accepting_jobs` סוף-סוף מצדיק את הקופי "כיבוי מפסיק את ההתראות מיד"** — בלי לשנות מילה במשפט.
- **`warn_expiring_selections()`** ב-pg_cron לצד `expire_stale_selections()`, עם `lapse_warned_at` בלי grant לאף client. משק בית כן: אזהרה שלא נורתה משאירה את בעל המקצוע בדיוק במצב של היום.
- **"בעל המקצוע בדרך"** נתלה על `after insert on job_locations` — שנורה בדיוק פעם אחת כי `job_id` הוא ה-PK — ו**לא** על `report_job_location()`, שרצה כל 15 שניות.
- **`push_subscriptions`** — שורה למכשיר. **בניגוד ל-`saved_places`, אין כאן `grant insert`:** endpoint אינו תמיד של הקורא. טלפון משותף, או בעל מקצוע שמתנתק ולקוח שמתחבר באותו דפדפן, מייצרים חשבון שני שתובע endpoint של הראשון — ותחת RLS הוא לא רואה את השורה שהוא חייב להחליף. **מדיניות לא יכולה להכריע מפתח שהיא לא רואה.** לכן `grant select, delete` ופונקציית `save_push_subscription()` שמעבירה בעלות. **ואין מדיניות קריאה לאדמין** — endpoint הוא יכולת לדחוף למכשיר, לא תיעוד של מה שקרה; צריך לומר את זה בהערת המיגרציה, אחרת "יתקנו" את זה לשם עקביות.
- `alter publication supabase_realtime add table public.notifications`, ו-`"notifications"` נוסף לאיחוד ב-`components/ui/RealtimeRefresh.tsx` — מילה אחת, והתג בהדר חי.

*האפליקציה:*
- **`lib/notifications/`** — `kinds.ts`, `messages.ts` (**הבית היחיד של הקופי**, לשורה ול-push כאחד), `provider.ts` (התפר), `providers/inApp.ts`, `providers/webPush.ts`, ו-`dispatch.ts`. התפר נושא `costsMoney: boolean`, כך ש**הדלקת SMS ביום שיוחלט היא דגל ולא עריכה** — אותו לולב ש-`lib/auth/bypass.ts` משתמש בו. **חיבור גייטוויי ישראלי בעתיד = קובץ אחד (`providers/sms.ts`) + שורה אחת**, בלי מיגרציה ובלי עותק שני של העברית.
- **Service worker:** ב-Next 16 הוא **`lib/service-worker.js`** שנרשם דרך `new URL(..., import.meta.url)` — **לא** `public/sw.js`. Next מקמפל אותו ומוסיף `Service-Worker-Allowed` בעצמו. ראו `node_modules/next/dist/docs/01-app/02-guides/progressive-web-apps.md`.
- **בקשת ההרשאה — לעולם לא בטעינה הראשונה, ותמיד מאחורי כפתור** (Safari דורש transient activation). בעל מקצוע: מיד אחרי ההצעה הראשונה — הרגע שבו השעתיים הופכות לבעיה שלו. לקוח: אחרי פרסום הקריאה, ואחרי `select_bid()`. **iOS דורש התקנה למסך הבית** — `InstallPrompt` בצד בעל המקצוע בלבד.
- **המסכים:** `/pro/notifications` לפי ה-PNG, `/account/notifications` ללקוח, ותגי ספירה ב-`ProShell` וב-`SiteHeader`. הספירה היא `count: "exact", head: true` תחת RLS ו**לא** פונקציית definer — סעיף 3 שומר פונקציות לאגרגט של אדמין; ספירה של המשתמש עצמו היא מדיניות שבוחרת שורות.

**הגדרת סיום:**
- [x] `select_bid()` יוצרת שורת התראה **באותה טרנזקציה**, ובחירה מחדש כותבת `selection_moved` בעוד ביטול כותב `selection_withdrawn` — שתי טענות pgTAP על אותה הצהרת `update` בדיוק, כי זה מה שטריגר שורה לא היה יכול להבחין בו
- [x] לאף client role אין INSERT על `notifications` (42501), `pushed_at` לא ניתן לכתיבה, ו-`read_at` נכתב רק על ידי הנמען — וטריגר קובע אותו ל-`now()` וחד-כיוונית, כך שאי אפשר לנפח את התג
- [x] `accept_job()` פעמיים = התראה אחת (אידמפוטנטיות, לצד טענת שורת-החיוב הקיימת בסעיף 11)
- [x] פיזור הפיד מגיע רק למי שבתוך **שני** הרדיוסים, לא ל-`accepting_jobs = false`, ולא לתחום אחר — והקריאה מאילת של Phase 12 מיידעת אפס אנשים
- [x] בעל מקצוע לא רואה endpoint של אחר — **וגם לא אדמין**, וקריאה שנייה מאותו דפדפן מעבירה בעלות במקום להיכשל
- [x] `/pro/notifications` נכון **כשהסחיפה כבויה לגמרי** — כל השורות נכתבות בטרנזקציה הגורמת, והסחיפה קובעת `pushed_at` בלבד
- [x] בדיקת Vitest שהאיחוד `NotificationKind` ב-TypeScript תואם מילה במילה ל-`check (kind in (...))` במיגרציה — התקדים של `RESERVED_SLUGS`, שגם הוא תוקן בשלב הזה
- [x] `npm run db:reset`, `db:test` (413), `db:types`, `lint`, `typecheck`, `build`, `test` (429), `test:e2e` (152)
- [ ] **push אמיתי אחד אומת ביד בדפדפן אמיתי** — **ממתין לבדיקת המשתמש**: `grantPermissions` נותן הרשאה אבל אין שירות push בדפדפן בדיקה, בדיוק כפי שהקבלה מאומתת ברינדור ובהסתכלות. דורש גם שני מפתחות VAPID ושני סודות ב-Vault (ראו `.env.example`)
- [x] ארבע ההבטחות ל-SMS הוחלפו, ו-`lib/content/legal.ts` מציין ש-endpoint הוא מידע אישי ושירות ה-push הוא מעבד צד שלישי
- [ ] בדיקה ידנית בדפדפן, RTL — **ממתין לבדיקת המשתמש**

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **`pg_net` כן זמין** (0.20.4, לצד `supabase_vault` שכבר מותקן), ולכן הדיספצ׳ר נבנה כפי שתוכנן — ו-`SUPABASE_SERVICE_ROLE_KEY` נשאר לא-נקרא בשום מקום בריפו.
2. **`bids_guard_update` היה מפיל את סחיפת האזהרה בפרודקשן, כל חמש דקות.** הוא מסרב לכל עדכון על הצעה שאינה `pending` אלא אם הסטטוס עצמו משתנה — ו-`warn_expiring_selections()` כותבת `lapse_warned_at` על הצעה שהיא `selected` בהגדרה. נתפס על ידי pgTAP, לא על ידי קריאה.
3. **`push_subscriptions.last_success_at` לא יכול היה להיכתב לעולם.** המסלול שיודע שדחיפה נחתה הוא זה שאין לו אישור גישה למסד — במכוון. ולכן `prune_push_subscriptions()` מחק לפי `is null AND ישן מ-90 יום`, כלומר **כל** מנוי ישן מ-90 יום, כולל חיים. הוחלף ב-`last_seen_at` שהדפדפן חותם בכל טעינה.
4. **`Date.now()` ברינדור הוא לא-טהור**, וההשלכה קונקרטית: השרת מרנדר "לפני 2 דק׳" והדפדפן מתייבש שנייה אחר כך וחולק עליו. הוזז ל-loader, שם "מתי נקרא" הוא ממילא עובדה על הקריאה.
5. **`useSyncExternalStore` הוא הכלי הנכון ל-`Notification.permission`** — לא effect עם setState, שכלל ה-lint סירב לו בצדק.
6. **בדיקת ה-E2E הראשונה שכתבתי הפילה בדיקת נגישות שכנה**, כי היא הריצה `select_bid()` דרך הממשק ושינתה קריאה זרועה שספק אחר נשען עליה. סוויטה שהסדר בה משנה נכשלת מסיבות שאינן קשורות לשינוי הנבדק.

**ההחלטה היחידה שדורשת אימות לפני כתיבת קוד — מי שולח את ה-push:**
ההמלצה היא **סחיפת pg_cron שקוראת `pg_net` ומפרסמת אצווה ל-route handler ב-Next**. נפסלו: Edge Function (אין `supabase/functions` בריפו, ו**הקופי בעברית היה קיים פעמיים** — פעם ב-Deno ופעם ב-TS), וטריגר לכל שורה (מכניס N בקשות יוצאות לטרנזקציה של `postJob`, **ולא יודע לאחד** — פרץ הוא המקרה הרגיל כאן). היתרון המכריע של הנבחר: **גוף ה-POST נושא את הכול, ולכן ה-route handler לעולם לא פונה למסד ולא מחזיק אישור גישה אליו** — מה ששומר על התכונה שהריפו נלחם עליה, ש-`SUPABASE_SERVICE_ROLE_KEY` לא נקרא בשום מקום בקוד.
> **⚠ `pg_net` אינו מותקן היום** — המיגרציות מפעילות `postgis` ו-`pg_cron` בלבד. הצעד הראשון בשלב הוא `select * from pg_available_extensions where name in ('pg_net','supabase_vault');`. **אם הוא לא זמין, החלופה היא Vercel Cron → route handler, ואז הדיספצ׳ר כן צריך `SUPABASE_SERVICE_ROLE_KEY` — הפעם הראשונה בריפו.** זו הכרעה שצריכה ליפול בעיניים פקוחות.

**קצה חד שקל לפספס:** `"notifications"` **אינו** ב-`RESERVED_SLUGS` היום, כלומר בעל מקצוע יכול לתפוס את `/pro/notifications` כ-slug שלו. צריך: (א) הרשימה ב-`lib/validation/publicProfile.ts`, (ב) הפלה ובנייה מחדש של `pro_profiles_public_slug_format_check`, ו-(ג) עדכון `lib/validation/__tests__/publicProfile.test.ts`, **שקורא בקוד קשיח את נתיב המיגרציה של Phase 8** וייפול ברגע שהאילוץ יזוז.

**מה שהשלב סוגר:** `CLAUDE.md` סעיף 9 (החלטת ה-push — ההחלטה הפתוחה היחידה שהשלב הזה באמת יכול לסגור), הסעיף התלוי של Phase 3 ("SMS כשהפרופיל אושר"), ו-`TECHNICAL_DEBT.md` #39.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 13 מתוך `docs/roadmap.md`: שכבת התראות מלאה — טבלת `notifications`, מרכז התראות, Web Push, ואדפטר ספק. **התחל באימות ש-`pg_net` זמין** (ראו ההחלטה בשלב), ואז במיגרציה וב-pgTAP — ורק אחר כך במסכים. הכלל שאסור לשבור: **שורת ההתראה נכתבת בתוך הטרנזקציה שיצרה את האירוע**, ו-`/pro/notifications` חייב להיות נכון גם כשהסחיפה כבויה לגמרי. אל תשלח שום SMS ואל תוסיף ספק בתשלום — האדפטר נכתב כדי שזו תהיה החלטה נפרדת.

---

## Phase 13.5 — שפה חזותית, מובייל ונגישות ✅

**מטרה:** שכבת העיצוב נכתבה פעם אחת ב-Phase 2 ולא נגעו בה בזמן שהאפליקציה גדלה
ל-126 קומפוננטות סביבה. השלב הזה הוא הביקורת והתיקון — **בלי ולו מיגרציה אחת**,
בלי שינוי RLS, ובלי שינוי ב-`lib/actions/**` או `lib/supabase/**`. זו תכונה: היא
מה שמאפשר לסקור את כל השלב כשכבת תצוגה בלבד.

**למה 13.5 ולא 14:** המספר 14 כבר תפוס, וסעיף "ניווט מובייל" היה קבור בתוך
Phase 16 — לצד בעל המקצוע בלבד, שתי פאזות משם, בזמן שמסך הלקוח והקונסולה שבורים
באותה מידה. הסעיף נגזר לכאן והורחב לשלוש המעטפות.

**מה שנמדד, ומה שכל ממצא הפך להיות:**

| הממצא | מה נעשה |
|---|---|
| **2** שימושי `shadow-` בכל הריפו | שלושה טוקני עומק; `CARD_BASE` לוקח את הראשון, `CARD_RAISED` את השני |
| `focus-visible` הופיע **פעם אחת**; ל-`BUTTON_BASE` לא הייתה טבעת בכלל | טבעת לכל וריאנט בגוון שלו, ו-fallback גלובלי לכל מה שאינו כפתור |
| **0** `<svg>`; כל פיקטוגרם אמוג'י | `components/ui/icons.tsx` — 26 אייקוני קו, בלי תלות. אמוג'י אינו יורש `currentColor`, ולכן אריח תחום נבחר שינה גבול, רקע וטקסט בזמן שהגליף במרכזו לא השתנה |
| `transition-colors`×50, `animate-pulse`×1, ואפס התייחסות ל-`prefers-reduced-motion` | שתי אנימציות בשם, והבלוק שהופך את הוספתן לבטוחה |
| **43** עותקי `mt-2 text-muted`, **59** של `text-lg font-bold text-ink`, ו-`<h1>` בשש איותים | אחת עשרה מצבי-ריק הפכו ל-`EmptyState`; השאר ל-PAGE_LEAD / SECTION_TITLE / HERO_TITLE / PAGE_TITLE, **וכולם נאכפים ב-`tests/designTokens.test.ts`** |
| אפס משטח משוב — "לא מתאים לי" העלים כרטיס בשקט | `Toast`, מותקן בשלוש המעטפות. אישורים בלבד |
| **אפס** utilities מסוג `md:hidden`; תשעה קישורים בשורה נשברת ב-390px | `MobileNav` — tab bar + גיליון "עוד", מתחת ל-`md` בלבד |
| הדגשת מסלול פעיל רק בקונסולת האדמין | `NavLink`, בשלוש המעטפות |
| `px-4 py-2 text-sm` = 34px, והוא היה הווריאנט של "פרסם קריאה" ו"פיד קריאות" | `BUTTON_COMPACT` עם `min-h-11`, ב-28 מקומות |
| **13** מסכים דו-טוריים קיבעו סדר רק ב-`lg` | סדר בסיס תואם, כך שבטלפון הטור הראשי ראשון |
| `opacity-70` על כרטיס הוריד `text-muted` מ-4.51:1 ל-2.73:1 | רקע קנבס במקום עמעום, בכרטיס הצעה ובשורת התראה |
| ספירת ההתראות עזבה את השם הנגיש של הקישור | הוחזרה לתוך הקישור |

**מה שנשאר בכוונה:**

- **תמונת בעל המקצוע ב-`BidCard`** — ההערה שם טענה שזה בלתי אפשרי כי הבאקט
  פרטי, וזה כבר לא נכון מאז Phase 8. זו הרחבת עמודות ב-`bids_for_job()`, כלומר
  מיגרציה, ולכן **Phase 14**.
- **אנימציית הגעה להצעה חדשה** — מחייבת השוואת רשימות בצד הלקוח.
- **ביטול פעולה ב-toast** — ל-`job_dismissals` כבר יש `delete` ומדיניות "pro
  deletes own" מאז Phase 3, אז זו פעולת שרת אחת; השלב הזה נמנע מ-`lib/actions/`.
- **`e2e/helpers.ts`** בודק את הודעת ה-throttle באנגלית, שהאפליקציה ממפה לעברית
  לפני שהיא מגיעה ל-DOM — הענף הזה לא יכול לרוץ. ו-"widens the radius when
  asked" משנה את הקריאה הזרועה ולא מחזיר אותה, אז ריצה שנייה בלי `db:reset`
  נכשלת.

**הגדרת סיום:**
- [x] `npm run lint`, `npm run typecheck`, `npm run test` (433), `npm run build`
- [x] `npm run db:test` — 413 טענות pgTAP, ללא שינוי (אין מיגרציה בשלב)
- [x] `npm run test:e2e` — 152 עוברים על מסד שאופס
- [x] בדיקה ידנית ב-390px וב-1280px, RTL, בשלוש הזהויות
- [x] `tests/designTokens.test.ts` הורחב ב-4 קבועים ועובר ירוק

---

## Phase 13.6 — הלקוח מפסיק לבחור רדיוס ✅

**מטרה:** להוריד מהלקוח שאלה שאין לו שום דרך לענות עליה.

עד עכשיו קריאה הגיעה לבעל מקצוע רק בתוך **שני** הרדיוסים —
`least(pro_profiles.radius_km, jobs.search_radius_km)` — והלקוח בחר את החצי שלו
בטופס הפרסום. ההחלטה (המשתמש, 11.9.2026): החצי הזה מבוטל, והעמודה נמחקת.

**הנימוק:** לקוח לא יודע עד כמה אינסטלטור מוכן לנסוע, אין לו דרך לברר, ומספר
שניחש צמצם לו את השוק בשקט. בעל מקצוע שהגדיר 30 ק״מ כבר ענה על השאלה — לעצמו,
עם מידע שללקוח אין.

**מה השתנה:**
- `pro_serves_job(point, search_radius_km)` → `pro_serves_job(point)`, ואיתה
  מדיניות ה-RLS על `jobs`, `can_bid_on_job()` ו-`can_read_job_media()`.
- `pros_in_range()`, `pros_serving_job()` ו-`pros_near_point()` מודדות את אותו
  דבר — מספר על מסך שסופר משהו אחר מהפיד הוא הבטחה שהפיד מפר.
- `pros_near_point(lat, lng, radius)` → `pros_near_point(lat, lng)`.
- `jobs.search_radius_km` **נמחקה**, ואיתה ה-grant של הלקוח עליה.
- בטופס: שבבי הרדיוס ירדו. הספירה החיה מתחת לכתובת נשארה — היא עונה על שאלה
  שללקוח **כן** יש ("יש בכלל מישהו שם?"), ובעיר טיפוסית ביום ההשקה התשובה היא
  אפס, ועדיף לגלות את זה לפני שכותבים תיאור.
- `NoProsNearby` איבד את כפתור "הרחיבו את החיפוש" — אין יותר מה להרחיב. הוא
  אומר מה נכון ולא מציע כלום: כפתור שלא עושה כלום גרוע מאין כפתור, במיוחד במסך
  שמגיעים אליו אחרי אכזבה.

**הגדרת סיום:**
- [x] `npm run db:reset` נקי, `npm run db:test` — 410 טענות
- [x] הטענה ב-pgTAP הפכה: אותה קריאה בשישה ק״מ נראית ברדיוס 10, נעלמת ב-3
- [x] `npm run test` (427), `npm run lint`, `npm run typecheck`, `npm run build`
- [x] `npm run test:e2e` — 151
- [x] אין מחרוזת "רדיוס" באף מסך לקוח

---

## Phase 13.7 — משפך הלקוח ✅

**מטרה:** שהדרך מ"יש לי תקלה" ל"יש לי שעה שמישהו מגיע" תהיה קצרה, מזמינה וכנה — בלי לפתוח שום הרשאה במסד.

**מאיפה זה בא:** סקירת מוצר עם המשתמש (14.9.2026) שהציעה 29 רעיונות. המשתמש בחר שבעה, וארבעת אלה נוגעים לזרימת הלקוח. השלושה הנותרים הם Phase 13.8, והשאר ב"רשימת המתנה" בסוף הקובץ.

**למה לפני 14:** Phase 14 משפר את הרגע שבו לקוח בוחר בין הצעות. השלב הזה מביא אותו לרגע הזה מלכתחילה, ומוודא שיש לו שעה מוסכמת אחריו.

**החלטות המשתמש לפני קוד (14.9.2026):**
- **חלון הגעה:** חובה בקריאה של `today` ו-`tomorrow`, רשות ב-`this_week` ו-`flexible`. ב-`asap` נשאר `eta_minutes`.
- **צפיות:** הלקוח רואה **מספר**, ורק כל עוד אין אף הצעה על הקריאה. אחרי ההצעה הראשונה השורה נעלמת, כי "5 צפו ו-1 הציע" מלמד את הלקוח לחשוד בהצעה היחידה שיש לו.

**מה נכלל:**

*1. פרסום קודם, הזדהות בסוף.*
- **הבעיה:** `app/(customer)/(authed)/new-request/page.tsx` מתחיל ב-`requireRole("customer")`, ו-`proxy.ts` שולח גולש אנונימי למסך ה-OTP לפני שראה שדה אחד. זה סותר את מסמך האפיון, סעיף 2: *"ההרשמה קורית תוך כדי פרסום הקריאה הראשונה"*.
- **המסלול:** הדף עובר אל `app/(customer)/new-request/` ויוצא מ-`loginPathFor()` ב-`lib/routes.ts`. הוא קורא `getCurrentUser()` כאופציה, לא כשער.
- **ההתחברות:** `OtpLoginForm` מוצג בתוך כרטיס הסיכום, במצב חדש שבו הצלחה מחזירה תשובה במקום הפניה, והפרסום ממשיך באותו מסך.
- **טיוטה:** נשמרת ב-`localStorage`, עטופה ב-try/catch, ונמחקת אחרי פרסום.
- **מדיה:** `MediaFields` מעלה היום מיד, לנתיב `<customer_id>/<upload_group>/…`, וזה דורש זהות. אצל אנונימי הקבצים והקלטה נשמרים בזיכרון ועולים אחרי ההזדהות, לפני `createJob`. **מדיניות הבאקט לא משתנה.**
- **מה שאסור שישתנה:** `createJob` ממשיך לקרוא `requireRole("customer")`, ו-RLS לא זז. נפתחת נקודת כניסה, לא הרשאה.
- **קצה:** בעל מקצוע או אדמין מחובר שמגיע לדף רואה הודעה, לא טופס.

*2. תקלות נפוצות, ותיבת "מה קרה?".*
- **בטופס:** שבבים מ-`CATEGORY_COPY[slug].commonJobs` בשלב 2. לחיצה מכניסה את הטקסט לתחילת התיאור. הרשימות כתובות כבר מ-Phase 8, ומוצגות היום רק ב-`CategoryLanding`.
- **בדף הבית:** שדה אחד מעל אריחי התחומים ("המזגן מטפטף מים").
  - התאמה מקומית ב-`lib/content/intent.ts`, בלי AI: מילים נרדפות לכל תחום, לפי מילים שלמות, באותו עיקרון של `lib/maps/gazetteer.ts` — ומאותה סיבה: מילה עברית קצרה היא לפעמים חלק ממילה אחרת.
  - ההפניה היא ל-`/new-request?category=<slug>&q=<טקסט>`, והדף מעביר `initialDescription`.
  - "לא זוהה" היא תשובה לגיטימית: התיאור ממולא והתחום נשאר פתוח.

*3. "מה קורה עם הקריאה שלי".*
- **הטבלה `job_views`:** `(job_id, pro_id)` כמפתח, ו-`first_viewed_at`.
  - אין עליה grant לאף client role.
  - נכתבת רק דרך `record_job_view(p_job_id)`, פונקציית `security definer` שבודקת שהקורא בעל מקצוע מאומת והקריאה נגישה לו.
  - נקראת מ-`/pro/jobs/[jobId]` ומ-`/quote`.
- **הספירה:** `job_view_count(p_job_id)`, definer שמחזירה **מספר בלבד**, ורק לבעל הקריאה ולאדמין. באותה משפחה של `pros_in_range()`: הלקוח יודע כמה צפו, לעולם לא מי.
- **ציר זמן במסך ההצעות:** "נשלחה ל-N" ← "X צפו" ← "הצעה ראשונה". כל מספר נספר משורות.
- **`warn_quiet_jobs()` ב-pg_cron:**
  - `jobs.quiet_warned_at` בלי grant, וסוג התראה `no_bids_yet` אחרי 30 דקות בלי הצעה.
  - אותו דפוס של `warn_expiring_selections()`, ומשק בית בלבד: אם הסחיפה לא רצה, הלקוח במצב של היום.
- **`add_job_details(p_job_id, …)`:** הוספת טקסט ותמונות לקריאה ב-`open`/`bidding`.
  - **הוספה בלבד, לא שכתוב**, כי בעלי מקצוע כבר תמחרו את מה שנכתב.
  - בלעדיה, להתראה אין פעולה להציע.

*4. מועד מוסכם בהצעה.*
- **הפער:** להצעה יש היום `eta_minutes` בלבד, שמתאים ל"עכשיו". לקריאה של "מחר" או "השבוע" אין בשום מקום שעה מוסכמת, והלקוח לא יודע מתי לחכות בבית.
- **`bids.arrival_window_start` / `arrival_window_end`:**
  - נוספות ל-grant ה-INSERT של `bids`, כי הן חלק מההצעה של בעל המקצוע, כמו `price`.
  - check: הסוף אחרי ההתחלה, ורוחב עד 4 שעות.
  - טריגר: חלון חובה בקריאה של `today`/`tomorrow`, רשות ב-`this_week`/`flexible`, ולעולם לא בעבר.
  - `bids_for_job()` מקבלת `create or replace` שמחזירה אותן.
- **`SubmitBidForm`** בוחר חלון לפי `preferred_time`. אותו כלל ב-`lib/validation/bids.ts`.
- **`BidCard` ומסך המעקב** מציגים "מחר 16:00–18:00", עם "הוסף ליומן" (`.ics` שנבנה בדפדפן, בלי route).
- **תזכורת:** סוג התראה `visit_reminder` בבוקר הביקור, לשני הצדדים.

**הגדרת סיום:**
- [x] pgTAP — צפיות (8 טענות):
  - לקוח לא רואה מי צפה, ולא קורא את השורות.
  - לקוח אחר לא מקבל ספירה (42501).
  - בעל מקצוע לא רושם צפייה בקריאה מחוץ לרדיוס שלו, וצפייה חוזרת נספרת פעם אחת.
- [x] pgTAP — פרטים, נדנוד וחלונות (22 טענות):
  - `add_job_details` מוסיפה אחרי הטקסט המקורי, מסרבת לקריאה של אחר, לנתיב תמונה של אחר ולקריאה משובצת.
  - `warn_quiet_jobs` מנדנדת פעם אחת, לא קריאה בת עשר דקות ולא קריאה בת יומיים.
  - הצעה בלי חלון על קריאה של "מחר" נדחית, וכך גם חלון שעבר וחלון של שמונה שעות. קריאה של "השבוע" מקבלת הצעה בלי חלון.
  - הלקוח לא יכול להזיז את החלון, ורואה אותו ב-`bids_for_job()`.
  - ~~החלון לא משתנה ב-`accept_job()`~~ — לא נבדק כטענה נפרדת: `accept_job()` לא נוגעת בעמודות האלה, והטריגר יוצא מוקדם כשהן לא משתנות.
- [x] Vitest: המתאם ב-`lib/content/intent.ts` (20), `lib/validation/arrivalWindow.ts` (14, כולל סוף שעון הקיץ), ומיון "הגעה מהירה" לפי חלון. שניהם בתוך ספי הכיסוי. בדיקת הסנכרון של `NotificationKind` עוברת עם שני הסוגים החדשים.
- [x] E2E: גולש אנונימי ← ממלא עם תמונה ← מזדהה בחלון ← "הקריאה פורסמה", עם התמונה. תיבת "מה קרה?" ← התחום נבחר. הצעה על קריאה של היום לא נשלחת בלי חלון, והחלון מוצג ללקוח. `/new-request` פתוח לאנונימי, ו-`/new-request/published` עדיין מוגן.
- [x] `npm run db:reset`, `db:test` (440), `db:types`, `lint`, `typecheck`, `build`, `test` (464), `test:e2e` (154)
- [x] שורות חדשות במילון המונחים ב-`CLAUDE.md`, וארבעה כללים חדשים בסעיף 3
- [x] נבדק בצילומי מסך ב-390px וב-1280px, RTL: דף הבית, הטופס, מסך ההצעות (עם הצעות ובלי), וטופס ההצעה של בעל המקצוע
- [ ] בדיקה ידנית בדפדפן — **ממתינה לבדיקת המשתמש**

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **טריגר BEFORE רץ לפני מדיניות ה-RLS.** הגרסה הראשונה של `bids_check_arrival_window` ענתה ללקוח שמנסה לזייף הצעה "arrival window required" במקום 42501. שתי טענות pgTAP ישנות תפסו את זה, כי הן ציפו ל-42501. התיקון: הטריגר יוצא מוקדם לכל מי שאינו בעל המקצוע המאומת שמגיש הצעה משלו.
2. **ה-UPDATE grant של Phase 1 על `jobs.description` ו-`photo_urls` עדיין קיים.** לקוח יכול, דרך PostgREST, לשכתוב תיאור שבעלי מקצוע כבר תמחרו — בדיוק מה ש-`add_job_details()` בנויה למנוע. **נסגר ב-15.9.2026** (`20260919120000_revoke_job_rewrites.sql`): כל ה-UPDATE grant על `jobs` בוטל, יחד עם המדיניות שתחמה אותו, והטענות עודכנו — כולל שתיים חדשות שלקוח לא משכתב את התיאור שלו ולא מזיז את המיקום.
3. **פעולת שרת שנקראת מטופס פתוח לא יכולה לקרוא ל-`requireRole()`.** `countProsCovering()` עשתה את זה, ו-`redirect()` מתוך פעולת שרת היה מנווט גולש באמצע תיאור התקלה אל מסך ההתחברות.
4. **טופס עם `action` מאפס את השדות הלא-מבוקרים שלו כשהפעולה חוזרת.** אחרי שגיאת "נשלחו יותר מדי בקשות" שדה הטלפון מתרוקן. זו התנהגות קיימת גם במסך ההתחברות; בדיקת ה-E2E ממלאת את המספר מחדש.
5. **חלונות שנזרעו ביחס ל-`now()` נראו מוזר על המסך** ("22:44–00:44"), כי הצעה אמיתית תמיד מתחילה בשעה עגולה. הזריעה מעוגלת עכשיו לשעה.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 13.7 מתוך `docs/roadmap.md`: משפך הלקוח. התחל מ"פרסום קודם, הזדהות בסוף", כי הוא משנה את נקודת הכניסה שכל השאר נשען עליה. הכלל שאסור לשבור: **נפתח המסך, לא ההרשאה** — `createJob` ממשיך לדרוש לקוח מחובר, ומדיניות `job-media` לא משתנה. ואחריו: כל מספר חדש על המסך נספר משורות, והלקוח לעולם לא יודע *מי* צפה בקריאה שלו.

---

## Phase 13.8 — חזרה וצמיחה ✅

**מטרה:** סיבה לפתוח את Handy גם כשאין תקלה, ודרך שבה בעלי מקצוע מביאים את הלקוחות שכבר יש להם.

**החלטות המשתמש לפני קוד (14.9.2026):**
1. **פטור מ-35 ₪ — הוכרע: לקוח חדש בלבד, עבודה ראשונה בלבד.**
   - הקריאה נפתחה דרך הקישור האישי של בעל המקצוע (`requested_pro_id`).
   - ללקוח **אין אף עבודה קודמת** ב-Handy שבעל מקצוע כלשהו קיבל.
   - `accept_job()` מחייב 0 ₪ **רק** לבעל המקצוע המבוקש, ורק על הקריאה הזו.
   - ה"לקוח חדש" הוא מה שמונע ניצול: לקוח שהגיע דרך הפיד כבר אינו חדש, ושליחת הקישור אליו לא חוסכת דבר.
2. **חלון הבלעדיות — הוכרע: עד שבעל המקצוע מוותר.**
   - קריאה מופנית לא נפתחת לפיד מעצמה.
   - היא נפתחת כשבעל המקצוע לוחץ "לא מתאים לי", **או כשהלקוח לוחץ "פתחו לכל בעלי המקצוע"**. הכפתור הזה נוסף כדי שלקוח לא ייתקע מול בעל מקצוע שלא עונה.
3. **מחוץ לרדיוס — הוכרע: נשלחת אליו בכל זאת.**
   - הלקוח בחר בו בשמו, והרדיוס קובע רק מה מגיע אליו מהפיד.
   - ⚠ **זה משנה את כלל סעיף 3 ב-`CLAUDE.md`** ("A job reaches a pro inside the pro's own radius, and that is the whole rule"). הכלל נכתב מחדש באותו commit שמשנה את `pro_serves_job()`, עם טענת pgTAP שקריאה מופנית מגיעה לבעל המקצוע המבוקש **ולאף אחד אחר מחוץ לרדיוס**.
4. **ברקוד QR — אושרה תלות npm חדשה.**
5. **ניסוי ה-AI — נדחה.** סעיף 7 לא ממומש בשלב הזה, וההחלטה נשארת פתוחה בסעיף 9.

**מה נכלל:**

*5. תיק הבית.*
- **המסך:** `/account/home`, תחת `/account` ולכן כבר מוגן. קישור מ-`/account` ומ-`MobileNav`.
- **`my_home_record()`:**
  - מקבצת עבודות שהושלמו לפי קרבה (`ST_DWithin`, כ-150 מ׳) לכתובות ב-`saved_places`. עבודה שלא קרובה לאף כתובת נכנסת ל"כתובות אחרות".
  - **security invoker, לא definer:** RLS כבר נותן ללקוח את השורות שלו, ואין כאן שום חשיפה שמצדיקה פונקציה מוגברת.
- **כל שורה:** תחום, תאריך סיום, בעל המקצוע עם קישור ל-`/pro/[slug]`, סכום מ-`job_effective_price()`, קבלה מ-`/api/receipts/[jobId]`, ותמונות הקריאה.

*6. הקישור האישי של בעל המקצוע.*
- **המנגנון, שעבר לכאן מ-Phase 14:**
  - `jobs.requested_pro_id` נקבע רק ביצירת הקריאה.
  - קריאה מופנית נשלחת קודם לבעל המקצוע המבוקש לחלון קצר, ורק אחר כך נפתחת לפיד. זה נאכף ב-RLS וב-`open_jobs_for_pro()`, לא ב-UI.
  - `/new-request?pro=<slug>` ממלא אותו.
- **ב-`/pro/profile`:** כרטיס שיתוף עם הקישור, כפתור לוואטסאפ, וברקוד QR להדבקה על הרכב.
  - ⚠ QR דורש תלות חדשה, באישור המשתמש לפני התקנה.
- **הפטור:** נבנה **רק** אם הוכרע.
  - אז `job_acceptance_fee()` תקבל את הקריאה ואת בעל המקצוע, ו-`accept_job()` ימשיך לקרוא את הסכום ממנה ולעולם לא מהקורא.
  - אם לא הוכרע, השלב נסגר בלי שורת קוד בנושא, וההחלטה נשארת פתוחה בסעיף 9.

*7. צילום ← קריאה — ניסוי מדידה. **נדחה (14.9.2026), לא ממומש בשלב הזה.** התיאור נשאר כאן לשלב שבו יוחזר.*
- **הכלי:** `scripts/photo-intake-spike.mjs`, מורץ ביד כמו `build-gazetteer.mjs`, ולא חלק מה-build.
  - על 20 תמונות אמיתיות של תקלות, הוא מריץ Claude Haiku 4.5, Claude Sonnet 5 ו-Claude Opus 5.
  - הוא מודד טוקנים ועלות **בפועל**, דיוק בסיווג לאחד מעשרת התחומים, ואיכות טיוטת תיאור בעברית.
  - התוצאה היא דוח קצר.
- **הערכה מוקדמת, לא מדידה:** כ-2 אגורות לקריאה ב-Haiku 4.5, כ-4–5 ב-Sonnet 5, וכ-10–15 ב-Opus 5. גם הגבוהה שבהן היא פחות מאחוז אחד מדמי קבלת העבודה, ולכן **העלות כנראה לא מה שיכריע**.
- **מה שכן יכריע:**
  - ספק חיצוני בנתיב הקריטי.
  - תמונה מתוך בית של אדם שנשלחת לצד שלישי.
  - גולש אנונימי שמריץ עלויות.
- **הדרישות לניסוי:** `@anthropic-ai/sdk` כתלות פיתוח, ו-`ANTHROPIC_API_KEY` ב-`.env.example`.
- **אם יאושר, זה שלב נפרד ולא המשך של זה.** הוא יכלול:
  - שורה בסעיף 2.
  - פעולת שרת מאחורי דגל, **שממלאת את הטופס ולעולם לא מפרסמת**.
  - אישור מפורש לפני שליחת תמונה, והגבלת קצב.
  - טופס רגיל שעובד כשהשירות לא זמין.
  - סעיף ב-`lib/content/legal.ts`.

**הגדרת סיום:**
- [x] pgTAP — קריאה מופנית (20 טענות):
  - אי אפשר להפנות קריאה למי שאינו בעל מקצוע מאומת, ואי אפשר להפנות אותה מחדש אחרי היצירה.
  - בעל המקצוע המבוקש קורא אותה גם באילת, והיא ראשונה בפיד שלו ומסומנת. בעל מקצוע אחר ברדיוס לא רואה אותה ולא יכול להציע עליה.
  - רק בעל המקצוע המבוקש מקבל התראה; "לא מתאים לי" של אחר לא פותח כלום.
  - "לא מתאים לי" של המבוקש, או `open_job_to_all()` של הבעלים, פותחים אותה, מודיעים ללקוח ולבעלי המקצוע ברדיוס, ולא מרחיבים רדיוס של אף אחד.
- [x] pgTAP — הפטור (9 טענות): לקוח חדש דרך הקישור ← 0 ₪, גם בכרטיס האישור וגם בשורת החיוב; `accept_job()` פעמיים ← שורה אחת; העבודה השנייה של אותו לקוח ← 35 ₪; לקוח עם עבודה קודמת ← 35 ₪; בעל מקצוע שאינו המבוקש ← 35 ₪; הפונקציה הדו-ארגומנטית לא נגישה ללקוח.
- [x] pgTAP — תיק הבית (3 טענות): עבודה נכנסת תחת הכתובת השמורה שלידה, ולקוח אחר לא רואה עבודה או כתובת של לקוח A.
- [x] ~~דוח הניסוי ל-AI~~ — **נדחה על ידי המשתמש**, וההחלטה נשארת פתוחה בסעיף 9
- [x] הכלל על ההגעה לבעל מקצוע ועל העמלה בסעיף 3 של `CLAUDE.md` נכתבו מחדש, הפטור קופל מסעיף 9 לסעיף 3, ושבע שורות נוספו למילון המונחים
- [x] E2E: הקישור והברקוד מופיעים ב-`/pro/profile`; `?pro=` מציג את בעל המקצוע בטופס; הפיד מסמן "ביקש אותך"; הלקוח רואה על מי הקריאה ממתינה ופותח אותה לכולם; תיק הבית מציג קבלה והזמנה חוזרת
- [x] `npm run db:reset`, `db:test` (472), `db:types`, `lint`, `typecheck`, `build`, `test` (465), `test:e2e` (156)
- [x] נבדק בצילומי מסך: הטופס עם בעל מקצוע מבוקש (1280px), תיק הבית (1280px), והפרופיל של בעל המקצוע (390px)
- [ ] בדיקה ידנית בדפדפן — **ממתינה לבדיקת המשתמש**

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **`my_home_record()` אינה יכולה להיות security invoker**, כפי שהשלב תכנן: שם בעל המקצוע וה-slug שלו נמצאים ב-`profiles` וב-`pro_profiles`, שסגורות ללקוחות בכוונה. היא definer במשפחה של `my_saved_pros()` — מוגבלת ל-`auth.uid()` בתוך ה-WHERE שלה, ומפרטת כל עמודה שהיא מחזירה.
2. **"הזמנה חוזרת" הייתה אסורה ב-`tests/copyPromises.test.ts`** כהבטחה בלי קוד. בתיק הבית יש לה עכשיו קוד — קריאה מופנית לאותו בעל מקצוע — ולכן הכלל מתיר אותה בקובץ הזה בלבד. הרשימה השמורה ב-`/account` עדיין בלי מסלול, ונשארת ל-Phase 14.
3. **`truncate` על הקישור גלל את `/pro/profile` לרוחב בטלפון** (450px ב-390px). בלי `min-w-0` על פריט ה-grid הוא לא חותך כלום. בדיקת ה-RTL הקיימת תפסה את זה. הקישור מוצג עכשיו במלואו, שבור לשורות — ממילא מעתיקים אותו.
4. **כרטיס "מה קורה אחרי הפרסום?"** המשיך לומר שהקריאה נשלחת לכל בעלי המקצוע גם כשהיא מופנית לאחד. נראה רק בצילום מסך.
5. **הכרטיס עם הקישור האישי יושב בתחתית `/pro/profile` בטלפון**, אחרי הביקורות — העמודה הצדדית היא `order-2` בכל רוחב. לא הוזז בשלב הזה; אם הקישור יתגלה ככלי הגיוס המרכזי, מקומו בדשבורד.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 13.8 מתוך `docs/roadmap.md`: תיק הבית והקישור האישי של בעל המקצוע, לפי ההחלטות שבראש השלב. ניסוי ה-AI נדחה ולא נבנה. הכלל שאסור לשבור: **הסכום נקבע בתוך `accept_job()` ולא אצל הקורא**, והפטור נבדק ב-pgTAP בכל אחד מהתרחישים שבהגדרת הסיום, כולל הניסיון לנצל אותו.

---

## Phase 14 — אמון ברגע הבחירה, וסגירת לולאת החזרה ✅

**מטרה:** שהמסך שבו לקוח מחליט את מי להכניס הביתה יציג את מה שכבר קיים במסד הנתונים — ושהזמנה חוזרת תפסיק להיות קופי בלבד.

**מה נכלל:**
- **חיבור `BidCard` לפרופיל הציבורי.** היום הכרטיס מציג ראשי תיבות בריבוע אפור, דירוג ומחיר — בזמן ש-`/pro/[slug]` כבר מכיל תמונה, גלריית עבודות, ביקורות מאומתות ומסמכים. `bids_for_job()` **כבר עושה `join public.pro_profiles`**, ולכן זה `create or replace` שמוסיף `public_slug`, `avatar_path`, `years_experience` ו-`reviews_count` — לא שאילתה חדשה. התמונה מגיעה מ-`pro-media`, שהוא הבאקט הציבורי: אין שאלת RLS ואין URL חתום שפג.
- **"הצצה מהירה"** — פאנל נשלף עם הביו, שלוש ביקורות אחרונות והגלריה, מ-`pro_public_profile()` ו-`pro_public_reviews()` שכבר מוענקות ל-`anon`.
- **ספירה לאחור לכל הצעה.** `minutesLeft` מחושב היום רק לשורת האזהרה המצרפית.
- **סגירת לולאת ההזמנה החוזרת:** `saved_pros` באזור האישי הוא היום `<li>` בלי קישור, בזמן ש-3 מקומות בקופי מבטיחים "הזמנה חוזרת בלחיצה אחת". להפוך אותו ללחיץ (`/new-request?pro=<slug>`) ולהעביר `savedPros` ל-`PostJobForm`. **המנגנון עצמו — `jobs.requested_pro_id` וחלון הבלעדיות — עבר ל-Phase 13.8**, כי הקישור האישי של בעל המקצוע נשען עליו. כאן נשאר רק החיבור של הרשימה השמורה אליו.
- **"מה זה בדרך כלל עולה"** בשלב בחירת התחום, מ-`pricing_guide()` — פונקציה שכבר קיימת, כבר סופרת רק עבודות שנסגרו, וכבר יודעת לומר "אין מספיק נתונים". מורידה הלם מחיר **בלי מספר מומצא אחד**.
- **תג "עונה מהר"** מ-`my_bid_stats.avg_response_minutes`, שמחושב היום ולא מוצג ללקוח בשום מקום.
- **תזכורת דירוג** אחרי סגירה (אפשרית רק עכשיו, אחרי Phase 13). דירוגים הם הדלק של כל השאר בשלב הזה.

**הגדרת סיום:**
- [x] לקוח מגיע מהצעה לפרופיל ובחזרה בלי לאבד את ההשוואה — "פרופיל וביקורות" פותח את הפרופיל בפאנל מעל הרשימה, והפרופיל המלא נפתח בכרטיסייה חדשה. E2E בודק שמספר ההצעות וה-URL לא זזים
- [x] ~~טענת pgTAP שחלון הבלעדיות באמת מסתיר את הקריאה משאר הפיד, **ואז משחרר אותה**~~ — עברה ל-Phase 13.8 יחד עם המנגנון, ונבדקה שם
- [x] אף מחרוזת "הזמנה חוזרת" לא נשארת בלי נתיב — שני המסכים שאומרים אותה (`/account` ותיק הבית) מובילים לקריאה מופנית, ו-`tests/copyPromises.test.ts` מתיר אותה רק בהם
- [x] pgTAP (10 חדשות): ההצעה נושאת slug ומספר ביקורות; זמן תגובה לעולם לא מוצג מתחת לשלוש הצעות, ומוצג מעליהן; הפונקציה אינה נגישה ללקוח; בעל מקצוע שמור נושא slug; תזכורת הדירוג נשלחת פעם אחת, ללקוח הנכון, ולא לעבודה בת 12 יום
- [x] `npm run db:reset`, `db:test` (484), `db:types`, `lint`, `typecheck`, `build`, `test` (466), `test:e2e` (158)
- [ ] בדיקה ידנית ב-RTL — **ממתינה לבדיקת המשתמש**

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **ל-`pro_public_profile()` אין רצפת מדגם לזמן התגובה** מאז Phase 8 — בעל מקצוע עם הצעה מהירה אחת נקרא "עונה תוך 2 דקות". בכרטיס ההצעה המספר הזה יושב ליד מחיר, ולכן `pro_response_minutes()` דורשת שלוש הצעות. הדף הציבורי עדיין מציג את המספר בלי רצפה — **לתקן כשנוגעים בו.**
2. **`lib/content/help.ts` מבטיח "ביטול לפני בחירת הצעה הוא ללא עלות ובלחיצה אחת"** — ואין במוצר ביטול בכלל. `tests/copyPromises.test.ts` לא תופס את זה כי אין לו כלל על ביטול. שייך ל-Phase 15, שמחכה להכרעות המשתמש.
3. **הודעות "The destination stream closed early" בלוג השרת** בזמן `test:e2e` — נבדקו מול commit מלפני Phase 13.7 ומופיעות שם באותה כמות (14, כולן מ-`e2e/rtl.spec.ts`, שמנווט מהר בין מסכים ומבטל תגובות בזרימה). קיימות, לא נגרמו בשלב הזה.
4. **תיקון ביניים לפני השלב:** `20260919120000_revoke_job_rewrites.sql` ביטל את כל ה-UPDATE grant על `jobs` — החוב שנרשם ב-Phase 13.7.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 14 מתוך `docs/roadmap.md`: לחבר את מסך השוואת ההצעות לפרופיל הציבורי שכבר קיים, ולסגור את לולאת ההזמנה החוזרת שהקופי מבטיח בשלושה מקומות. שים לב ש-`bids_for_job()` כבר עושה join ל-`pro_profiles` — זו הרחבת עמודות, לא שאילתה חדשה. אל תרחיב שום מדיניות RLS על `pro_profiles`: מה שציבורי עובר דרך פונקציות ה-definer של Phase 8.

---

## Phase 15 — ביטול, אי-הגעה, והוגנות הכסף ✅

**מטרה:** לסגור את החור ש-`CLAUDE.md` סעיף 9 מסמן כשלוש שאלות פתוחות שהן החלטה אחת — ושה-Phase 10 הפך לכואב.

**למה זה דחוף עכשיו:** `jobs.status` מכיל `cancelled` **בלי שום מעבר לתוכו**, ויש עמוד מדיניות ביטול ציבורי (`/cancellation`) בלי שום משטח מוצרי שמממש אותו. מאז Phase 10 זה כבר לא חסר תיאורטי: **בעל מקצוע משלם 35 ₪ לא מוחזרים לפני העבודה**, ולקוח שנעלם או ביטל בטלפון עולה לו כסף בלי שום מסלול במוצר.

> **⚠ השלב הזה דורש הכרעות של המשתמש לפני שכותבים קוד** (`CLAUDE.md` סעיף 8 — כסף אמיתי):
> 1. מי רשאי לבטל, ועד מתי? (לפני בחירה / אחרי בחירה לפני אישור / אחרי אישור)
> 2. מה קורה ל-35 ₪ כשהלקוח מבטל אחרי שבעל המקצוע אישר? *זיכוי לעבודה הבאה הוא התשובה שאינה נוגעת בהחלטה "הדמים אינם מוחזרים", ולכן ככל הנראה הנכונה.*
> 3. אי-הגעה של לקוח — מי מכריז, ומה ההוכחה? (`job_locations` כבר מוכיח שבעל המקצוע הגיע לכתובת)

**ההכרעות (המשתמש, 15.9.2026) — לפני שנכתבה שורת SQL:**
1. **מי מבטל ועד מתי:** הלקוח מבטל בעצמו **עד שבעל מקצוע מאשר** — לפני בחירה, וגם אחרי בחירה שעוד לא נענתה. אחרי האישור, הביטול עובר דרך **בעל המקצוע** ("הלקוח ביטל") או **האדמין**.
2. **ה-35 ₪ כשהלקוח מבטל אחרי שבעל המקצוע אישר:** **זיכוי לעבודה הבאה.** הדמים לא מוחזרים (ההכרעה מ-8.9.2026 נשארת), אבל העבודה הבאה שבעל המקצוע יאשר לא תחויב.
3. **אי-הגעה של לקוח:** **לא בשלב הזה.** נשאר פנייה לתמיכה.
4. **דירוג דו-כיווני:** **כן — בעל המקצוע מדרג את הלקוח, והדירוג פרטי לאדמין בלבד.** לא מוצג לבעלי מקצוע אחרים.

**שתי תת-הכרעות שנגזרו מהן במימוש, ונרשמות כאן כדי שיהיה אפשר לערער עליהן:**
- **ביטול של בעל מקצוע אחרי האישור מודיע ללקוח**, והלקוח יכול לפתוח מחלוקת אם לא ביטל. בלי זה, בעל מקצוע יכול לאשר, לבצע את העבודה מחוץ לפלטפורמה ולדווח "ביטול" כדי לקבל זיכוי. מספר הביטולים לכל בעל מקצוע מוצג בקונסולה.
- **הזיכוי נצרך אוטומטית** בעבודה הבאה שבעל המקצוע מאשר. עבודה שלא חויבה (הפטור מ-13.8) לא מייצרת זיכוי — אין מה להחזיר.

**מה נכלל (בכפוף להכרעות):** מעבר `cancelled` אמיתי כפונקציית `security definer` עם כללי מי-ומתי; מסך ביטול לשני הצדדים; דיווח אי-הגעה; **דירוג דו-כיווני** — בעל מקצוע מדרג לקוח, התרופה הסטנדרטית לאי-הגעה ובסיס ל"לקוח מאומת"; והצגת מונה הביטולים בקונסולת האדמין — המונה שהעיצוב ביקש ו-Phase 6 סירב לצייר כאפס קבוע.

**הגדרת סיום:**
- [x] שלוש ההכרעות (וההכרעה על הדירוג) התקבלו ותועדו כאן ובסעיף 9 של `CLAUDE.md` לפני שנכתבה שורת SQL — commit נפרד, `docs: record the user's phase 15 decisions before any SQL`
- [x] אין מסלול לקוח או בעל מקצוע שמגיע ל-`cancelled` בלי לעבור בפונקציית ה-definer — pgTAP: עדכון ישיר נדחה (42501), ואילוץ check קושר את הסטטוס לשדות הביטול
- [x] מה שקורה ל-35 ₪ נבדק במפורש ב-pgTAP, בכל תרחיש:
  - ביטול לפני אישור ← אין זיכוי.
  - ביטול של בעל מקצוע אחרי אישור ← זיכוי של 35 ₪, והעבודה הבאה מראה 0.
  - הזיכוי נצרך באישור הבא, שורת החיוב ב-0, והזיכוי מתעד על איזו עבודה נוצל. העבודה שאחריה חוזרת ל-35.
  - עבודה בפטור (0 ₪) ← אין זיכוי. עבודה שהזיכוי כיסה ובוטלה ← אין זיכוי שני.
  - ביטול כפול נדחה, וסגירת עבודה מבוטלת נדחית.
- [x] `/cancellation`, סעיף 5 בתקנון ומרכז העזרה מתארים את מה שהקוד עושה, סעיף מול סעיף — כולל שלאי-הגעה אין מנגנון
- [x] דירוג דו-כיווני, פרטי לאדמין: pgTAP — רק על עבודה שהסתיימה, פעם אחת, רק בעל המקצוע שביצע, בלי insert grant; הלקוח ובעל מקצוע אחר לא קוראים, האדמין קורא
- [x] מונה הביטולים בקונסולה, עם בעל המקצוע שמדווח הכי הרבה
- [x] `npm run db:reset`, `db:test` (524), `db:types`, `lint`, `typecheck`, `build`, `test` (473), `test:e2e` (160)
- [x] נבדק בצילומי מסך: טופס הביטול בטלפון, פאנל הפרופיל מעל ההצעות, והיסטוריית העבודות עם הדירוג
- [ ] בדיקה ידנית ב-RTL — **ממתינה לבדיקת המשתמש**

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **טופס ההצעה אמר "לקוח חדש שהגיע דרך הקישור האישי שלך" על כל עבודה בלי דמים** — וזיכוי מביטול מאפס את הדמים באותה צורה. זו טענה שקרית לבעל מקצוע על כסף. נתפס ב-E2E של הזרימה הקריטית (שני אלמנטים עם אותו טקסט), ותוקן ב-`my_base_fee_for_job()`, שמבחינה בין פטור לזיכוי.
2. **בדיקות E2E שחולקות בעל מקצוע חולקות גם את הזיכויים שלו.** בדיקת הביטול נתנה לבעל המקצוע של הדמו זיכוי, והזרימה הקריטית שרצה אחריה ציפתה ל-35 ₪. בדיקת הביטול רצה עכשיו כבעל מקצוע זרוע אחר (מוסא חדד).
3. **מרכז העזרה עוד הציע "להרחיב את רדיוס החיפוש"**, שנמחק ב-Phase 13.6. תוקן יחד עם טקסט הביטול.
4. **הרצת pgTAP אחרי E2E בלי `db:reset` נכשלת ב-15 טענות** שאין בהן שום באג — ה-E2E משנה את הזריעה. אותה מלכודת שהזיכרון מתעד לגבי ה-sessions ב-`e2e/.auth`.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 15 מתוך `docs/roadmap.md`: ביטול קריאה, אי-הגעה, והוגנות ה-35 ₪. **אל תתחיל לכתוב קוד לפני ששלוש ההכרעות שבראש השלב התקבלו ממני** — הן נוגעות בכסף אמיתי, וסעיף 8 של `CLAUDE.md` שומר אותן אצלי. הצג לי את התרחישים ואת ההשלכה של כל אפשרות, וחכה לתשובה.

---

## Phase 16 — שימור בעל מקצוע ✅

**מטרה:** שבעל מקצוע ימשיך לפתוח את האפליקציה. הוא צד ההיצע, והוא זה שמשלם.

**מה נכלל:**
- **דשבורד שלם.** להחזיר את חצי ההכנסות שנדחה ב-Phase 6 ומעולם לא חובר — `my_earnings_stats()` ו-`my_bid_stats()` כבר קיימות ויושבות רק ב-`/pro/wallet`. וגם `RealtimeRefresh` על הדשבורד: היום אין, וכרטיס האישור שפג בשעתיים יושב בדיוק עליו.
- **פיד שאפשר לעבוד איתו:** מיון (חדש / קרוב / פחות הצעות), סינון לפי תחום ולפי דחיפות, Realtime, ו"פורסם לפני N דקות" על כל כרטיס — במקום סרט "חדש" בינארי, בפיד שהמוצר עצמו טוען שמהירות מכריעה בו.
- **הצעה בלחיצה אחת:** מחיר וזמן ברירת מחדל לכל תחום → "הגש הצעה מהירה" ישירות מהפיד. זו התשובה האמיתית לעצת ה-10 דקות, במקום לחזור עליה בארבעה מסכים.
- **שקיפות אכיפה וסיבות:** `set_pro_verification()` לא שומרת שדה סיבה בכלל, ו-`ProStatusCard` מנחש בקול ("לרוב מדובר במסמך לא קריא"). להוסיף `reason` ולהציג אותה. ולחשוף את `price_updates_blocked` ו-`documents_required_at`, ש**אינם נקראים היום ב-`PRO_COLUMNS` בכלל** — בעל מקצוע שנחסם מגלה את זה כשהוא נכשל בשליחה.
- **לתעד סיבה** ב-`decline_job()` וב-`job_dismissals` (רשימה סגורה), שהיא גם הנתון היחיד שיאפשר מתישהו התאמה חכמה יותר מרדיוס.
- **"מאמן תמחור"** — להפוך את הארנק מדוח לכלי: "ההצעות שלך באינסטלציה גבוהות ב-18% מהטווח שנבחר באזור", מחושב מ-`bids` ו-`similar_bid_range`.
- **ניווט מובייל — ~~נעשה ב-Phase 13.5~~.** ה-tab bar, גיליון ה"עוד" והדגשת המסלול הפעיל קיימים בשלוש המעטפות. **מה שנשאר כאן:** איחוד עריכת הפרופיל, שמפוצלת היום על פני ארבעה מסלולים (אחד מהם הוא טופס ההרשמה) — זה שינוי מבנה מסלולים, לא ניווט.
- **קיצור ההרשמה:** פרטי הבנק אינם נדרשים ב-`submit_pro_for_approval()` ואין סליקה מאחוריהם — להזיז אותם מחסם לתזכורת אחרי האישור, ולהפוך את שלב התרגול לאופציונלי. המטרה: לראות קריאות אמיתיות מוקדם ככל האפשר.
- **בזמן שנוגעים בפיד — לתקן את חוב #27.** `open_jobs_for_pro()` היא סריקה לינארית (1265ms על 50k קריאות), וזה נתיב הקריאה המרכזי של המרקטפלייס. הוספת מיון וסינון בלי לתקן את האינדקס תחמיר את זה.

**הגדרת סיום:**
- [x] אין מצב אכיפה שבעל המקצוע לא רואה על מסך משלו — `verification_reason`, `price_updates_blocked` ו-`documents_required_at` נקראים ומוצגים בכרטיס הסטטוס, והניחוש "לרוב מדובר במסמך לא קריא" נמחק
- [x] ~~`npm run perf:postgis` מראה שיפור מדיד על הפיד~~ — **הוכרע אחרת (המשתמש, 15.9.2026): להשאיר את הפיד ב-RLS ולנטר.** בדרך נמצאה ותוקנה רגרסיה מ-Phase 13.8 (585ms ← 240ms, ראו `TECHNICAL_DEBT.md` #26). הסידור והסינון החדשים רצים על השורות שהפוליסה החזירה, כך שהעלות לא זזה: 254ms בסוף השלב, מול תקציב 400
- [x] הפיד, ההצעות והדשבורד עובדים ב-390px בלי שורת ניווט שנשברת *(Phase 13.5)*
- [x] pgTAP (12 חדשות): הסיבה של האדמין נקראת על ידי בעל המקצוע, לא ניתנת למחיקה על ידו ומתנקה באישור; סיבות ויתור והסתרה הן אוצר מילים סגור; הלקוח לא קורא למה בעל מקצוע ויתר, האדמין כן
- [x] Vitest: סידור הפיד, ההצעה המהירה, מאמן התמחור, פרטי בנק "הכול או כלום", וסנכרון סיבות הוויתור מול המיגרציה
- [x] E2E: הדשבורד, מיון הפיד והסתרה עם סיבה, והמאמן בארנק
- [x] `npm run db:reset`, `db:test` (538), `db:types`, `lint`, `typecheck`, `build`, `test` (485), `test:e2e` (163)
- [x] נבדק בצילומי מסך: הפיד, ההגדרות ומרכז העזרה ב-1280px, והדשבורד ב-390px
- [ ] בדיקה ידנית ב-RTL — **ממתינה לבדיקת המשתמש**

**מה שלא נבנה, ולמה:**
- **איחוד עריכת הפרופיל** (ארבעה מסלולים) — שינוי מבנה מסלולים, ולא נדרש בהגדרת הסיום. עבר לרשימת ההמתנה.
- **האינדקס של הפיד** — הוכרע להשאיר ולנטר, ראו למעלה.

**מה שהתגלה בבנייה, ולא נראה מקריאת קוד:**

1. **חמישה מסכים דו-טוריים הציגו את התוכן הראשי בעמודה הצרה בדסקטופ** — הפיד, ההגדרות, עריכת הפרופיל, ההרשמה ומרכז העזרה. התבנית הייתה `[20rem_minmax(0,1fr)]` בזמן שהטור הראשי הוא `order-1`, ולכן קיבל את ה-20rem. הבאג קדם ל-Phase 13.5 ונראה רק בצילום מסך מול `design/screens/pro-2.2-job-feed.png`. תוקן בכל החמישה.
2. **הפיד היה איטי פי 2.75 מאז Phase 13.8** — פונקציית definer לא עוברת inline. תוקן בענף נפרד לפני השלב, ו-`CLAUDE.md` דורש מעכשיו `perf:postgis` בכל שינוי מדיניות על `jobs`.
3. **`decline_reason` כעמודה על `bids` הייתה חושפת ללקוח למה בעל המקצוע ויתר**, כי ללקוח יש קריאה של כל עמודות ההצעות על הקריאה שלו. עברה לטבלה משלה לפני שנכתב קוד שקורא אותה.
4. **"אחוז ההצעות שהפכו לעבודה" נמדד על הצעות שהוכרעו בלבד**, ולכן "100%" ליד "5 מתוך 7" נראה סותר. ההסבר בכרטיס תוקן.
5. **קופי שנשאר מלפני Phase 13.6** — "גם בתוך הרדיוס שהלקוח ביקש" בהגדרות, במרכז העזרה ובמדיניות הפרטיות, ו"הרחיבו את רדיוס החיפוש" בדפי הקטגוריה. **ו"הגבייה מתבצעת כל שני וחמישי"** בשני מסכים, בלי קוד מאחוריה. כולם תוקנו.

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 16 מתוך `docs/roadmap.md`: שימור בעל מקצוע — דשבורד שלם, פיד שאפשר לעבוד איתו, שקיפות אכיפה, וניווט מובייל. שים לב שרוב הנתונים כבר קיימים בפונקציות שנבנו ב-Phase 4 וב-Phase 6 ופשוט לא חוברו למסך הבית. ותקן את האינדקס של `open_jobs_for_pro()` באותו שלב — לא אחריו.

---

## Phase 17 — צמיחה ורכישה

**מטרה:** להביא אנשים — אחרי שהמשפך כבר לא דולף.

**החלטות המשתמש לפני קוד (15.9.2026):**
- **הפניה — בלי תגמול.** כפתור שיתוף אחרי עבודה שהסתיימה, בלי זיכוי ובלי התחייבות כספית. ללקוח אין מה לזכות: הוא לא משלם ל-Handy.
- **קבלה לוואטסאפ — קישור שפג אחרי 7 ימים.** הלקוח מייצר אותו בלחיצה, הקישור נושא טוקן אקראי, פג אחרי שבוע, ואפשר לבטל אותו. מחזיק הקישור רואה את גרסת הלקוח של הקבלה — בלי דמי קבלת העבודה.

**מה נכלל:**
- **דף נחיתה לבעלי מקצוע עם ביקוש אמיתי:** "יש 7 קריאות פתוחות בפתח תקווה עכשיו", נספר מ-`jobs`, ובלי כלום להראות — אומר זאת. זו ההוכחה היחידה שמגייסת בעל מקצוע, והיא נעדרת לגמרי מ-`/pro` היום.
- **הפניה (referral):** הרגע היחיד עם כוונה גבוהה במוצר הוא מסך הסיכום אחרי עבודה מוצלחת, ואין בו שום דבר. הזיכוי מתחבר למנגנון שנבנה ב-Phase 15.
- **קבלה שאפשר לשלוח בוואטסאפ** — היום היא PDF מאחורי אימות.
- **עמודי SEO שנשמרים ב-cache:** 180 עמודי קטגוריה+עיר הם היום `force-dynamic` (חוב #19). `generateStaticParams` + `revalidate` — הם קוראים רק פונקציות ציבוריות ואין בהם זהות.
- **המדריכים נכנסים לזרימה:** `lib/content/guides.ts` מכיל 9 מדריכים שאיש לא מגיע אליהם. "מה כבר ניסיתם?" בטופס הקריאה — לפעמים יחסוך את הקריאה (וזה בונה אמון), ותמיד ייצר תיאור טוב יותר לבעלי המקצוע.
- **תיבת דואר ללקוח, ומחלוקת כשיחה:** ללקוח אין היום `/messages` חוצה-קריאות, ומחלוקת היא מחרוזת סטטוס שאף פעם לא מקבלת תשובה גלויה. ומסך לקריאת `support_tickets` בקונסולת האדמין — הטבלה שסעיף 9 מסמן כטבלה שאיש לא קורא.

**הגדרת סיום:**
- [ ] כל מספר בעמוד ציבורי נספר משורות, ואם אין מה לספור — העמוד אומר זאת
- [ ] עמודי הקטגוריה+עיר נבנים סטטית ונבדקים ב-Lighthouse
- [ ] כל שרשרת האימות + בדיקה ידנית ב-RTL

**טקסט קיקאוף לקלוד קוד:**
> ננוע ל-Phase 17 מתוך `docs/roadmap.md`: צמיחה ורכישה. הכלל שאסור לשבור הוא זה של סעיף 3 ב-`CLAUDE.md` — **אף מספר בעמוד ציבורי אינו מומצא**; אלו בדיוק העמודים שכל תפקידם הוא להיות אמינים.

---

## רשימת המתנה — רעיונות שלא נבחרו (14.9.2026)

אלה הרעיונות מסקירת המוצר שממנה נגזרו Phases 13.7 ו-13.8, ושהמשתמש לא בחר לעת עתה. הם כאן כדי שלא יוצעו שוב מאפס. **אף אחד מהם אינו התחייבות**, והמספור הוא של הסקירה המקורית.

| # | רעיון | גודל | הערה |
|---|---|---|---|
| 3 | הקלטה קולית כפעולה בולטת בטלפון ("ספרו לנו בקול") | S | תלוי בקהל היעד, שעוד לא הוחלט |
| 5 | שתיים-שלוש שאלות המשך סגורות לפי תחום ("יש נזילה פעילה?") | M | מקצר סבבי צ׳אט לפני הצעה |
| 6 | "עד שמגיעים": צעדי בטיחות מהמדריכים אחרי קריאה דחופה | S | תוכן קיים ב-`lib/content/guides.ts` |
| 7 | הטופס כאשף של שאלה-אחת-בכל-מסך, בטלפון בלבד | M | לבדוק מול העיצוב; תלוי בקהל היעד |
| 8 | "איך זה נראה" בדף הבית, עם צילום מסך מסומן "דוגמה" | S | בלי שמספר בצילום ייקרא כנתון |
| 12 | "שאלו את כולם": שאלה אחת לכל מי שהגיש הצעה | M | הודעה נפרדת בכל שיחה, כי שיחה היא (קריאה, בעל מקצוע) |
| 13 | פירוט אופציונלי בהצעה: עבודה / חלקים / מה כלול | M | שקיפות מחיר בלב המוצר |
| 16 | "להתכונן לביקור": 3–4 שורות לפי תחום במסך המעקב | S | תוכן בלבד |
| 17 | קישור מעקב לבן/בת הזוג, לצפייה בלבד | M–L | ⚠ מיקום חי של אדם: definer עם טוקן מוגבל בזמן, לעולם לא מדיניות |
| 19 | תזכורות תחזוקה עונתיות, ביוזמת המשתמש | M | משתלב טוב עם תיק הבית (13.8) |
| 20 | אחריות על העבודה, וקריאה חוזרת לאותו בעל מקצוע בתוכה | L | 💰 החלטה עסקית |
| — | איחוד עריכת הפרופיל של בעל המקצוע (ארבעה מסלולים, אחד מהם טופס ההרשמה) | M | הועבר מ-Phase 16: שינוי מבנה מסלולים, לא נדרש בהגדרת הסיום |
| 21 | תמונת "אחרי" בביקורת | S–M | מזינה את הגלריה בפרופיל הציבורי |
| 25 | הגשת הצעה ישר מה-push של "קריאה חדשה" | S | הרחבה של ההצעה המהירה ב-Phase 16 |
| 27 | הערות שמורות להצעה, בלחיצה | S | |
| 28 | "פנוי עכשיו": מתג לשעתיים הקרובות, ותג בכרטיס ההצעה | S–M | הצהרה של בעל המקצוע, לא מספר מחושב |
| 30 | "הזמינו את בעל המקצוע שלכם" כשאין בעל מקצוע באזור | S–M | הצד השני של הקישור האישי (13.8) |
| 31 | "תוקן השבוע באזור" בדפי עיר ותחום, מעבודות שנסגרו | M | ⚠ עיר בלבד, לעולם לא רחוב, ורק מעל סף מינימלי |
| 32 | הצעת התקנה למסך הבית גם ללקוח | S | באייפון בלי התקנה אין push, גם לא על בקשת שינוי מחיר |
| 34 | קריאה דרך וואטסאפ | L | 💰 דורש אימות Business ב-Meta וחברה רשומה (סעיף 9) |
| 35 | "הבטחת Handy": התחייבות מענה אחת וברורה | M | 💰 כסף וניסוח משפטי, תלוי ב-Phase 15 |

---

## מה שהתוכנית הזו לא פותרת, ונשאר בסעיף 9 של `CLAUDE.md`

- **הכסף עדיין לא זז.** `job_fees` נשאר פנקס בלי סליקה, ו-`disputes.credit_amount` זיכוי שאיש לא משלם. Phase 15 מסדר את **הכללים**; מי מחייב בפועל נשאר הכרעה של המשתמש.
- **Web Push לא מכסה את כולם** — iOS דורש התקנה למסך הבית, ומי שידחה את ההרשאה לא יקבל דבר. **זה בדיוק המקום שבו האדפטר של Phase 13 משתלם:** הוא הופך "להוסיף SMS" להחלטת כסף ולא לפרויקט.
- **נפח התראות הוא דרך חדשה לאבד בעל מקצוע.** האיחוד באצווה עוזר; מתגים לפי סוג ב-`/pro/settings` הם התשובה האמיתית, והם בהיקף Phase 13.
- **קודי הדמו והבייפס עדיין חיים על הפרודקשן.** סעיף 9 מגדיר את זה כסיכון מקובל שנפתח מחדש *"לפני המשתמש האמיתי הראשון"*. **השקה בעיר אחת היא בדיוק האירוע הזה** — ההכרעה צריכה ליפול לפני Phase 12, לא אחריו.
- **הטקסטים המשפטיים עדיין טיוטה** (`DRAFT_NOTICE`). Phase 13 מוסיף להם סעיף על מידע אישי, אבל השקה מול משתמשים אמיתיים מחייבת את הסקירה המשפטית שסעיף 8 מחזיק אצל המשתמש.

---

## סדר עדיפויות אם צריך לקצר את ה-MVP

_(נכתב על חלק א׳, Phases 0–11. סדר העדיפויות של חלק ב׳ נמצא בטבלה שבראשו.)_

אם יש לחץ זמן, אלו השלבים שאפשר לדחות **בלי** לשבור את ההבטחה המרכזית של המוצר (שקיפות מחיר):
- Phase 7 (לוח ניהול) אפשר לצמצם לגרסה מינימלית מאוד — הכי חשוב זה אישור בעלי מקצוע ומחלוקות.
- Phase 8 (SEO/תוכן) אפשר לדחות אחרי השקה — הוא לא חלק מהזרימה התפעולית.

**מה שאסור לקצר:** Phase 1 (DB+RLS) ו-Phase 5 (אכיפת עדכון מחיר) — אלו הבסיס והבטחת הליבה של Handy.
